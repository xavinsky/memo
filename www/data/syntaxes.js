const SYNTAXES_SECTIONS = [
  {id:"creation", group:"numpy", title:"Création de tableaux", rows:[
    ["Créer un tableau à partir d'une liste","**np.array**([1, 2, 3])","array([1, 2, 3])"],
    ["Tableau de zéros","**np.zeros**((3, 4))","matrice 3x4 remplie de 0"],
    ["Tableau de uns","**np.ones**((2, 2))","matrice 2x2 remplie de 1"],
    ["Matrice identité","**np.eye**(3)","matrice 3x3 avec des 1 sur la diagonale, 0 ailleurs"],
    ["Suite régulière (début, fin, pas)","**np.arange**(0, 10, 2)","array([0, 2, 4, 6, 8])"],
    ["N valeurs régulières entre deux bornes","**np.linspace**(0, 1, 5)","array([0., 0.25, 0.5, 0.75, 1.])"],
    ["Tableau aléatoire uniforme [0,1)","**np.random.rand**(3, 3)","matrice 3x3 de floats aléatoires"],
    ["Tableau d'entiers aléatoires","**np.random.randint**(0, 10, size=(2, 3))","matrice 2x3 d'entiers entre 0 et 9"],
  ]},
  {id:"attributs-numpy", group:"numpy", title:"Attributs d'un tableau (ndim, shape, size, dtype)", rows:[
    ["Nombre de dimensions","a.**ndim**","int — 1 pour un vecteur, 2 pour une matrice, etc."],
    ["Forme (dimensions) du tableau","a.**shape**","tuple — (lignes, colonnes) pour un tableau 2D"],
    ["Nombre total d'éléments","a.**size**","int — produit de toutes les dimensions de shape"],
    ["Type des éléments","a.**dtype**","ex: int64, float64 — tous les éléments d'un ndarray ont le même type, contrairement à une liste Python"],
    ["Reconvertir en liste Python native","a.**tolist**()","list (ou liste de listes) — utile pour interopérer avec du code qui attend des listes plutôt que des ndarray"],
  ]},
  {id:"indexation", group:"numpy", title:"Indexation et masques", rows:[
    ["Élément précis (ligne, colonne)","a**[1, 2]**","valeur unique à la position [1,2]"],
    ["Une ligne entière","a**[1]**","tableau 1D, la ligne d'index 1"],
    ["Une colonne entière","a**[:, 0]**","tableau 1D, la colonne d'index 0"],
    ["Sous-matrice (plage de lignes et colonnes)","a**[1:3, 0:2]**","matrice 2x2 extraite"],
    ["Toutes les lignes, colonnes choisies","a[:, **[0, 2]**]","colonnes 0 et 2 pour toutes les lignes"],
    ["Un axe entier avec un pas","a[**::2**, **::2**]","1 ligne sur 2, 1 colonne sur 2"],
    ["Inverser l'ordre d'un axe","a[**::-1**]","lignes dans l'ordre inverse"],
    ["Filtrer par condition","a[**a > 5**]","tableau 1D des éléments > 5"],
    ["Combiner plusieurs conditions","a[(a > 2) **&** (a < 8)]","éléments entre 2 et 8 (exclus)"],
    ["Remplacer selon condition","**np.where**(a > 5, 1, 0)","1 si > 5, sinon 0"],
    ["Remplacer en place tous les éléments vérifiant une condition","a[**a > 4**] = 99","modifie a directement (contrairement à np.where qui renvoie un nouveau tableau sans toucher a)"],
  ]},
  {id:"reshape-tri", group:"numpy", title:"Forme et tri des tableaux", rows:[
    ["Changer la forme d'un tableau","a.**reshape**(3, 4)","même données, forme 3x4"],
    ["Aplatir un tableau","a.**flatten**()","tableau 1D"],
    ["Transposer","a.**T**","lignes/colonnes inversées"],
    ["Trier un tableau (nouvelle copie)","**np.sort**(a)","tableau trié croissant, a n'est pas modifié"],
    ["Trier un tableau sur place","a.**sort**()","modifie a directement, ne renvoie rien"],
    ["Indices qui donneraient le tableau trié","**np.argsort**(a)","tableau des positions, dans l'ordre de tri"],
  ]},
  {id:"stats-numpy", group:"numpy", title:"Opérations et stats", rows:[
    ["Somme des éléments","a.**sum**()","scalaire"],
    ["Choisir l'axe d'agrégation sur un tableau 2D","a.sum(**axis=0**)  # somme par colonne (vertical)\na.sum(**axis=1**)  # somme par ligne (horizontal)","même principe pour mean()/std()/min()/max() — axis=0 écrase les lignes (1 résultat par colonne), axis=1 écrase les colonnes (1 résultat par ligne) : piège fréquent d'inverser les deux"],
    ["Moyenne","a.**mean**()","scalaire"],
    ["Écart-type","a.**std**()","scalaire"],
    ["Min / max","a.**min**(), a.**max**()","scalaire"],
    ["Index du max","a.**argmax**()","position (int)"],
    ["Concaténer deux tableaux","**np.concatenate**([a, b])","tableau fusionné"],
    ["Produit matriciel","**np.dot**(a, b)","matrice ou scalaire selon dimensions"],
    ["Opération élément par élément","**a + b**","tableau (broadcasting)"],
  ]},
  {id:"alea-lois", group:"numpy", title:"Tirages aléatoires (lois usuelles)", rows:[
    ["Entier aléatoire entre low (inclus) et high (exclu)","**np.random.randint**(1, 6+1)","int aléatoire entre 1 et 6 (ex: lancer de dé)"],
    ["Plusieurs tirages d'un coup","**np.random.randint**(1, 6+1, 3)","array de 3 entiers aléatoires"],
    ["Tirage(s) suivant une loi Binomiale(n,p)","**np.random.binomial**(n=10, p=0.7, size=20)","array de 20 résultats, chacun = nb de succès sur n essais"],
    ["N valeurs régulières pour tracer une courbe continue","**np.linspace**(mu - 4*sigma, mu + 4*sigma, 1000)","array de 1000 valeurs, utile pour une pdf"],
  ]},
  {id:"linalg-numpy", group:"numpy", title:"Algèbre linéaire — résoudre un système, empiler des tableaux", headers:["Nom","Syntaxe","Explication"], colClasses:["col-nom","col-syntaxe","col-explication"], rows:[
    ["Produit matriciel","a **@** b","équivalent générique de np.dot(a, b) / np.matmul(a, b) — opérateur Python, pas une fonction spécifique à un cours"],
    ["Inverser une matrice carrée","X_inv = np.linalg.**inv**(X)","X : array carré (n,n) — condition nécessaire (X non carrée ⇒ erreur)"],
    ["Résoudre Xθ=y directement (plus stable que inv())","theta = np.linalg.**solve**(X, y)","évite de calculer explicitement X⁻¹ — équivalent numériquement plus stable de np.linalg.inv(X) @ y ; lève LinAlgError si X n'est pas carrée (système sur/sous-déterminé, cf. Maths ▸ Résoudre un système linéaire)"],
    ["Rang d'une matrice","np.linalg.**matrix_rank**(X)","rang < nb de colonnes ⇒ colonnes colinéaires, X non inversible (cf. Maths ▸ OLS — solution mathématique, condition d'existence de β̂)"],
    ["Trace (somme des éléments diagonaux)","np.**diag**(X).sum()","-","raccourci de np.diag(X) (extrait la diagonale) + .sum()"],
    ["Déterminant","np.linalg.**det**(X)","X : matrice carrée","det(X)=0 ⇔ X n'est PAS inversible (mêmes matrices singulières que rang < n colonnes, cf. matrix_rank ci-dessus) — deux façons différentes de détecter la même chose"],
    ["Comparer deux tableaux flottants (tolère les erreurs d'arrondi)","np.**allclose**(a, b)","bool — préférer à une égalité stricte a==b, qui échoue souvent à cause de la précision flottante"],
    ["Empiler des colonnes côte à côte","X = np.**hstack**((x0, features))","x0, features : même nombre de lignes — ex: ajouter une colonne de 1 (intercept) à une matrice de features"],
    ["Empiler des lignes (ajouter des observations)","X_updated = np.**vstack**([X, nouvelle_ligne])","X, nouvelle_ligne : même nombre de colonnes — ex: ajouter une nouvelle observation à un dataset existant"],
  ]},
  {id:"images-arrays", group:"numpy", title:"Une image (ou un son) est un tableau NumPy", rows:[
    ["Charger un tableau sauvegardé (.npy)","array = **np.load**(\"fichier.npy\")","recharge un array NumPy tel quel (dtype/shape préservés) — plus rapide qu'un CSV pour de gros tableaux numériques"],
    ["Deviner ce qu'un array représente","array.ndim, array.shape, array.dtype","1D + valeurs ±grandes = son ; 2D 0-255 = image niveaux de gris ; 3D avec 3/4 canaux = image RGB/RGBA ; 4D = suite d'images (vidéo/animation)"],
    ["Afficher une image (tableau 2D ou 3D)","plt.**imshow**(array, cmap=\"gray\")\nplt.axis(\"off\")","cmap=\"gray\" obligatoire pour une image 2D en niveaux de gris (sinon fausses couleurs par défaut) ; ignoré si l'array a déjà 3 canaux (RGB)"],
    ["Sauvegarder un array comme image","plt.**imsave**(\"out.png\", array, cmap=\"gray\")","PNG (pas JPEG) si l'array a un canal alpha (RGBA, transparence) — JPEG ne supporte pas la transparence"],
    ["Isoler une portion / sous-échantillonner","crop = array[:500, :500]\nsous_ech = array[::3, ::3]","slicing 2D classique — un pas (`::3`) sous-échantillonne 1 pixel sur 3 dans chaque dimension, réduit la résolution sans redimensionner"],
  ]},
  {id:"lecture", group:"pandas", subgroup:"Import / export données", title:"Lecture / écriture de données", rows:[
    ["Lire un CSV","**pd.read_csv**(\"fichier.csv\", sep=\",\", encoding=\"utf-8\")","DataFrame"],
    ["Désactiver la détection auto des valeurs manquantes","pd.read_csv(\"fichier.csv\", **na_filter=False**)","DataFrame — aucune valeur convertie en NaN, même \"NA\", \"null\", \"\" etc. — piège classique : le code pays ISO \"NA\" (Namibie) serait sinon lu comme valeur manquante par défaut"],
    ["Lire un Excel","**pd.read_excel**(\"fichier.xlsx\", sheet_name=\"Feuille1\")","DataFrame"],
    ["Lire un Parquet","**pd.read_parquet**(\"fichier.parquet\")","DataFrame"],
    ["Lire un JSON","**pd.read_json**(\"fichier.json\")","DataFrame"],
    ["Écrire un CSV","df.**to_csv**(\"sortie.csv\", index=False)","fichier créé, sans colonne d'index"],
    ["Écrire un Excel","df.**to_excel**(\"sortie.xlsx\", index=False)","fichier créé"],
  ]},
  {id:"construction-pandas", group:"pandas", subgroup:"Import / export données", title:"Construire une Series / un DataFrame from scratch", rows:[
    ["Créer une Series à partir d'une liste","pd.**Series**([10, 20, 30], index=[\"a\",\"b\",\"c\"])","Series indexée par a/b/c"],
    ["Créer une Series à partir d'un dict","pd.**Series**({\"a\": 10, \"b\": 20})","Series — les clés du dict deviennent l'index"],
    ["Créer un DataFrame à partir de listes de listes","pd.**DataFrame**([[1,2],[3,4]], index=[\"r1\",\"r2\"], columns=[\"c1\",\"c2\"])","DataFrame 2x2"],
    ["Créer un DataFrame à partir d'un dict de listes/Series","pd.**DataFrame**({\"col1\": [1,2,3], \"col2\": [4,5,6]})","DataFrame — chaque clé du dict devient une colonne, alignée sur le même index : un DataFrame est fondamentalement un dict de Series partageant le même index"],
  ]},
  {id:"sourcing-api", group:"pandas", subgroup:"Import / export données", title:"Récupérer des données via une API", rows:[
    ["Importer requests","import **requests**","-"],
    ["Requête GET vers une API REST","response = requests.**get**(url, params={\"key\": \"value\"})","objet Response — params est encodé automatiquement en query string dans l'URL"],
    ["Vérifier le succès de la requête","response.**status_code**","code HTTP : 2xx succès, 4xx erreur côté client (ex: 404 not found), 5xx erreur côté serveur"],
    ["Récupérer le contenu JSON de la réponse","data = response.**json**()","dict ou liste Python — équivalent de json.loads(response.text)"],
    ["Paginer une API pour tout récupérer",[
      {code:"results = []\npage = 1\nwhile True:\n    page_data = requests.**get**(url, params={\"page\": page}).json()\n    if not page_data:\n        break\n    results += page_data\n    page += 1\ndf = pd.DataFrame(results)"}
    ],"boucle jusqu'à recevoir une page vide — beaucoup d'APIs limitent le nombre de résultats par requête et exposent un paramètre page/offset pour tout récupérer"],
  ]},
  {id:"sourcing-scraping", group:"pandas", subgroup:"Import / export données", title:"Scraper une page HTML & choisir un format", rows:[
    ["Parser le HTML d'une page","from bs4 import BeautifulSoup\nsoup = **BeautifulSoup**(response.text, \"html.parser\")","objet navigable dans l'arbre HTML"],
    ["Extraire des balises","soup.**find_all**(\"a\", class_=\"result-link\")","liste de balises correspondant au sélecteur (tag + attributs)"],
    ["Format de sauvegarde pour un gros dataset","df.**to_parquet**(\"fichier.parquet\")  # vs. df.to_csv(...)","Parquet : binaire, compressé, types préservés (int/float/date), bien plus rapide à lire/écrire qu'un CSV — à préférer dès que le dataset devient volumineux ou est réutilisé souvent"],
  ]},
  {id:"exploration", group:"pandas", subgroup:"Exploration", title:"Exploration initiale", rows:[
    ["Voir les premières lignes","df.**head**()","5 premières lignes"],
    ["Voir les dernières lignes","df.**tail**(3)","3 dernières lignes"],
    ["Lignes prises au hasard (souvent préféré à head/tail)","df.**sample**(5)","5 lignes aléatoires — head/tail montrent toujours les mêmes lignes (biais si le fichier est trié/groupé), sample donne une vue plus représentative"],
    ["Sous-échantillon reproductible, sans remise (ex: exploration rapide sur un gros dataset)","df.**sample**(10000, random_state=42)","DataFrame de 10000 lignes distinctes tirées au hasard — random_state fixe le tirage pour obtenir le même résultat à chaque exécution"],
    ["Tirage avec remise sur une colonne","df[\"col\"].**sample**(10, replace=True)","Series de 10 valeurs tirées au hasard dans la colonne, doublons possibles"],
    ["Dimensions du DataFrame","df.**shape**","tuple (lignes, colonnes)"],
    ["Résumé des types et valeurs non nulles","df.**info**()","affichage console — liste chaque colonne avec son type et son nombre de valeurs non nulles"],
    ["Stats descriptives","df.**describe**()","DataFrame avec, pour chaque colonne numérique : count, mean, std, min, 25%, 50%, 75%, max"],
    ["Liste des colonnes","df.**columns**","Index des noms de colonnes"],
    ["Types des colonnes","df.**dtypes**","Series type par colonne"],
    ["Nombre de NaN par colonne","df.**isna**().sum()","Series (nb de NaN)"],
    ["Nombre de NaN par colonne (alias)","df.**isnull**().sum()","identique à isna().sum(), juste un autre nom"],
    ["Nombre de valeurs uniques par colonne","df.**nunique**()","compte le nombre de valeurs uniques de toutes les colonnes"],
  ]},
  {id:"exploration-colonnes", group:"pandas", subgroup:"Exploration", title:"Exploration des colonnes", rows:[
    ["Nombre de valeurs uniques","df[\"col\"].**nunique**()","compte le nombre de valeurs uniques dans la colonne"],
    ["Valeurs uniques d'une colonne","df[\"col\"].**unique**()","array des valeurs distinctes"],
    ["Fréquence des valeurs","df[\"col\"].**value_counts**()","Series triée par fréquence"],
    ["Moyenne d'une colonne","df[\"col\"].**mean**()","scalaire"],
    ["Médiane d'une colonne","df[\"col\"].**median**()","scalaire — contrairement à la moyenne, robuste aux valeurs extrêmes (outliers)"],
    ["Écart-type d'une colonne","df[\"col\"].**std**()","scalaire"],
    ["Asymétrie (skewness) d'une colonne","df[\"col\"].**skew**()","scalaire — 0 si symétrique, >0 asymétrie à droite, <0 à gauche"],
    ["Aplatissement (kurtosis) d'une colonne","df[\"col\"].**kurtosis**()","scalaire — équivalent à scipy.stats.kurtosis(), directement en méthode pandas"],
  ]},
  {id:"index-comprendre", group:"pandas", subgroup:"Index", title:"Comprendre l'index", rows:[
    ["Labels d'index (lignes) d'un DataFrame/Series","df.**index**","par défaut RangeIndex(start=0, stop=n, step=1) — un simple compteur, pas forcément lié aux données"],
    ["Labels des colonnes","df.**columns**","aussi un objet Index (même type d'objet que df.index, mais sur l'axe des colonnes)"],
    ["Vérifier que l'index n'a pas de doublons","df.**index.is_unique**","bool — l'index n'est PAS unique par nature (contrairement à une clé primaire SQL) : rien n'empêche deux lignes de porter le même label sauf si on le vérifie soi-même"],
    ["Nombre de lignes portant un label donné","(df.index == 0).**sum**()","int — si > 1, plusieurs lignes partagent ce label (df.loc[0] renverrait alors un DataFrame, pas une Series)"],
  ]},
  {id:"index-set-reset", group:"pandas", subgroup:"Index", title:"Définir / réinitialiser l'index", rows:[
    ["Fixer une colonne comme index, en place","df.**set_index**(\"col\", inplace=True)","l'index de df devient la colonne col (col n'est plus une colonne normale) — inverse de reset_index()"],
    ["Fixer une colonne comme index en refusant les doublons","df.**set_index**(\"col\", verify_integrity=True)","lève une erreur si la colonne choisie contient des valeurs dupliquées, au lieu de laisser passer un index non unique"],
    ["Réinitialiser l'index (ancien index → colonne)","df.**reset_index**()","DataFrame avec un nouveau RangeIndex propre ; l'ancien index redevient une colonne normale"],
    ["Réinitialiser l'index en jetant l'ancien","df.**reset_index**(drop=True)","DataFrame avec un nouveau RangeIndex propre ; l'ancien index est perdu (pas remis en colonne) — utile après un filtrage ou un concat pour repartir sur un index 0..n-1 propre"],
  ]},
  {id:"selection", group:"pandas", subgroup:"Sélection, filtrage & tri", title:"Sélection", rows:[
    ["Sélectionner une colonne","df**[\"col\"]**","Series"],
    ["Sélectionner une colonne — notation par point (raccourci)","df.**col**","Series — équivalent à df[\"col\"], mais seulement si \"col\" est un identifiant Python valide (pas d'espace, ne commence pas par un chiffre) et ne rentre pas en conflit avec une méthode/attribut existant de DataFrame (ex: df.count, df.shape) ; ne fonctionne pas pour créer une nouvelle colonne par assignation (df.nouvelle_col = ... échoue, il faut df[\"nouvelle_col\"] = ...)"],
    ["Sélectionner plusieurs colonnes","df**[[\"col1\", \"col2\"]]**","DataFrame"],
    ["Ligne par position","df.**iloc**[0]","1ère ligne"],
    ["Plage de lignes par position","df.**iloc**[0:5]","5 premières lignes"],
    ["Ligne par label d'index","df.**loc**[3]","ligne d'index 3"],
    ["Valeur précise par label","df.**loc**[3, \"col\"]","valeur unique"],
    ["Plage de lignes par label (bornes incluses des deux côtés)","df.**loc**[3:7]","lignes dont le label d'index va de 3 à 7 INCLUS — contrairement à .iloc[3:7] (borne de fin exclue), .loc inclut la borne de fin"],
  ]},
  {id:"tri", group:"pandas", subgroup:"Sélection, filtrage & tri", title:"Tri", rows:[
    ["Trier par une colonne","df.**sort_values**(\"col\", ascending=False)","DataFrame trié décroissant"],
    ["Trier par plusieurs colonnes","df.**sort_values**([\"col1\",\"col2\"], ascending=[True, False])","DataFrame trié selon priorités"],
    ["Piège : le tri ne réordonne pas l'index","df.**sort_values**(\"col\")","les lignes changent de position mais gardent leur label d'origine — l'index n'est plus dans l'ordre 0,1,2... ; ajouter .reset_index(drop=True) si on veut un index propre après tri"],
  ]},
  {id:"filtrage", group:"pandas", subgroup:"Sélection, filtrage & tri", title:"Filtrage", rows:[
    ["Opérateurs autorisés dans une condition",[
      {table:"==   égal à\n!=   différent de\n>    strictement supérieur\n>=   supérieur ou égal\n<    strictement inférieur\n<=   inférieur ou égal\n&    ET logique\n|    OU logique\n~    négation (NON)"}
    ],"chaque condition entre parenthèses, avec & | ~ — jamais and / or / not (ne fonctionnent pas sur une Series)"],
    ["Filtrer sur une condition","df**[df[\"col\"] > 10]**","lignes où col > 10"],
    ["Filtrer avec ET","df[(df[\"col1\"] > 10) **&** (df[\"col2\"] == \"A\")]","lignes vérifiant les deux conditions"],
    ["Filtrer avec OU","df[(df[\"col1\"] > 10) **|** (df[\"col2\"] == \"A\")]","lignes vérifiant au moins une condition"],
    ["Filtrer sur une liste de valeurs","df[df[\"col\"].**isin**([\"A\", \"B\"])]","lignes où col vaut A ou B"],
    ["Exclure une liste de valeurs","df[**~**df[\"col\"].isin([\"A\", \"B\"])]","lignes où col n'est ni A ni B"],
    ["Filtrer un Index (mêmes opérateurs qu'une Series)","serie.index[~serie.index.**isin**(liste_complete)]","clés de serie.index absentes de liste_complete — ex: repérer les orphelins avant un reindex()"],
    ["Filtrer sur un texte contenu","df[df[\"col\"].str.**contains**(\"texte\", na=False)]","lignes contenant \"texte\""],
    ["Filtrer en syntaxe lisible","df.**query**(\"col1 > 10 and col2 == 'A'\")","équivalent au filtrage par masque"],
    ["Piège : après un filtrage, l'index n'est plus contigu","df**[df[\"col\"] > 0]**","les lignes gardent leur label d'origine — l'index devient un sous-ensemble discontinu (ex: 0,1,3,4,6...), ce n'est plus un RangeIndex propre même s'il ressemble à des entiers croissants ; .reset_index(drop=True) pour repartir sur un index propre"],
  ]},
  {id:"filtrage-pieges", group:"pandas", subgroup:"Sélection, filtrage & tri", title:"Filtrage — Piège", rows:[
    ["Cas sûr : lecture seule après filtrage, aucune modification prévue","sous_df = df[df[\"col\"] > 10]\nprint(sous_df.**describe**())","aucun risque : tu ne fais que lire, .copy() est inutile"],
    ["Cas sûr : modifier l'original en une seule étape","df.**loc**[df[\"col\"] > 10, \"autre_col\"] = 0","modification directe et sûre de df, aucune ambiguïté"],
    ["Piège : sélection puis modification en deux étapes, sans copie","sous_df = df[df[\"col\"] > 10].**copy**()\nsous_df[\"nouvelle_col\"] = 1","avec .copy() : sous_df devient un objet à part entière, le modifier ne touche jamais df\nsans .copy() : ambigu -> SettingWithCopyWarning, pandas ne sait pas si tu voulais modifier df ou créer un objet séparé","sous_df = df[df[\"col\"] > 10]\nsous_df[\"nouvelle_col\"] = 1"],
  ]},
  {id:"pandas-clip", group:"pandas", subgroup:"Sélection, filtrage & tri", title:"Series.clip — borner une colonne", rows:[
    ["Ramener toutes les valeurs négatives à 0","df[\"col\"].**clip**(lower=0)","Series — plus court que .apply(lambda x: x if x>0 else 0) ou np.where(df[\"col\"]>0, df[\"col\"], 0)"],
    ["Borner entre deux valeurs","df[\"col\"].**clip**(lower=0, upper=100)","Series bornée des deux côtés"],
  ]},
  {id:"nettoyage", group:"pandas", subgroup:"Transformation", title:"Nettoyage des données", rows:[
    ["Supprimer les lignes avec NaN","df.**dropna**()","DataFrame sans lignes incomplètes"],
    ["Supprimer si NaN dans une colonne précise","df.**dropna**(subset=[\"col\"])","DataFrame filtré"],
    ["Remplacer les NaN par une valeur","df.**fillna**(0)","DataFrame sans NaN"],
    ["Remplacer les NaN par la moyenne","df[\"col\"].**fillna**(df[\"col\"].mean())","Series sans NaN"],
    ["Supprimer les doublons","df.**drop_duplicates**()","DataFrame sans lignes dupliquées"],
    ["Supprimer les doublons sur une colonne","df.**drop_duplicates**(subset=[\"col\"])","DataFrame filtré"],
    ["Renommer des colonnes","df.**rename**(columns={\"ancien\": \"nouveau\"})","DataFrame avec nouveaux noms"],
    ["Supprimer des colonnes","df.**drop**(columns=[\"col1\", \"col2\"])","DataFrame sans ces colonnes"],
    ["Convertir un type de colonne","df[\"col\"] = df[\"col\"].**astype**(int)","colonne convertie en int"],
    ["Convertir un nombre écrit en texte, sans planter sur ce qui ne parse pas","df[\"col\"] = **pd.to_numeric**(df[\"col\"], errors=\"coerce\")","colonne numérique — les valeurs non convertibles deviennent NaN (traitées ensuite comme des valeurs manquantes classiques) plutôt que de lever une exception comme .astype()"],
    ["Retirer les espaces parasites en début/fin de chaîne","df[\"col\"] = df[\"col\"].str.**strip**()","Series nettoyée — piège classique : un isin()/comparaison qui ne matche rien peut être causé par des espaces invisibles, repérables avec .unique()"],
    ["Piège : filtrer les valeurs manquantes (NaN). NaN n'est jamais égal à lui-même en Python/numpy (même deux NaN comparés entre eux donnent False), donc comparer avec == ne peut jamais fonctionner pour les repérer — et pire, ça ne renvoie aucune erreur ni aucun warning, juste un résultat vide.","df[df[\"col\"].**isna**()]","lignes où col est NaN","df[df[\"col\"] == np.nan]"],
  ]},
  {id:"index-add-remove-rows", group:"pandas", subgroup:"Transformation", title:"Ajouter / enlever des lignes", rows:[
    ["Ajouter une ligne","pd.**concat**([df, pd.DataFrame([{\"col1\": val1, \"col2\": val2}])], ignore_index=True)","concat garde l'index d'origine de chaque morceau — la nouvelle ligne a par défaut un index qui recommence à 0, donc sans ignore_index=True elle peut dupliquer un label déjà présent dans df ; ignore_index=True régénère un RangeIndex propre pour tout le résultat"],
    ["Ajouter une ligne à un label précis","df.**loc**[len(df)] = [val1, val2]","ajoute une ligne au label len(df) — ne fonctionne proprement que si l'index est déjà un RangeIndex 0..n-1 sans trou, sinon le label choisi peut déjà exister"],
    ["Supprimer une ligne cible le label, pas la position","df.**drop**(1)","supprime la ligne dont le label d'index vaut 1 — si l'index n'est plus un RangeIndex propre (après un filtrage précédent), ça peut ne pas être la 2e ligne du DataFrame"],
    ["Supprimer une ligne par position (peu importe son label)","df.**drop**(df.index[1])","supprime la 2e ligne du DataFrame, quel que soit son label — équivalent positionnel de drop(1)"],
    ["Sélection par label vs par position","df.**loc**[1] / df.**iloc**[1]","loc cherche le label 1 dans l'index (peut échouer ou renvoyer plusieurs lignes si doublon) ; iloc prend toujours la 2e ligne, par position, indépendamment des labels"],
  ]},
  {id:"transformation", group:"pandas", subgroup:"Transformation", title:"Transformation de colonnes", rows:[
    ["Créer une colonne à partir de deux autres","df[\"**nouvelle**\"] = df[\"col1\"] + df[\"col2\"]","nouvelle colonne calculée"],
    ["Appliquer une fonction à une colonne","df[\"nouvelle\"] = df[\"col\"].**apply**(lambda x: x * 2)","colonne transformée"],
    ["Transformer une colonne simplement (opération vectorisée)","**df[\"col\"] * 2**","plus rapide que .apply(lambda x: x * 2) pour les opérations simples"],
    ["Remplacer des valeurs (mapping)","df[\"col\"] = df[\"col\"].**map**({\"A\": 1, \"B\": 2})","valeurs remplacées"],
    ["Remplacer une valeur exacte par une autre","df[\"col\"] = df[\"col\"].**replace**(\"OLD\", \"NEW\")","toutes les valeurs égales à OLD deviennent NEW"],
    ["Remplacer un sous-texte (substring) dans chaque valeur","df[\"col\"] = df[\"col\"].str.**replace**(\"OLD\", \"NEW\", regex=False)","chaque occurrence du texte OLD remplacée par NEW à l'intérieur des valeurs"],
    ["Anti-pattern à reconnaître (et éviter) : boucler ligne par ligne","for index, row in df.**iterrows**():\n    ...","très lent sur un gros DataFrame — préférer une opération vectorisée (df[\"col\"] * 2), .apply(), ou du boolean indexing ; utile seulement pour reconnaître ce pattern dans du code existant, pas pour en écrire"],
    ["Réordonner les colonnes","df = df**[[\"col3\", \"col1\", \"col2\"]]**","DataFrame avec les colonnes dans l'ordre de la liste — pratique pour remonter des colonnes clés (ex: id) en première position"],
    ["Arrondir toutes les valeurs numériques","df.**round**(3)","DataFrame avec les valeurs arrondies à 3 décimales"],
  ]},
  {id:"transformation-conditionnelle", group:"pandas", subgroup:"Transformation", title:"Transformation conditionnelle", rows:[
    ["Créer une colonne indicatrice 0/1 à partir d'une condition","df[\"dim_is_five_star\"] = (df[\"review_score\"] == 5).**astype**(int)","colonne de 0/1 — le masque booléen (True/False) se convertit directement en (1/0) avec .astype(int), plus direct qu'un .apply(lambda x: 1 if x==5 else 0)"],
    ["Colonne conditionnelle simple","df[\"cat\"] = **np.where**(df[\"col\"] > 10, \"haut\", \"bas\")","colonne \"haut\"/\"bas\""],
    ["Calculer une valeur seulement si la condition est vraie, sinon garder la valeur d'origine","df[\"col\"] = **np.where**(df[\"col\"] > 10, df[\"col\"] + 1, df[\"col\"])","+1 si col > 10, valeur inchangée sinon"],
    ["Même chose, en ne touchant que les lignes concernées","df.**loc**[df[\"col\"] > 10, \"col\"] += 1","+1 uniquement sur les lignes où col > 10, le reste n'est pas modifié"],
    ["Colonne à conditions multiples","df[\"cat\"] = **np.select**(conditions, choix, default=\"grand\")","colonne selon plusieurs seuils"],
  ]},
  {id:"reshaping", group:"pandas", subgroup:"Transformation", title:"Reshaping (pivot, melt)", rows:[
    ["Tableau croisé dynamique (format long -> large)",[
      {label:"df :"},
      {table:"  magasin mois  ventes\n0   Paris  Jan     100\n1   Paris  Fév     120\n2    Lyon  Jan      80\n3    Lyon  Fév      90"},
      {code:"df.**pivot_table**(index=\"magasin\", columns=\"mois\", values=\"ventes\", aggfunc=\"mean\")"}
    ],[
      {label:"DataFrame :"},
      {table:"mois      Fév  Jan\nmagasin\nLyon       90   80\nParis     120  100"}
    ]],
    ["Format large -> long (inverse du pivot)",[
      {label:"df :"},
      {table:"  magasin  Jan  Fév\n0   Paris  100  120\n1    Lyon   80   90"},
      {code:"df.**melt**(id_vars=[\"magasin\"], value_vars=[\"Jan\",\"Fév\"],\n        var_name=\"mois\", value_name=\"ventes\")"}
    ],[
      {label:"DataFrame :"},
      {table:"  magasin mois  ventes\n0   Paris  Jan     100\n1    Lyon  Jan      80\n2   Paris  Fév     120\n3    Lyon  Fév      90"}
    ]],
  ]},
  {id:"dates", group:"pandas", subgroup:"Dates", title:"Dates et temps", rows:[
    ["Convertir en type date","df[\"date\"] = **pd.to_datetime**(df[\"date\"], format=\"%Y-%m-%d\")","colonne au format datetime"],
    ["Extraire l'année","df[\"date\"].dt.**year**","Series d'entiers"],
    ["Extraire le mois","df[\"date\"].dt.**month**","Series d'entiers"],
    ["Extraire le jour de la semaine","df[\"date\"].dt.**day_name**()","Series de noms de jours"],
    ["Agréger par mois","df.set_index(\"date\").**resample**(\"ME\").sum()","DataFrame agrégé mensuellement (fin de mois)"],
  ]},
  {id:"pandas-dates-diff", group:"pandas", subgroup:"Dates", title:"Différence entre deux dates : précision", rows:[
    ["Piège : .dt.days tronque à l'entier (perte de précision)","(df[\"date_livree\"] - df[\"date_achat\"]).**dt.days**","int — arrondit vers le bas, 8.9 jours devient 8 : à éviter si la valeur exacte compte (ex: variable d'entrée d'une régression)"],
    ["Différence de dates en jours, avec décimales","(df[\"date_livree\"] - df[\"date_achat\"]) / **np.timedelta64**(24, \"h\")","float — ex: 8.9, garde la précision"],
    ["Équivalent avec pd.Timedelta","(df[\"date_livree\"] - df[\"date_achat\"]) / **pd.Timedelta**(days=1)","float, même résultat que np.timedelta64(24,\"h\"), lisibilité différente"],
  ]},
  {id:"groupby", group:"pandas", subgroup:"Agrégation", title:"Groupby / agrégation", rows:[
    ["Moyenne par groupe","df.groupby(\"col\").**mean**()","DataFrame indexé par groupe"],
    ["Somme d'une colonne par groupe","df.groupby(\"col\")[\"valeur\"].**sum**()","Series indexée par groupe"],
    ["Agrégations multiples nommées","df.groupby(\"col\").**agg**(total=(\"valeur\",\"sum\"), moyenne=(\"valeur\",\"mean\"))","DataFrame avec colonnes nommées"],
    ["Agrégations multiples, forme dict (équivalente, plus rapide à écrire)","df.groupby(\"col\").**agg**({\"valeur\": \"sum\", \"autre\": \"mean\"})","DataFrame — une colonne par clé du dict, nommée comme la colonne d'origine (pas de renommage possible contrairement à la forme nommée ci-dessus)"],
    ["Nombre de lignes par groupe(s)","df.groupby([\"col1\", \"col2\"]).**size**()","Series du nb de lignes (NaN inclus) — pour compter, c'est size(), pas count()"],
    ["Nombre de valeurs non-nulles par groupe, colonne par colonne","df.groupby(\"col\").**count**()","DataFrame du nb de valeurs non-NaN pour chaque colonne (peut différer de size() s'il y a des NaN)"],
  ]},
  {id:"pandas-groupby-index", group:"pandas", subgroup:"Agrégation", title:"Groupby : gérer l'index (to_frame, reset_index, reindex)", rows:[
    ["Nombre de lignes par groupe, en colonne directement nommée","df.groupby(\"order_id\").**size**().**to_frame**(\"number_of_items\").reset_index()","DataFrame à 2 colonnes [\"order_id\", \"number_of_items\"] : .size() (Series) -> .to_frame(nom) (nomme la colonne) -> .reset_index() (l'index de groupe redevient une colonne)"],
    ["Repasser en DataFrame \"plat\" après groupby","df.groupby(\"col\").mean().**reset_index**()","index de groupe redevenu colonne — même logique utilisée dans to_frame().reset_index() ci-dessus"],
    ["Combler les groupes absents avec une valeur par défaut","df.groupby(\"col\").size().**reindex**(liste_complete, fill_value=0)","recale la Series sur toutes les clés de liste_complete — les groupes manquants du groupby prennent 0 au lieu d'être absents"],
    ["Piège : reindex() droppe silencieusement ce qui n'est pas dans la nouvelle liste","serie.**reindex**(liste_complete)","toute clé présente dans serie mais absente de liste_complete disparaît sans avertissement — utile si liste_complete est bien exhaustive, dangereux sinon (incohérence de données, ex: order_id de reviews absent de orders)"],
  ]},
  {id:"fusion", group:"pandas", subgroup:"Jointures", title:"Fusion / jointures", rows:[
    ["Jointure sur une clé commune","**pd.merge**(df1, df2, on=\"cle\", how=\"inner\")","DataFrame fusionné (lignes communes)"],
    ["Jointure avec clés différentes","**pd.merge**(df1, df2, left_on=\"cle1\", right_on=\"cle2\", how=\"left\")","toutes les lignes de df1 + correspondances"],
    ["Empiler des lignes","**pd.concat**([df1, df2], axis=0)","DataFrame concaténé verticalement"],
    ["Ajouter des colonnes côte à côte","**pd.concat**([df1, df2], axis=1)","DataFrame concaténé horizontalement"],
    ["Piège : concaténer sans régénérer l'index → doublons","pd.**concat**([df1, df2])","chaque morceau garde son propre index d'origine : si df1 et df2 ont tous les deux un label 0, le résultat a deux lignes d'index 0 — utiliser ignore_index=True ou reset_index(drop=True) après pour éviter ça"],
    ["Forcer une erreur plutôt qu'un doublon silencieux","pd.**concat**([df1, df2], verify_integrity=True)","lève une erreur si le résultat contient des labels d'index dupliqués, au lieu de les laisser passer"],
  ]},
  {id:"pandas-anti-join", group:"pandas", subgroup:"Jointures", title:"Anti-jointure — repérer les lignes sans correspondance", rows:[
    ["Compter les order_id sans review, via une jointure outer","merged = orders[[\"order_id\"]].**merge**(reviews[[\"order_id\",\"review_id\"]], on=\"order_id\", how=\"outer\")\nmerged.**isna**().sum()[\"review_id\"]","nb de order_id qui n'ont trouvé aucune review — how=\"outer\" garde toutes les lignes des deux côtés ; un NaN dans review_id signifie \"pas de review pour cette commande\" (à comparer à l'approche isin()+~ de la section Filtrage, plus directe si on n'a pas besoin des colonnes de droite)"],
    ["Indicateur explicite de la provenance de chaque ligne après un merge","pd.merge(orders, reviews, on=\"order_id\", how=\"outer\", **indicator=True**)","ajoute une colonne _merge (\"left_only\" / \"right_only\" / \"both\") — utile pour diagnostiquer précisément quelles lignes ne matchent pas de chaque côté"],
  ]},
  {id:"scipy-stats", group:"stats", title:"Scipy.stats — lois continues & statistiques", headers:["Nom","Formule","Params","Explication"], colClasses:["col-nom","col-formule","col-params","col-explication"], rows:[
    ["Importer les fonctions utiles","from scipy.stats import **norm, skew, kurtosis**","-","-"],
    ["Asymétrie d'une distribution (skewness)","**skew**(array)","-","0 si symétrique, >0 asymétrie à droite, <0 à gauche"],
    ["Aplatissement d'une distribution (kurtosis)","**kurtosis**(array)","-","proche de 0 pour une gaussienne"],
    ["Créer une loi Normale N(µ,σ)","**norm**(mu, sigma)","$\\mu$ : moyenne\n$\\sigma$ : écart-type","objet distribution, réutilisable avec .pdf / .cdf"],
    ["Densité de probabilité (pdf) en un point x","norm(mu, sigma).**pdf**(x)","$x$ : point où évaluer la densité","float ou array — hauteur de la courbe"],
    ["Fonction de répartition (cdf)","$$F(x) = P(X \\le x)$$\nnorm(mu, sigma).**cdf**(x)","$x$ : valeur seuil\n$X \\sim \\mathcal{N}(\\mu,\\sigma)$","-"],
    ["Probabilité que X dépasse une valeur","$$P(X>v) = 1 - F(v)$$\n1 - norm(mu, sigma).**cdf**(valeur)","$v$ : valeur seuil\n$X \\sim \\mathcal{N}(\\mu,\\sigma)$","-"],
    ["cdf de la loi normale centrée réduite N(0,1), à partir d'un z-score","$$\\Phi(z) = P(Z \\le z)$$\nnorm.**cdf**(z)","$z$ : score-z\n$Z \\sim \\mathcal{N}(0,1)$","-"],
    ["Loi Binomiale(n,p) — raccourci scipy","from scipy.stats import binom\nbinom(n, p).**pmf**(k)","$n$ : nombre d'essais\n$p$ : probabilité de succès\n$k$ : nombre de succès recherché","équivalent scipy de la formule manuelle math.comb(n,k) * p**k * (1-p)**(n-k) (cf. [Lois de probabilité discrètes](#lois-discretes), page Maths) — même logique que norm(mu, sigma) ci-dessus, mais pour une loi discrète"],
  ]},
  {id:"scipy-fft", group:"stats", title:"Scipy — FFT (analyse fréquentielle d'un signal)", headers:["Nom","Formule / Syntaxe","Params","Explication"], colClasses:["col-nom","col-formule","col-params","col-explication"], rows:[
    ["Générer une onde sinusoïdale (signal de test)","t = np.**linspace**(0, D, int(R*D))\ny = np.**sin**(2*np.pi*f*t)","R : taux d'échantillonnage (Hz)\nD : durée (s)\nf : fréquence (Hz)","une note pure — superposer plusieurs y(t) de fréquences différentes = un accord"],
    ["Transformée de Fourier rapide","from scipy.fft import **fft, fftfreq**\nspectre = fft(y)\nfreqs = fftfreq(len(y), 1/R)","R : même taux d'échantillonnage que pour générer y","spectre : amplitude complexe par fréquence — fftfreq donne l'axe des fréquences correspondant, à ne garder que sur sa moitié positive (spectre symétrique)"],
    ["Écouter un signal (notebook Jupyter)","from IPython.display import **Audio**\nAudio(y, rate=R)","rate : même taux d'échantillonnage R","lit le tableau numpy comme un fichier audio — pratique pour valider un signal généré/filtré sans l'exporter"],
    ["Charger un fichier .wav","from scipy.io import wavfile\nR, y = wavfile.**read**(\"son.wav\")","-","renvoie le taux d'échantillonnage R et le signal y (array) — même pipeline FFT ensuite que pour un signal généré"],
  ]},
  {id:"scipy-optimize", group:"stats", subgroup:"Optimisation", title:"Scipy — Optimisation (minimize, curve_fit, interpolate)", headers:["Nom","Formule / Syntaxe","Params","Explication"], colClasses:["col-nom","col-formule","col-params","col-explication"], rows:[
    ["Minimiser une fonction depuis un point de départ (minimum LOCAL)","from scipy import optimize\nres = optimize.**minimize**(f, x0)\nres.x, res.fun","f : fonction à minimiser (accepte un scalaire ou un array)\nx0 : point de départ","ne trouve qu'un minimum LOCAL — reste bloqué dedans si f n'est pas convexe, sauf si x0 est déjà proche du minimum global (cf. minimum local, page Modélisation ▸ Descente de gradient)"],
    ["Minimiser sous contraintes et bornes","optimize.minimize(h, X0, **method**=\"SLSQP\", **bounds**=((1,5),)*4, **constraints**=[{\"type\":\"eq\",\"fun\":c1}, {\"type\":\"ineq\",\"fun\":c2}])","bounds : une paire (min,max) par variable\nconstraints : liste de dicts — type \"eq\" (=0 requis) ou \"ineq\" (≥0 requis)","SLSQP est la méthode qui supporte contraintes ET bornes simultanément ; une contrainte d'égalité s'écrit comme une fonction qui doit valoir 0 à la solution, une inégalité comme une fonction qui doit rester ≥0"],
    ["Trouver le minimum GLOBAL (recherche multi-départs)","optimize.**shgo**(g, bounds)\n# ou, plus stochastique :\noptimize.**dual_annealing**(g, bounds)","bounds : une paire (min,max) par dimension","optimize.minimize seul ne garantit qu'un minimum local — shgo (déterministe, efficace en petite dimension) et dual_annealing (recuit simulé, stochastique) explorent l'espace plus largement pour approcher le minimum global"],
    ["Ajuster les paramètres d'une fonction à un nuage de points","def f(x, a, b, c):\n    return a*x**2 + b*x + c\n\npopt, pcov = optimize.**curve_fit**(f, x, y)","popt : paramètres optimaux trouvés (a,b,c)\npcov : matrice de covariance (incertitude sur popt), souvent ignorée","trouve directement les paramètres qui minimisent l'erreur quadratique entre f(x, *popt) et y — pas besoin d'implémenter soi-même la recherche (cf. optimize.minimize)"],
    ["Interpoler entre des points connus (remplir les blancs)","from scipy import interpolate\nf_interp = interpolate.**interp1d**(x, y, kind=\"cubic\")\ny_dense = f_interp(x_dense)","kind : \"linear\" (défaut, coudes aux points connus), \"quadratic\", \"cubic\" (plus lisse)","renvoie une fonction continue calée exactement sur les points connus (x,y) — appelable sur n'importe quelle grille dense (x_dense) pour combler les blancs entre eux"],
  ]},
  {id:"ols-fit", group:"regression", title:"Régression linéaire multivariée (OLS) avec statsmodels", headers:["Nom","Formule / Syntaxe","Params","Explication"], colClasses:["col-nom","col-formule","col-params","col-explication"], rows:[
    ["Modèle de régression multivariée (OLS)","$$y = \\beta_0 + \\beta_1 x_1 + ... + \\beta_p x_p + \\epsilon$$\nimport statsmodels.formula.api as smf\nmodel = smf.**ols**(\"y ~ x1 + x2\", data=df).**fit**()","$y$ : variable cible (dépendante)\n$x_i$ : features (indépendantes)\n$\\beta_i$ : coefficients estimés — $\\beta_0$ est l'Intercept","OLS = Ordinary Least Squares, minimise la MSE (Mean Squared Error) entre prédictions et valeurs réelles ; la formule texte façon R décrit le modèle, .fit() calcule les coefficients"],
    ["Ce que produit .fit()","model.params\nmodel.**fittedvalues**\nmodel.resid\nmodel.**summary**()","params : Series des $\\beta_i$ estimés, indexée par nom de feature (+ Intercept) — une valeur par feature, pas par ligne\nfittedvalues : $\\hat y$, une valeur par ligne de df\nresid : $y - \\hat y$, une valeur par ligne","fit() renvoie un objet Results complet, pas juste les coefficients — model.fittedvalues donne directement ŷ (les prédictions sur les données d'entraînement, même échelle que y) ; à comparer avec Logit ci-dessous, où fittedvalues n'est PAS sur l'échelle de y"],
    ["Ajouter une variable catégorielle à la formule","model = smf.ols(\"y ~ x1 + **C(**cat_feature**)**\", data=df).fit()","cat_feature : colonne catégorielle (str)","C() crée une variable indicatrice (dummy 0/1) par catégorie — une modalité sert de référence (dans l'Intercept), les autres apparaissent en coefficients séparés"],
    ["Standardiser les features avant régression (rendre les coefficients comparables)","df_std = (df[features] - df[features].**mean**()) / df[features].**std**()","features : liste de colonnes numériques","transforme chaque feature en z-score (moyenne 0, écart-type 1, cf. Score-z) — sans ça, les coefficients ne sont pas comparables entre eux à cause des différences d'échelle des variables d'origine"],
    ["Ajuster un modèle via l'API bas niveau (tableaux plutôt que formule texte)","import statsmodels.api as sm\nmodel = sm.**OLS**(y, X).fit()","y : array/Series cible\nX : DataFrame/array des features","alternative à smf.ols(formula=...) — utile quand X/y sont déjà construits séparément ; contrairement à smf.ols, il faut ajouter l'intercept soi-même avec sm.add_constant(X) (sinon le modèle passe par l'origine)"],
    ["Supprimer l'intercept dans une formule (un coefficient par catégorie, sans référence)","model = smf.ols(\"y ~ C(cat_feature) **- 1**\", data=df).fit()","-","sans le -1, une modalité sert de référence (absorbée dans l'Intercept) et les autres coefficients représentent des écarts à cette référence ; avec -1, chaque modalité reçoit directement son propre coefficient, égal à sa moyenne"],
  ]},
  {id:"ols-summary", group:"regression", title:"Lire le résumé d'une régression (.summary())", headers:["Nom","Formule / Syntaxe","Params","Explication"], colClasses:["col-nom","col-formule","col-params","col-explication"], rows:[
    ["Afficher le résumé complet du modèle",[
      {code:"model.**summary**()"},
      {label:"→ extrait, valeurs fictives :"},
      {table:"                            OLS Regression Results\n==============================================================================\nDep. Variable:                      y   R-squared:                       0.712\nModel:                            OLS   Adj. R-squared:                  0.706\nMethod:                 Least Squares   F-statistic:                     121.4\n                                        Prob (F-statistic):           3.45e-28\nNo. Observations:                 200   Log-Likelihood:                -410.22\nDf Residuals:                     197   AIC:                             826.4\nDf Model:                            2   BIC:                             836.3\n==============================================================================\n                 coef    std err          t      P>|t|      [0.025      0.975]\n------------------------------------------------------------------------------\nIntercept      4.5678      0.512      8.921      0.000       3.559       5.577\nx1             2.3456      0.187     12.545      0.000       1.977       2.714\nx2            -1.2345      0.298     -4.142      0.000      -1.822      -0.647\n==============================================================================\nOmnibus:                        1.203   Durbin-Watson:                   1.987\nProb(Omnibus):                  0.548   Jarque-Bera (JB):                1.102\nSkew:                           0.145   Prob(JB):                        0.577\nKurtosis:                       2.891   Cond. No.                         12.4\n=============================================================================="}
    ],"-","tableau texte avec tous les indicateurs détaillés ci-dessous — R-squared / Adj. R-squared / F-statistic / Prob (F-statistic) en haut à droite ; coef/std err/t/P>|t| par feature dans le tableau du milieu ; Omnibus/JB/Skew/Kurtosis/Durbin-Watson/Cond. No. en bas"],
    ["Accès direct aux valeurs clés (sans repasser par .summary())","model.**rsquared**\nmodel.**resid**","-","rsquared : float ; resid : Series des résidus, équivalent à model.predict(df) - df[\"y\"] mais déjà calculé par statsmodels — formule du R² et pièges d'interprétation détaillés en page Maths ▸ Régression"],
    ["Coefficient, erreur standard, t-value, p-value","**coef** / **std err** / **t** / **P>|t|**","coef : $\\beta_i$ estimé\nt = coef / std err","P>|t| < 0.05 (seuil usuel) ⇒ le coefficient est statistiquement significatif, on rejette $H_0: \\beta_i=0$ (cf. [Test d'hypothèse](#test-hypothese), page Maths) — formule du std err en page Maths ▸ Régression"],
    ["Intervalle de confiance du coefficient","**[0.025   0.975]**","-","IC 95% de $\\beta_i$ — s'il ne contient pas 0, le coefficient est significatif (cohérent avec sa p-value)"],
  ]},
  {id:"ols-diagnostics", group:"regression", title:"Prédictions, résidus, RMSE et coefficients significatifs", headers:["Nom","Formule / Syntaxe","Params","Explication"], colClasses:["col-nom","col-formule","col-params","col-explication"], rows:[
    ["Prédire à partir du modèle","y_pred = model.**predict**(df)","df : mêmes colonnes que celles utilisées pour fit()","applique les coefficients appris aux lignes de df"],
    ["Résidus","$$\\epsilon_i = \\hat y_i - y_i$$\n**residuals** = y_pred - df[\"y\"]","-","toujours vérifier que residuals.mean() ≈ 0 après un fit OLS — sinon, il y a une erreur dans le calcul ; diagnostics des résidus (hétéroscédasticité, autocorrélation) détaillés en page Maths ▸ Régression"],
    ["RMSE (Root Mean Squared Error)","$$RMSE = \\sqrt{\\dfrac{1}{n}\\sum_{i=1}^n (\\hat y_i - y_i)^2}$$\nrmse = pow((residuals ** 2).mean(), 0.5)","-","erreur moyenne de prédiction, dans la même unité que y — plus petit = mieux, mais dépend de l'échelle de y (contrairement au R², sans unité)"],
    ["Récupérer coefficients et p-values du modèle","model.**params**    # Series des coefficients (dont Intercept)\nmodel.**pvalues**   # Series des p-values associées","-","utile pour filtrer/trier les coefficients programmatiquement plutôt que lire .summary() à l'œil"],
    ["Ne garder que les coefficients statistiquement significatifs","p = model.pvalues.reset_index()\np.columns = [\"variable\",\"p_value\"]\nc = model.params.reset_index()\nc.columns = [\"variable\",\"coef\"]\np.**merge**(c, on=\"variable\").**query**(\"p_value<0.05\").sort_values(\"coef\")","-","pattern utilisé dans olist/utils.py::return_significative_coef — combine pvalues et params par jointure (cf. [Fusion/jointures](#fusion)), puis filtre p<0.05"],
    ["Graphiques de régression partielle (une feature à la fois, effet des autres neutralisé)","import statsmodels.api as sm\nfig = plt.figure(figsize=(10,10))\nsm.graphics.**plot_partregress_grid**(model, fig=fig)","-","visualise, pour chaque feature, sa relation avec y une fois l'effet des autres features retiré — équivalent graphique des partial correlation coefficients (coefficients de la régression multivariée)"],
    ["Visualiser les résidus pour détecter hétéroscédasticité/autocorrélation","sns.**scatterplot**(x=y_pred, y=residuals)","-","scatter résidus vs. valeurs prédites — motifs à repérer et leur signification détaillés en page Maths ▸ Régression"],
    ["QQ-plot — vérifier la normalité des résidus","import statsmodels.api as sm\nsm.**qqplot**(residuals, line=\"s\")","line=\"s\" : trace la droite de référence (standardisée)","compare les quantiles des résidus à ceux d'une loi normale — points alignés sur la droite = résidus ≈ normaux ; écart aux extrémités = queues plus lourdes/asymétrie (visible aussi sur l'histogramme des résidus, mais le QQ-plot est plus sensible aux écarts dans les queues)"],
  ]},
  {id:"vif", group:"regression", title:"Détecter la multicolinéarité (VIF)", headers:["Nom","Formule / Syntaxe","Params","Explication"], colClasses:["col-nom","col-formule","col-params","col-explication"], rows:[
    ["Variance Inflation Factor (VIF)","$$VIF(X_k) = \\dfrac{1}{1-R_k^2}$$\nfrom statsmodels.stats.outliers_influence import variance_inflation_factor as vif\n[**vif**(X_scaled.values, i) for i in range(X_scaled.shape[1])]","$R_k^2$ : R² obtenu en régressant $X_k$ sur toutes les autres features\nX_scaled : features standardisées (cf. Standardiser les features avant régression)","détecte la multicolinéarité multivariée (contrairement à la matrice de corrélation, limitée aux paires de variables) — règle empirique : VIF ≥ 10 signale une feature redondante avec les autres, candidate à retirer ou combiner ; s'applique aussi bien à une régression linéaire (OLS) que logistique"],
  ]},
  {id:"logit-fit", group:"regression", title:"Régression logistique avec statsmodels", headers:["Nom","Formule / Syntaxe","Params","Explication"], colClasses:["col-nom","col-formule","col-params","col-explication"], rows:[
    ["Modèle de régression logistique","$$\\hat p = \\dfrac{1}{1+e^{-(\\beta_0+\\beta_1 x_1+...+\\beta_p x_p)}}$$\nimport statsmodels.formula.api as smf\nmodel = smf.**logit**(\"y ~ x1 + x2\", data=df).fit()","$y$ : cible binaire (0/1)\n$\\hat p$ : probabilité prédite d'appartenir à la classe 1\nmême syntaxe formule que smf.ols : C() pour catégorielles, -1 pour retirer l'intercept","fit() maximise la log-vraisemblance par une méthode itérative, pas de solution fermée comme pour OLS (cf. page Maths ▸ Régression)"],
    ["Ce que produit .fit()","model.params\nmodel.**fittedvalues**\nmodel.**predict**(df)\nmodel.**summary**()","params : Series des $\\beta_i$ estimés, indexée par nom de feature (+ Intercept) — une valeur par feature, pas par ligne\nfittedvalues : le prédicteur linéaire Xβ (log-odds), une valeur par ligne — PAS une probabilité","piège : contrairement à OLS, model.fittedvalues n'est pas sur l'échelle de y ni une probabilité — c'est le log-odds brut, non borné ]-∞,+∞[ ; pour la probabilité (bornée 0-1), il faut model.predict(df) (cf. carte 'chaîne concrète' ci-dessous)"],
    ["Prédire une classe à partir d'une probabilité","y_pred = (model.**predict**(df) > 0.5).astype(int)","0.5 : seuil de décision par défaut","au-dessus du seuil → classe 1, en dessous → classe 0 ; le seuil peut être ajusté selon le coût respectif des faux positifs / faux négatifs (cf. Erreurs de Type I/II, page Maths)"],
    ["La chaîne concrète, une valeur PAR LIGNE : de Xβ à la probabilité","$$\\text{odds} = \\dfrac{p}{1-p} \\qquad \\text{logit}(p) = \\log(\\text{odds}) = X\\beta$$\nlogit_p = model.**fittedvalues**        # = X.β, log-odds brut du modèle\np       = model.**predict**(df)          # = sigmoïde(logit_p), la probabilité\nodds    = p / (1 - p)                # = repasse de p aux odds","logit_p, p, odds : trois Series de même longueur que df, un trio de valeurs par ligne — juste 3 façons d'exprimer la même chose","le modèle apprend une droite sur l'échelle logit (comme une régression linéaire classique) ; predict() applique la sigmoïde pour la ramener en probabilité (bornée 0-1, interprétable) ; repasser en odds est optionnel, seulement utile pour retrouver le lien direct avec les coefficients (ligne suivante)"],
    ["Interpréter un coefficient, une valeur PAR FEATURE : l'effet de βᵢ sur les odds","$$\\text{OR}_i = \\dfrac{\\text{odds}(x_i+1)}{\\text{odds}(x_i)} = e^{\\beta_i}$$\nimport numpy as np\nodds_ratios = np.**exp**(model.params)","$\\beta_i$ : coefficient de $x_i$ (model.params) — effet de +1 unité de $x_i$ sur logit(p), à additionner à Xβ ci-dessus","odds_ratios : Series indexée comme model.params (une valeur par feature + Intercept, pas par ligne) — OR_i (= exp(βᵢ)) est le rapport entre les odds à xᵢ+1 et les odds à xᵢ : le facteur multiplicatif sur les odds pour +1 unité de xᵢ, toutes les autres features fixées ; ex: odds_ratios[\"x1\"]=1.01 ⇒ +1% des odds ; <1 ⇒ effet négatif sur la probabilité de succès"],
  ]},
  {id:"logit-summary", group:"regression", title:"Lire le résumé d'une régression logistique (.summary())", headers:["Nom","Formule / Syntaxe","Params","Explication"], colClasses:["col-nom","col-formule","col-params","col-explication"], rows:[
    ["Afficher le résumé complet du modèle",[
      {code:"model.**summary**()"},
      {label:"→ extrait, valeurs fictives :"},
      {table:"                           Logit Regression Results\n==============================================================================\nDep. Variable:                      y   No. Observations:                  200\nModel:                          Logit   Df Residuals:                      197\nMethod:                           MLE   Df Model:                            2\n                                        Pseudo R-squ.:                  0.2130\n                                        Log-Likelihood:                -98.234\n                                        LL-Null:                       -124.79\n                                        LLR p-value:                 1.234e-12\n==============================================================================\n                 coef    std err          z      P>|z|      [0.025      0.975]\n------------------------------------------------------------------------------\nIntercept     -1.2345      0.321     -3.845      0.000      -1.864      -0.605\nx1             0.6789      0.145      4.682      0.000       0.395       0.963\nx2            -0.3456      0.198     -1.745      0.081      -0.734       0.043\n=============================================================================="}
    ],"-","tableau texte avec tous les indicateurs détaillés ci-dessous — Pseudo R-squ. / Log-Likelihood / LL-Null en haut à droite ; coef/std err/z/P>|z| par feature dans le tableau du bas (z au lieu de t, cf. ligne ci-dessous) — mêmes emplacements que pour OLS, avec z à la place de t"],
    ["Log-Likelihood (LL) et Pseudo R²","$$\\text{Pseudo } R^2 = 1 - \\dfrac{LL(\\text{model})}{LL(\\text{null})}$$\nmodel.**llf**        # LL(model)\nmodel.**llnull**     # LL(null)\nmodel.**prsquared**  # Pseudo R²","LL(model) : log-vraisemblance du modèle ajusté\nLL(null) : log-vraisemblance du modèle sans aucune feature (intercept seul)","les 3 sont déjà calculés par statsmodels, pas besoin de les recalculer à la main — accessibles en attributs (llf/llnull/prsquared) ET affichés directement dans l'en-tête de model.summary() (\"Log-Likelihood\", \"LL-Null\", \"Pseudo R-squ.\") ; LL ∈ ]-∞,0], plus proche de 0 = meilleur fit (rôle analogue au SSR pour OLS) ; Pseudo R² ∈ [0,1] permet de comparer des modèles entre eux, mais est moins directement interprétable qu'un vrai R²"],
    ["Vérifier LL(null) en ajustant soi-même le modèle nul (intercept seul)","model_null = smf.**logit**(\"y ~ 1\", data=df).fit()\nmodel_null.**llf**  # == model.llnull","\"y ~ 1\" : formule sans aucune feature, juste l'intercept","optionnel : sert surtout à vérifier/comprendre d'où vient model.llnull (log-vraisemblance d'un modèle qui ne fait que prédire la fréquence moyenne de y) — la même syntaxe \"y ~ 1\" fonctionne avec smf.ols pour une régression linéaire"],
  ]},
  {id:"ml-models-overview", group:"ml", subgroup:"Choix du modèle", title:"Modèles Sklearn vus — Régression vs Classification", headers:["Nom","Syntaxe","Métrique .score()","Explication"], colClasses:["col-nom","col-formule","col-params","col-explication"], rows:[
    ["LinearRegression — régression","from sklearn.linear_model import LinearRegression\nmodel = **LinearRegression**()","R²","target continue (ex: prix) — équivalent Sklearn de smf.ols, sans les p-values/IC (cf. page Maths ▸ Régression pour l'inférence statistique)"],
    ["LogisticRegression — classification","from sklearn.linear_model import LogisticRegression\nmodel = **LogisticRegression**()","accuracy","malgré le nom « regression », c'est un modèle de CLASSIFICATION : target catégorielle (ex: churn oui/non) — prédit une probabilité puis une classe ; équivalent Sklearn de smf.logit (cf. logit-fit pour l'inférence statistique)"],
    ["KNeighborsRegressor / KNeighborsClassifier — KNN","from sklearn.neighbors import KNeighborsRegressor, KNeighborsClassifier\nmodel = **KNeighborsClassifier**(n_neighbors=5)","R² / accuracy","modèle non-linéaire basé sur la distance : prédit à partir des K plus proches voisins du point (moyenne de leurs y en régression, vote majoritaire en classification) — sensible à l'échelle des features, toujours scaler avant (cf. Feature Scaling) ; existe aussi en NearestNeighbors pour la similarité/clustering (cf. Unsupervised Learning) et en KNNImputer pour imputer les valeurs manquantes"],
  ]},
  {id:"ml-model-attributes", group:"ml", subgroup:"Choix du modèle", title:"Travailler avec un estimator déjà choisi", rows:[
    ["Coefficients appris (régression linéaire)","model.**coef_**\nmodel.**intercept_**","pente(s) a et ordonnée à l'origine b — attributs suffixés _ par convention Sklearn (disponibles seulement après fit)"],
    ["Trouver les valeurs valides d'un paramètre (ex: strategy)","**SimpleImputer?**  # Jupyter : ouvre la docstring\n# ou Shift+Tab dans SimpleImputer(strategy=|)\n# ou provoquer l'erreur exprès :\nSimpleImputer(strategy=\"bogus\").fit([[1],[2]])","le message d'erreur liste les valeurs acceptées : \"... must be a str among {'most_frequent', 'constant', 'median', 'mean'}...\" — marche pour n'importe quel paramètre/classe Sklearn"],
  ]},
  {id:"ml-multiclass-strategy-syntax", group:"ml", subgroup:"Choix du modèle", title:"Forcer une stratégie multiclasse (One-vs-Rest / One-vs-One)", rows:[
    ["One-vs-Rest — un modèle par classe","from sklearn.multiclass import OneVsRestClassifier\nmodel = **OneVsRestClassifier**(LogisticRegression())","cf. page Modélisation ▸ [Stratégies multiclasse (One-vs-Rest vs One-vs-One)](#ml-multiclass-strategies)"],
    ["One-vs-One — un modèle par paire de classes","from sklearn.multiclass import OneVsOneClassifier\nmodel = **OneVsOneClassifier**(LogisticRegression())","utilisable ensuite comme n'importe quel estimator Sklearn (cross_validate, .fit/.predict...)"],
  ]},
  {id:"ml-knn-hyperparams", group:"ml", subgroup:"Choix du modèle", title:"KNN — hyperparamètres", rows:[
    ["n_neighbors (K)","KNeighborsClassifier(**n_neighbors**=5)","nombre de voisins pris en compte — K petit → overfitting (sensible au bruit d'un seul point) ; K grand → underfitting (signal dilué parmi trop de voisins)"],
    ["weights","KNeighborsClassifier(n_neighbors=5, **weights**=\"distance\")","uniform (défaut) : chaque voisin compte pareil, stable mais un voisin proche peut être noyé par des voisins lointains ; distance : les voisins proches comptent plus, plus réactif mais plus sensible à une exception proche"],
    ["p (distance utilisée)","KNeighborsClassifier(n_neighbors=5, **p**=1)","2 (défaut) : distance euclidienne, frontières de décision lisses ; 1 : distance de Manhattan, frontières plus anguleuses mais plus rapide en grande dimension"],
  ]},
  {id:"ml-svm", group:"ml", subgroup:"Choix du modèle", title:"SVM — Support Vector Machines (SVC / SVR)", headers:["Nom","Syntaxe","Params","Explication"], colClasses:["col-nom","col-formule","col-params","col-explication"], rows:[
    ["SVC — classification à marge maximale","from sklearn.svm import SVC\nmodel = **SVC**(kernel='linear', C=10)","kernel : linear/poly/rbf/sigmoid — C : force de la pénalité sur les points mal classés","toujours scaler les features avant (cf. Feature Scaling) — cf. page Modélisation ▸ SVM pour le détail marge/kernel"],
    ["SVR — régression à marge maximale","from sklearn.svm import SVR\nmodel = **SVR**(kernel='rbf', C=1, epsilon=0.1)","epsilon : largeur de la \"rue\" dans laquelle les points ne sont pas pénalisés","même principe que SVC, inversé : fitter le plus de points possible DANS la marge plutôt qu'en dehors"],
    ["Équivalent SGD (kernel linéaire uniquement)","from sklearn.linear_model import SGDClassifier\nmodel = **SGDClassifier**(loss='hinge', penalty='l2', alpha=1/C)","-","solver SGD au lieu d'une résolution exacte — beaucoup plus rapide sur un gros dataset, mais uniquement kernel linéaire (pas de kernel trick)"],
    ["Analyser le modèle entraîné","model.**n_support_**       # nb de vecteurs de support par classe\nmodel.**decision_function**(X)  # distance signée à l'hyperplan","-","n_support_ élevé = frontière complexe (beaucoup de points proches de la marge) ; decision_function() renvoie la distance AVANT application du seuil (contrairement à predict())"],
    ["⚠️ Éviter un blocage du solveur (kernel linéaire + C élevé)","SVC(kernel='linear', C=100, **max_iter**=10_000)","-","sur des données pas parfaitement séparables, libsvm peut ne JAMAIS converger (bloqué indéfiniment, pas juste lent) — max_iter force un arrêt propre (ConvergenceWarning) au lieu d'un blocage"],
  ]},
  {id:"ml-explore", group:"ml", subgroup:"Exploration & Nettoyage", title:"Explorer avant de modéliser", rows:[
    ["Vérifier NaN, doublons, équilibre des classes","df.**isnull**().sum()\ndf.**duplicated**().sum()\ndf['target'].**value_counts**()","cf. section Pandas ▸ Exploration pour les commandes générales (head, describe, dtypes...) — ici on regarde spécifiquement ce qui impacte un modèle ML"],
    ["Séparer colonnes numériques / catégorielles","X_num = X.**select_dtypes**(exclude=['object'])\nX_cat = X.**select_dtypes**(include=['object'])","pratique pour appliquer un scaler aux unes et un encoder aux autres sans lister les noms de colonnes à la main"],
  ]},
  {id:"ml-duplicates", group:"ml", subgroup:"Exploration & Nettoyage", title:"Nettoyer — Doublons", rows:[
    ["Détecter / compter les doublons","df.**duplicated**()          # booléen par ligne\ndf.duplicated().**sum**()    # nombre de doublons","-"],
    ["Supprimer les doublons","df = df.**drop_duplicates**()","cf. Data Leakage : des lignes dupliquées présentes à la fois en train et en test faussent l'évaluation"],
  ]},
  {id:"ml-missing-data", group:"ml", subgroup:"Exploration & Nettoyage", title:"Nettoyer — Valeurs manquantes", rows:[
    ["Compter les NaN par colonne (nombre / %)","df.**isnull**().sum().sort_values(ascending=False)\ndf.**isnull**().sum() / len(df)   # en proportion","-"],
    ["Imputer avec SimpleImputer","from sklearn.impute import SimpleImputer\nimputer = SimpleImputer(strategy=\"mean\")\nimputer.**fit**(df[['col']])\ndf[['col']] = imputer.**transform**(df[['col']])\nimputer.**statistics_**","strategy : mean, median, most_frequent, constant — .fit() apprend la valeur de remplacement, .transform() l'applique (logique commune à tous les transformers Sklearn) — garder les doubles crochets df[['col']] des DEUX côtés (et pas df['col'] à gauche) marche pour toutes les colonnes, numériques ou catégorielles"],
    ["Piège (si jamais assigné en simple crochet df['col'])","df['col'] = imputer.transform(df[['col']]).**ravel**()  # ou .flatten()","transform() renvoie toujours un array **2D** (n,1) ; assigné à df['col'] (Series 1D), ça passe silencieusement sur une colonne numérique (pandas squeeze tout seul) mais lève une ValueError sur une colonne texte/objet (ex: most_frequent) — le plus simple est d'éviter le problème en assignant à df[['col']] (double crochets) plutôt que d'ajouter .ravel()"],
    ["Imputer avec KNNImputer","from sklearn.impute import KNNImputer\nimputer = **KNNImputer**(n_neighbors=5)\ndf[cols] = imputer.fit_transform(df[cols])","n_neighbors : nb de voisins utilisés pour la moyenne","remplace par la moyenne des k plus proches voisins (sur les AUTRES colonnes) au lieu d'une statistique globale — scaler les features avant (sensible à l'échelle, comme tout KNN)"],
  ]},
  {id:"ml-outliers", group:"ml", subgroup:"Exploration & Nettoyage", title:"Nettoyer — Outliers", rows:[
    ["Visualiser les outliers","df[['col']].**boxplot**()\nsns.**boxplot**(data=df, x='col')","-"],
    ["Supprimer les valeurs clairement fausses","mask = (df['col'] > 0) & (df['col'] < 5000)\ndf = df[mask].**reset_index**(drop=True)","⚠️ un outlier n'est pas toujours une erreur — peut être une observation rare (novelty) ou une feature en soi ; à traiter au cas par cas"],
  ]},
  {id:"ml-holdout", group:"ml", subgroup:"Test", title:"Train/test split — train_test_split (Holdout Method)", rows:[
    ["Séparer train / test","from sklearn.model_selection import train_test_split\nX_train, X_test, y_train, y_test = **train_test_split**(X, y, test_size=0.3, random_state=88)","~70/30 usuel — random_state fixe le tirage aléatoire pour la reproductibilité"],
    ["Conserver les proportions de classes (target déséquilibrée)","train_test_split(X, y, test_size=0.3, **stratify**=y, random_state=88)","répartit chaque classe de y dans les mêmes proportions entre train et test — indispensable sur un dataset déséquilibré (cf. Balancing, groupe ml)"],
    ["Split en 3 : train / validation / test","X_tv, X_test, y_tv, y_test = train_test_split(X, y, test_size=0.2, stratify=y)\nX_train, X_val, y_train, y_val = train_test_split(X_tv, y_tv, test_size=0.25, stratify=y_tv)","pas de split 3-voies natif dans Sklearn — on enchaîne deux train_test_split ; ici 60/20/20 (0.25 × 0.8 restant = 0.2)"],
    ["Piège : scorer un modèle sur ses données d'entraînement","model.fit(X_train, y_train)\nmodel.score(**X_test**, **y_test**)  # jamais X_train, y_train","sinon on mesure la mémorisation, pas la capacité à généraliser sur des données non vues"],
    ["Stratifier sur une FEATURE plutôt que la target","train_test_split(X, y, **stratify**=X['col'], random_state=88)","cross_validate(cv=5) stratifie AUTOMATIQUEMENT sur la target pour un classifieur (StratifiedKFold en interne), mais jamais sur une feature — à faire soi-même si une catégorie (ex: une feature binaire) doit rester équilibrée entre folds"],
  ]},
  {id:"ml-data-leakage", group:"ml", subgroup:"Test", title:"Train/test split — Data Leakage, piège transversal", rows:[
    ["Règle générale","transformer.**fit**(X_train)  # jamais X_test ou X entier\nX_train = transformer.**transform**(X_train)\nX_test = transformer.**transform**(X_test)","toute transformation (scaler, encoder, imputer...) doit être fit uniquement sur le train set, puis appliquée (transform) identiquement au train et au test"],
  ]},
  {id:"ml-cross-validation", group:"ml", subgroup:"Test", title:"Cross-Validation", rows:[
    ["Valider sur K sous-échantillons","from sklearn.model_selection import cross_validate\ncv_results = **cross_validate**(model, X, y, cv=5)\ncv_results['test_score'].mean()","moyenne les scores sur K splits différents plutôt qu'un seul split — plus robuste qu'un simple train_test_split ; cv=5 (entier) choisit automatiquement KFold (régression) ou StratifiedKFold (classification), cf. Modélisation ▸ [Cross-Validation](#ml-cross-validation-concept)"],
    ["Variante : cross_val_score","from sklearn.model_selection import cross_val_score\n**cross_val_score**(model, X, y, cv=5, scoring=\"r2\").mean()","renvoie directement un array de scores (une seule métrique, via scoring) au lieu du dict de cross_validate (test_score, fit_time, ...) — pratique pour un score rapide sans les à-côtés"],
    ["Classification : préserver les proportions de classes","from sklearn.model_selection import StratifiedKFold\ncross_validate(model, X, y, **cv=StratifiedKFold(5)**)","déjà le comportement par défaut si on passe cv=5 à un classifieur — utile à expliciter si on veut personnaliser (shuffle=True, random_state)"],
    ["Time Series : respecter l'ordre chronologique","from sklearn.model_selection import TimeSeriesSplit\ncross_validate(model, X, y, **cv=TimeSeriesSplit(5)**)","chaque fold d'entraînement précède chronologiquement son fold de test — jamais de shuffle (cf. page Modélisation ▸ Modèles ▸ Time Series ▸ [Décomposition](#ts-decomposition))"],
    ["Données groupées : garder chaque groupe entier dans un seul fold","from sklearn.model_selection import GroupKFold\ncross_validate(model, X, y, **cv=GroupKFold(5)**, groups=groups)","évite qu'un même groupe (ex: plusieurs lignes par patient/utilisateur) se retrouve à la fois en train et en test — sinon data leakage"],
  ]},
  {id:"ml-feature-selection", group:"ml", subgroup:"Test", title:"Feature selection — corrélation & permutation importance", rows:[
    ["Repérer les features redondantes (corrélation univariée)","corr = df.select_dtypes('number').**corr**()\ncorr_df = corr.stack().reset_index()\ncorr_df.columns = ['f1','f2','correlation']","deux features très corrélées entre elles portent une information redondante — envisager d'en retirer une"],
    ["Piège : exclure la diagonale avant de trier","corr_df = corr_df[corr_df['f1'] **!= corr_df['f2']**]","chaque feature est corrélée à 1.0 avec elle-même — sans ce filtre, la diagonale (corrélation=1) noie le classement dès qu'on trie par force de corrélation"],
    ["Trier par force de corrélation","corr_df['abs_corr'] = corr_df['correlation'].**abs**()\ncorr_df.sort_values('abs_corr', ascending=False)","sans abs(), une corrélation négative forte (-0.9) passerait après une positive faible (0.5) — trie par force du lien, pas par signe"],
    ["Feature Permutation Importance","from sklearn.inspection import permutation_importance\nmodel.fit(X, y)\nresult = **permutation_importance**(model, X, y, n_repeats=10)\nresult.importances_mean","n_repeats (nombre de mélanges aléatoires par feature) : mélange aléatoirement une feature et mesure la chute de score — grosse chute = feature importante, chute nulle/négative = feature peu utile"],
    ["Éliminer les features faibles et re-valider","weak = importance_df[importance_df['importance_mean'] <= 0]['feature']\nX_reduced = X.**drop**(columns=weak)\ncross_validate(model, X_reduced, y, cv=5)['test_score'].mean()","seuil ≤ 0 : une importance nulle/négative signifie que mélanger la feature n'a pas dégradé (voire a amélioré) le score — comparer au score de base pour vérifier qu'il ne chute pas significativement"],
  ]},
  {id:"ml-scaling", group:"ml", subgroup:"Transformation", title:"Feature Scaling (Standard / MinMax / Robust)", headers:["Nom","Formule / Syntaxe","Params","Explication"], colClasses:["col-nom","col-formule","col-params","col-explication"], rows:[
    ["StandardScaler (standardiser)","$$z = \\dfrac{x - mean}{std}$$\nfrom sklearn.preprocessing import StandardScaler\nscaler = StandardScaler()\nscaler.**fit**(df[['col']])\ndf[['col']] = scaler.**transform**(df[['col']])","-","centre à 0 (μ=0) et réduit à écart-type 1 (σ=1) — sensible aux outliers ; bon choix par défaut pour la plupart des modèles (ex: régression linéaire, réseaux de neurones) — doubles crochets df[['col']] des deux côtés (cf. piège 2D/1D dans Valeurs manquantes)"],
    ["MinMaxScaler (normaliser)","$$X' = \\dfrac{X - X_{min}}{X_{max}-X_{min}}$$\nfrom sklearn.preprocessing import **MinMaxScaler**","-","compresse dans [0,1] — préserve la sparsité (0 reste 0), ne réduit pas l'effet des outliers — utile pour features positives/sparse (ex: pixels) ou pour KNN"],
    ["RobustScaler","$$RobustScaled = \\dfrac{x - median}{IQR}$$\nfrom sklearn.preprocessing import **RobustScaler**","IQR : écart interquartile Q3 − Q1","utilise médiane + IQR au lieu de moyenne + écart-type → moins sensible aux outliers que Standard/MinMax"],
    ["Fit sur train seulement, transform sur train ET test","scaler.**fit**(X_train)\nX_train = scaler.**transform**(X_train)\nX_test = scaler.**transform**(X_test)  # jamais de .fit() sur X_test","-","cf. Data Leakage — fitter un scaler sur tout le dataset (avant le split) fait fuiter de l'information du test set dans l'entraînement"],
    ["Réassigner plusieurs colonnes scalées d'un coup, par leur nom","X_train[cols] = scaler.transform(X_train[cols])\nX_train.columns  # ou : scaler.**get_feature_names_out**()","cols : liste de colonnes numériques","évite de retaper les noms de colonnes à la main quand on scale plusieurs features en une seule fois (contrairement à df[['col']] utilisé ci-dessus pour une seule colonne) — même pattern que pour OneHotEncoder (cf. Encoding)"],
    ["Récupérer directement un DataFrame (pas un array)","scaler.**set_output**(transform='pandas')\nX_scaled = scaler.fit_transform(X_num)  # DataFrame, colonnes/index conservés","-","évite de ré-assembler soi-même noms de colonnes et index après transform() — marche pour n'importe quel transformer Sklearn (scaler, encoder...)"],
    ["Même réglage, pour TOUS les transformers d'un coup","from sklearn import set_config\n**set_config**(transform_output=\"pandas\")  # une seule fois, en tête de notebook","-","équivalent global de .set_output(transform='pandas') — ⚠️ incompatible avec une sortie sparse : ajouter OneHotEncoder(sparse_output=False) sinon ValueError"],
  ]},
  {id:"ml-encoding", group:"ml", subgroup:"Transformation", title:"Encoding (variables catégorielles)", headers:["Nom","Syntaxe","Params","Explication"], colClasses:["col-nom","col-formule","col-params","col-explication"], rows:[
    ["OrdinalEncoder — catégories avec un ordre","from sklearn.preprocessing import OrdinalEncoder\nenc = OrdinalEncoder(categories=[['bad','average','good']])\nenc.**fit**(df[['col']])\nenc.**categories_**\ndf[['col_encoded']] = enc.**transform**(df[['col']])","categories : ordre explicite à préciser, sinon ordre alphabétique par défaut","à utiliser seulement si la variable a un vrai ordre — sinon crée une fausse relation numérique entre catégories"],
    ["OneHotEncoder — catégories sans ordre","from sklearn.preprocessing import OneHotEncoder\nohe = OneHotEncoder(sparse_output=False)\nohe.**fit**(df[['col']])\ndf[ohe.**get_feature_names_out**()] = ohe.**transform**(df[['col']])\ndf = df.**drop**(columns=['col'])","drop=\"if_binary\" : une seule colonne si la feature n'a que 2 catégories","crée une colonne binaire par catégorie — attention à la cardinalité (curse of dimensionality) sur les colonnes à beaucoup de catégories"],
    ["OneHotEncoder — limiter la cardinalité & gérer l'inconnu","OneHotEncoder(**max_categories**=10, **handle_unknown**='ignore')","max_categories : regroupe les catégories les moins fréquentes au-delà de N ; handle_unknown='ignore' : ne plante pas si le test set contient une catégorie absente du train (met 0 partout au lieu d'une erreur)","fit() reste sur le TRAIN uniquement — indispensable pour ne pas planter au .transform() du test set"],
    ["LabelEncoder — encoder la target (classification)","from sklearn.preprocessing import LabelEncoder\nle = LabelEncoder().fit(y)\ny_encoded = le.**transform**(y)\nle.**inverse_transform**(y_encoded)  # retour aux labels d'origine","-","souvent inutile : la plupart des modèles Sklearn gèrent une target texte directement — à réserver aux cas où l'encodage/décodage est explicitement nécessaire"],
  ]},
  {id:"ml-polynomial-features", group:"ml", subgroup:"Transformation", title:"PolynomialFeatures — capturer non-linéarités & interactions", rows:[
    ["Générer les termes polynomiaux et croisés","from sklearn.preprocessing import **PolynomialFeatures**\npoly = PolynomialFeatures(degree=2, include_bias=False)\nX_poly = poly.**fit_transform**(X)","pour chaque paire de features (a,b) : ajoute a², b², a·b (et plus haut degré si demandé) — permet à un modèle LINÉAIRE de capturer des relations non-linéaires/des interactions entre features"],
    ["Nommer les nouvelles colonnes générées","poly.**get_feature_names_out**(X.columns)","utile pour reconstruire un DataFrame lisible à partir du array retourné par fit_transform"],
  ]},
  {id:"ml-discretizing", group:"ml", subgroup:"Transformation", title:"Discretizing — pd.cut", rows:[
    ["Transformer une variable continue en catégories","df['col_bin'] = pd.**cut**(x=df['col'], bins=[min-1, mean, max+1], labels=['low','high'])","bins : n+1 bornes pour n labels — transforme une tâche de régression en tâche de classification (ex: prix → Low/High)"],
  ]},
  {id:"ml-balancing", group:"ml", subgroup:"Transformation", title:"Balancing (classes déséquilibrées)", rows:[
    ["SMOTE (oversampling synthétique)","from imblearn.over_sampling import SMOTE\nX_res, y_res = **SMOTE**(random_state=42).fit_resample(X_train, y_train)","génère de nouvelles observations minoritaires par combinaison linéaire d'observations existantes — équilibre parfaitement les deux classes"],
    ["RandomUnderSampler (undersampling aléatoire)","from imblearn.under_sampling import RandomUnderSampler\nX_res, y_res = **RandomUnderSampler**(random_state=42).fit_resample(X_train, y_train)","supprime aléatoirement des observations de la classe MAJORITAIRE jusqu'à équilibre parfait — simple mais perd de l'information (contrairement à SMOTE qui n'en ajoute que)"],
    ["TomekLinks (nettoyage de frontière)","from imblearn.under_sampling import TomekLinks\nX_res, y_res = **TomekLinks**().fit_resample(X_train, y_train)","ne rééquilibre PAS les classes — retire seulement les observations majoritaires formant une paire très proche avec une observation minoritaire (frontière ambiguë), pour rendre la frontière de décision plus nette"],
    ["⚠️ Resampler uniquement le TRAIN set","X_train_res, y_train_res = SMOTE().fit_resample(X_train, y_train)\n# jamais X_test/y_test — le test set doit refléter la vraie distribution","-","resampler le test fausserait l'évaluation (des observations synthétiques ou dupliquées évaluées comme si elles étaient réelles)"],
  ]},
  {id:"ml-baseline", group:"ml", subgroup:"Métriques", title:"Baseline Score", rows:[
    ["Baseline régression","from sklearn.dummy import DummyRegressor\nbaseline = **DummyRegressor**(strategy=\"mean\")\nbaseline.fit(X_train, y_train)\nbaseline.score(X_test, y_test)","strategy : mean, median, constant — prédit toujours la même valeur, quel que soit X"],
    ["Baseline classification","from sklearn.dummy import DummyClassifier\nbaseline = **DummyClassifier**(strategy=\"most_frequent\")","strategy : most_frequent (prédit toujours la classe majoritaire), stratified (tire aléatoirement selon les proportions de classes), uniform"],
    ["Baseline séquence/série temporelle — persistence","y_pred_baseline = X_test[:, **-1**, 0]  # dernière valeur observée de la séquence","DummyRegressor ignore X (prédit une constante) — pour une séquence, une baseline plus dure à battre est \"rien ne change\" : prédire la DERNIÈRE valeur connue plutôt qu'une moyenne globale"],
  ]},
  {id:"ml-metrics-regression-syntax", group:"ml", subgroup:"Métriques", title:"Métriques de régression", headers:["Nom","Syntaxe","Params","Explication"], colClasses:["col-nom","col-formule","col-params","col-explication"], rows:[
    ["Mean Squared Error (MSE)","from sklearn.metrics import mean_squared_error\n**mean_squared_error**(y_true, y_pred)","-","formule et quand l'utiliser : cf. page Modélisation ▸ [Métriques de régression](#ml-metrics-regression)"],
    ["Root Mean Squared Error (RMSE)","import math\nmath.**sqrt**(mean_squared_error(y_true, y_pred))","-","pas de fonction sklearn dédiée, se calcule à partir du MSE — cf. page Modélisation ▸ [Métriques de régression](#ml-metrics-regression)"],
    ["Mean Absolute Error (MAE)","from sklearn.metrics import mean_absolute_error\n**mean_absolute_error**(y_true, y_pred)","-","cf. page Modélisation ▸ [Métriques de régression](#ml-metrics-regression)"],
    ["Max Error","from sklearn.metrics import max_error\n**max_error**(y_true, y_pred)","-","cf. page Modélisation ▸ [Métriques de régression](#ml-metrics-regression)"],
    ["Coefficient de détermination R²","from sklearn.metrics import r2_score\n**r2_score**(y_true, y_pred)","-","métrique .score() par défaut d'un régresseur Sklearn — cf. page Modélisation ▸ [Métriques de régression](#ml-metrics-regression)"],
    ["Comparer plusieurs métriques en cross-validation","from sklearn.model_selection import cross_validate\ncross_validate(model, X, y, cv=5, **scoring**=['r2','max_error','neg_mean_absolute_error','neg_mean_squared_error'])","scoring : liste de noms de métriques Sklearn — préfixe neg_ obligatoire pour les métriques \"plus petit = meilleur\", car cross_validate maximise toujours son score","permet de comparer plusieurs métriques sans relancer un cross_validate par métrique"],
  ]},
  {id:"ml-metrics-classification-syntax", group:"ml", subgroup:"Métriques", title:"Métriques de classification", headers:["Nom","Syntaxe","Params","Explication"], colClasses:["col-nom","col-formule","col-params","col-explication"], rows:[
    ["Matrice de confusion","pd.**crosstab**(index=y_true, columns=y_pred)\n# ou : from sklearn.metrics import confusion_matrix\nconfusion_matrix(y_true, y_pred)","TP/TN/FP/FN","base de toutes les métriques ci-dessous — définitions TP/TN/FP/FN : cf. page Modélisation ▸ [Métriques de classification](#ml-metrics-classification)"],
    ["Visualiser la matrice de confusion","from sklearn.metrics import ConfusionMatrixDisplay\n**ConfusionMatrixDisplay**.from_estimator(model, X_test, y_test)\n# ou : ConfusionMatrixDisplay.from_predictions(y_test, y_pred)","normalize='true' : proportions au lieu des comptages bruts","from_estimator = modèle + X à prédire ; from_predictions = prédictions déjà calculées (ex: via cross_val_predict)"],
    ["Toutes les métriques d'un coup, par classe","from sklearn.metrics import classification_report\nprint(**classification_report**(y_true, y_pred))","-","tableau récapitulatif precision/recall/f1-score/support pour chaque classe — pratique pour un premier coup d'œil sans appeler chaque fonction séparément"],
    ["Accuracy","from sklearn.metrics import accuracy_score\n**accuracy_score**(y_true, y_pred)","-","cf. page Modélisation ▸ [Métriques de classification](#ml-metrics-classification)"],
    ["Recall","from sklearn.metrics import recall_score\n**recall_score**(y_true, y_pred)","-","cf. page Modélisation ▸ [Métriques de classification](#ml-metrics-classification), et Precision-Recall Tradeoff ci-dessous"],
    ["Precision","from sklearn.metrics import precision_score\n**precision_score**(y_true, y_pred)","-","cf. page Modélisation ▸ [Métriques de classification](#ml-metrics-classification), et Precision-Recall Tradeoff ci-dessous"],
    ["F1 score","from sklearn.metrics import f1_score\n**f1_score**(y_true, y_pred)","-","cf. page Modélisation ▸ [Métriques de classification](#ml-metrics-classification)"],
  ]},
  {id:"ml-precision-recall-tradeoff", group:"ml", subgroup:"Métriques", title:"Precision-Recall Tradeoff", rows:[
    ["Explorer tous les seuils","from sklearn.metrics import precision_recall_curve\nprecision, recall, thresholds = **precision_recall_curve**(y_true, y_proba)","y_proba : probabilités prédites de la classe positive, via model.predict_proba(X)[:,1] — donne precision et recall pour chaque seuil possible"],
    ["Trouver le seuil garantissant un recall minimum","scores = pd.DataFrame({'threshold':thresholds,'precision':precision[:-1],'recall':recall[:-1]})\nscores[scores['recall'] >= 0.8].**tail**(1)","parmi les seuils qui garantissent le recall visé, prendre le plus élevé maximise la precision associée à ce recall"],
    ["Appliquer le nouveau seuil pour reclasser","preds = (y_proba >= **seuil_choisi**).astype(int)  # au lieu du seuil 0.5 par défaut","une fois le seuil optimal trouvé, reclasse les probabilités manuellement plutôt que d'utiliser model.predict() (seuil fixe à 0.5)"],
  ]},
  {id:"ml-roc-auc-syntax", group:"ml", subgroup:"Métriques", title:"ROC-AUC", headers:["Nom","Syntaxe","Params","Explication"], colClasses:["col-nom","col-formule","col-params","col-explication"], rows:[
    ["Courbe ROC","from sklearn.metrics import roc_curve\nfpr, tpr, thresholds = **roc_curve**(y_true, y_proba)","fpr (False Positive Rate) = FP/(FP+TN)\ntpr (True Positive Rate) = recall","contrairement à precision_recall_curve, les deux axes se basent sur la matrice de confusion complète — cf. page Modélisation ▸ [ROC-AUC](#ml-roc-auc)"],
    ["AUC (Area Under Curve)","from sklearn.metrics import roc_auc_score\n**roc_auc_score**(y_true, y_proba)","-","cf. page Modélisation ▸ [ROC-AUC](#ml-roc-auc)"],
    ["PR-AUC (Average Precision)","from sklearn.metrics import average_precision_score\n**average_precision_score**(y_true, y_proba)\n# ou : cross_validate(model, X, y, scoring='average_precision')","-","cf. page Modélisation ▸ [ROC-AUC](#ml-roc-auc)"],
    ["Visualiser la courbe precision-recall","from sklearn.metrics import PrecisionRecallDisplay\n**PrecisionRecallDisplay**.from_estimator(model, X_test, y_test)\n# ou : .from_predictions(y_test, y_proba)","-","équivalent \"tout-en-un\" de precision_recall_curve + plt.plot (cf. [Precision-Recall Tradeoff](#ml-precision-recall-tradeoff), ci-dessus)"],
  ]},
  {id:"ml-calibration-syntax", group:"ml", subgroup:"Métriques", title:"Calibration de probabilités", rows:[
    ["Tracer la courbe de calibration","from sklearn.calibration import CalibrationDisplay\n**CalibrationDisplay**.from_estimator(model, X_test, y_test)","à faire sur un jeu de TEST dédié (pas le train) — diagonale = bien calibré ; cf. page Modélisation ▸ [Calibration de probabilités](#ml-calibration)"],
    ["Calculer les points de la courbe soi-même","from sklearn.calibration import calibration_curve\nprob_true, prob_pred = **calibration_curve**(y_test, y_proba, n_bins=10)","n_bins : nombre de buckets de probabilité","version \"bas niveau\" de CalibrationDisplay — utile pour calculer un écart chiffré (ex: |prob_true - prob_pred|) plutôt que juste visualiser"],
    ["Recalibrer un modèle mal calibré","from sklearn.calibration import CalibratedClassifierCV\ncalibrated = **CalibratedClassifierCV**(model, cv=5)\ncalibrated.fit(X_train, y_train)\ncalibrated.predict_proba(X_new)","entraîne des clones du modèle sur des sous-folds puis recalibre leurs probabilités — les prédictions de classe (predict()) ne changent pas, seules les probabilités sont corrigées"],
  ]},
  {id:"ml-regularization-syntax", group:"ml", subgroup:"Régularisation & Tuning", title:"Régularisation (Ridge / Lasso / ElasticNet)", headers:["Nom","Syntaxe","Params","Explication"], colClasses:["col-nom","col-formule","col-params","col-explication"], rows:[
    ["Ridge (pénalité L2)","from sklearn.linear_model import Ridge\nmodel = **Ridge**(alpha=0.2)","alpha : force de la régularisation (α grand → plus de bias, moins de variance)","pénalise les gros coefficients sans jamais les mettre à 0 — bon choix par défaut si toutes les features comptent un peu"],
    ["Lasso (pénalité L1)","from sklearn.linear_model import Lasso\nmodel = **Lasso**(alpha=0.2)","alpha : idem Ridge","peut ramener des coefficients exactement à 0 → fait aussi office de sélection de features (meilleure interprétabilité)"],
    ["ElasticNet (L1 + L2)","from sklearn.linear_model import ElasticNet\nmodel = **ElasticNet**(alpha=1, l1_ratio=0.2)","l1_ratio : proportion L1/L2 (0 = Ridge pur, 1 = Lasso pur)","moyenne pondérée Ridge/Lasso — 2 hyperparamètres à tuner (cf. page Modélisation ▸ Régularisation pour le détail des formules)"],
    ["⚠️ Toujours scaler avant de régulariser","StandardScaler().fit_transform(X)  # AVANT Ridge/Lasso/ElasticNet","-","sinon chaque βᵢ n'est pas pénalisé équitablement (cf. Feature Scaling)"],
  ]},
  {id:"ml-grid-search", group:"ml", subgroup:"Régularisation & Tuning", title:"Model Tuning — GridSearchCV / RandomizedSearchCV", headers:["Nom","Syntaxe","Params","Explication"], colClasses:["col-nom","col-formule","col-params","col-explication"], rows:[
    ["GridSearchCV — recherche exhaustive","from sklearn.model_selection import GridSearchCV\ngrid = {'alpha':[0.01,0.1,1], 'l1_ratio':[0.2,0.5,0.8]}\nsearch = **GridSearchCV**(model, grid, scoring='r2', cv=5, n_jobs=-1)\nsearch.fit(X_train, y_train)","n_jobs=-1 : parallélise le calcul sur tous les cœurs disponibles","teste toutes les combinaisons de la grille — search.best_score_ / search.best_params_ / search.best_estimator_ une fois fit"],
    ["RandomizedSearchCV — recherche aléatoire","from sklearn.model_selection import RandomizedSearchCV\nfrom scipy import stats\ngrid = {'alpha':[0.001,0.01,0.1,1], 'l1_ratio': stats.uniform(0,1)}\nsearch = **RandomizedSearchCV**(model, grid, n_iter=100, cv=5, n_jobs=-1)","n_iter : nombre de tirages aléatoires","tire n_iter combinaisons au hasard dans l'espace des hyperparamètres — scalable à un grand espace, contrairement à GridSearchCV"],
    ["Distributions pour une recherche coarse-grain","from scipy import stats\nstats.uniform(1, 100)      # pas d'a priori\nstats.**loguniform**(0.01, 1)  # balaie plusieurs ordres de grandeur\nstats.norm(10, 2)          # si on a une valeur pressentie","-","loguniform : idéal pour un premier balayage large (ex: alpha) avant d'affiner autour de la meilleure zone trouvée"],
    ["Optimiser sur une métrique maison","from sklearn.metrics import make_scorer\n\ndef precision_classe_2(y_true, y_pred):\n    tp2 = sum(1 for yt, yp in zip(y_true, y_pred) if yt == 2 and yp == 2)\n    fp2 = sum(1 for yt, yp in zip(y_true, y_pred) if yt != 2 and yp == 2)\n    return tp2 / (tp2 + fp2)\n\nsearch = RandomizedSearchCV(model, grid, **scoring**=make_scorer(precision_classe_2))","-","une fonction brute `(y_true, y_pred) -> score` n'est PAS un scorer sklearn valide — make_scorer() l'enveloppe pour la rendre utilisable partout où sklearn attend un `scoring` (cross_validate, GridSearchCV...)"],
    ["Grilles différentes selon une valeur catégorielle (ex: kernel)","param_distributions = [\n    {\"kernel\": [\"linear\"], \"C\": loguniform(1e-1, 1e4)},\n    {\"kernel\": [\"rbf\"], \"C\": loguniform(1e-1, 1e4), \"**gamma**\": loguniform(1e-3, 1e2)},\n]\nRandomizedSearchCV(SVC(), param_distributions, n_iter=400)","une LISTE de dicts au lieu d'un seul","chaque dict ne contient que les hyperparamètres pertinents pour SA valeur (ex: gamma n'a pas de sens pour un kernel linéaire) — à chaque tirage, un dict est choisi au hasard puis échantillonné"],
  ]},
  {id:"ml-pipelines-preproc", group:"ml", subgroup:"Pipelines", title:"Pipeline, ColumnTransformer, FeatureUnion", headers:["Nom","Syntaxe","Params","Explication"], colClasses:["col-nom","col-formule","col-params","col-explication"], rows:[
    ["Pipeline — chaîne d'étapes en séquence","from sklearn.pipeline import Pipeline\npipe = **Pipeline**([('imputer', SimpleImputer()), ('scaler', StandardScaler())])\npipe.fit(X_train)","-","exécute .fit()/.transform() de chaque étape dans l'ordre — hérite des méthodes du dernier objet (transform si un transformer, predict/score si un modèle)"],
    ["ColumnTransformer — étapes en parallèle par colonne","from sklearn.compose import ColumnTransformer\npreproc = **ColumnTransformer**([('num', num_pipe, ['age','bmi']), ('cat', OneHotEncoder(), ['region'])], remainder='passthrough')","remainder='passthrough' : conserve les colonnes non listées telles quelles (sinon supprimées par défaut)","applique des transformations différentes à des colonnes différentes EN PARALLÈLE (contrairement à Pipeline, séquentiel)"],
    ["Récupérer les noms de colonnes après transformation","preproc.**get_feature_names_out**()","-","fit_transform() renvoie un array sans noms de colonnes — disponible sur tous les transformers depuis Sklearn 1.1.3"],
    ["FeatureUnion — transformers en parallèle, résultats concaténés","from sklearn.pipeline import FeatureUnion\nunion = **FeatureUnion**([('preprocess', preproc), ('ratio', ratio_constructor)])","-","contrairement à ColumnTransformer (colonnes différentes), applique plusieurs transformers au MÊME jeu de colonnes puis concatène — utile pour ajouter une feature entièrement nouvelle en plus du preprocessing"],
    ["Raccourcis make_*** (noms d'étapes auto-générés)","from sklearn.pipeline import make_pipeline, make_union\nfrom sklearn.compose import make_column_transformer\n**make_pipeline**(SimpleImputer(), StandardScaler())","-","équivalents de Pipeline/ColumnTransformer/FeatureUnion sans nommer chaque étape (nom = classe en minuscules)"],
    ["make_column_selector — sélection par dtype","from sklearn.compose import make_column_selector\nnum_col = **make_column_selector**(dtype_include=['float64'])\ncat_col = make_column_selector(dtype_include=['object','bool'])","dtype_include / dtype_exclude","sélectionne des colonnes automatiquement par type plutôt que de lister leurs noms — pratique quand les colonnes concernées varient d'un dataset à l'autre"],
  ]},
  {id:"ml-pipelines-custom", group:"ml", subgroup:"Pipelines", title:"Transformers personnalisés", headers:["Nom","Syntaxe","Params","Explication"], colClasses:["col-nom","col-formule","col-params","col-explication"], rows:[
    ["FunctionTransformer — encapsuler une fonction","from sklearn.preprocessing import FunctionTransformer\nrounder = **FunctionTransformer**(lambda x: np.round(x, decimals=2))","-","encapsule une fonction Python en Transformer Sklearn, utilisable en Pipeline/ColumnTransformer — uniquement pour des transformations stateless (qui n'apprennent rien pendant .fit(), ex: log(X))"],
    ["Classe custom — transformation stateful","from sklearn.base import TransformerMixin, BaseEstimator\nclass MyTransformer(TransformerMixin, BaseEstimator):\n    def **fit**(self, X, y=None):\n        ...  # stocker ce qui doit être appris\n        return self\n    def **transform**(self, X, y=None):\n        ...\n        return X","BaseEstimator fournit get_params()/set_params() ; TransformerMixin fournit fit_transform() à partir de fit()+transform()","nécessaire si la transformation est stateful (stocke une info pendant fit(), réutilisée dans transform(), ex: un scaler maison) — FunctionTransformer ne le permet pas"],
  ]},
  {id:"ml-pipelines-model", group:"ml", subgroup:"Pipelines", title:"Pipeline complet (preprocessing + modèle)", headers:["Nom","Syntaxe","Params","Explication"], colClasses:["col-nom","col-formule","col-params","col-explication"], rows:[
    ["Brancher un modèle en bout de pipeline","pipeline = **make_pipeline**(preproc, Ridge())\npipeline.fit(X_train, y_train)\npipeline.score(X_test, y_test)","-","rend le preprocessing + modèle utilisable comme n'importe quel estimator Sklearn (cross_validate, GridSearchCV...) tout en gardant le preprocessing synchronisé"],
    ["Cross-valider un pipeline complet","from sklearn.model_selection import cross_val_score\n**cross_val_score**(pipeline, X_train, y_train, cv=5, scoring='r2')","-","identique à cross-valider un modèle seul — le preprocessing est refit à chaque fold, sans fuite entre folds"],
    ["GridSearch un pipeline (preprocessing + modèle)","GridSearchCV(pipeline, param_grid={\n  '**ridge__alpha**': [0.1,1,10],\n  '**columntransformer__pipeline__simpleimputer__strategy**': ['mean','median']\n}, cv=5)","syntaxe : nom_étape__param (nom_étape__sous_étape__param si étape imbriquée)","pipeline.get_params() liste tous les hyperparamètres accessibles, à tous les étages — permet de tuner preprocessing ET modèle en une seule GridSearch"],
    ["Debug — inspecter une étape intermédiaire","pipeline.**named_steps**.keys()\npipeline.named_steps['columntransformer'].fit_transform(X_train).shape","-","accède à chaque étape par son nom pour inspecter un résultat intermédiaire (ex: shape avant/après preprocessing)"],
    ["Exporter / recharger un pipeline entraîné","import pickle\npickle.**dump**(pipeline_tuned, open('pipeline.pkl','wb'))\nmy_pipeline = pickle.**load**(open('pipeline.pkl','rb'))","-","sauvegarde preprocessing + modèle entraînés pour les recharger sans réentraîner — utile pour le déploiement (module ML Ops)"],
  ]},
  {id:"ml-predictions", group:"ml", subgroup:"Prédictions", title:"Réentraînement final & prédiction sur nouvelle donnée", rows:[
    ["Réentraîner sur l'ensemble des données","model.**fit**(X, y)  # X, y = dataset complet, pas X_train/y_train","remplace le modèle entraîné sur X_train par un modèle entraîné sur toutes les données disponibles"],
    ["Prédire sur une donnée nouvelle","new_point_scaled = scaler.transform(new_point)\nmodel.**predict**(new_point_scaled)","ne jamais oublier de réappliquer le même preprocessing (scaler déjà fit pendant l'étape Test, jamais refit) aux nouvelles données avant de prédire"],
  ]},
  {id:"ml-pca-syntax", group:"ml", subgroup:"Unsupervised Learning", title:"PCA — réduction de dimension", headers:["Nom","Syntaxe","Params","Explication"], colClasses:["col-nom","col-formule","col-params","col-explication"], rows:[
    ["PCA — fit + projeter","from sklearn.decomposition import PCA\npca = **PCA**(n_components=3)\nX_proj = pca.**fit_transform**(X)","n_components : k dimensions gardées","toujours scaler (StandardScaler) avant de fitter une PCA — cf. page Modélisation ▸ PCA"],
    ["Variance expliquée par composante","pca.**explained_variance_ratio_**","-","sert à choisir k avec la méthode du coude (cf. Modélisation ▸ PCA)"],
    ["Choisir k via un seuil de variance cumulée","cum_var = np.**cumsum**(pca.explained_variance_ratio_)\nk = int(np.argmax(cum_var >= 0.80) + 1)","seuil (ex: 0.80)","alternative chiffrée à la méthode du coude — plus petit k qui atteint au moins X% de variance cumulée totale"],
    ["Revenir à l'espace d'origine (approx.)","X_reconstructed = pca.**inverse_transform**(X_proj)","-","reconstruction imparfaite si k < nombre de features initial (perte d'info)"],
    ["Chercher k par GridSearch, dans un pipeline","pipe = make_pipeline(PCA(), SVC())\nGridSearchCV(pipe, {'**pca__n_components**': [50,100,200,300]}, cv=5)","-","laisse la cross-validation trancher k au lieu d'un seuil de variance fixé à l'avance — syntaxe étape__param, cf. GridSearch un pipeline (groupe ml, Model Tuning)"],
  ]},
  {id:"ml-kmeans-syntax", group:"ml", subgroup:"Unsupervised Learning", title:"K-Means — clustering", headers:["Nom","Syntaxe","Params","Explication"], colClasses:["col-nom","col-formule","col-params","col-explication"], rows:[
    ["KMeans — fit","from sklearn.cluster import KMeans\nkm = **KMeans**(n_clusters=3)\nkm.fit(X)","n_clusters : K","cf. page Modélisation ▸ K-Means pour l'algorithme (centroïdes, inertia)"],
    ["Récupérer les clusters trouvés","km.**labels_**          # cluster de chaque observation\nkm.**cluster_centers_**  # coordonnées des centroïdes","-","-"],
    ["Choisir K (méthode du coude)","km.**inertia_**   # somme des distances au carré aux centroïdes","-","tracer inertia_ pour plusieurs K et chercher le coude — même logique que pour choisir k en PCA"],
    ["Prédire le cluster d'une nouvelle donnée","km.**predict**(new_X)","-","K-Means peut aussi bien fitter que prédire, contrairement à un clustering purement descriptif"],
  ]},
  {id:"ml-recommender-syntax", group:"ml", subgroup:"Unsupervised Learning", title:"Systèmes de recommandation — TruncatedSVD & similarité", headers:["Nom","Syntaxe","Params","Explication"], colClasses:["col-nom","col-formule","col-params","col-explication"], rows:[
    ["Réduire une matrice CREUSE (comptage de mots, notes...)","from sklearn.decomposition import TruncatedSVD\nsvd = **TruncatedSVD**(n_components=25, random_state=42)\nlatent = svd.fit_transform(count_matrix)","n_components : dimensions gardées","équivalent de PCA mais NE CENTRE PAS les données — utilisable directement sur une matrice creuse (Bag-of-Words, pivot de notes), contrairement à PCA qui exige un centrage préalable"],
    ["Calculer une similarité item-à-tous","from sklearn.metrics.pairwise import cosine_similarity\nsims = **cosine_similarity**(latent_df.loc[['item_cible']], latent_df)[0]","-","angle entre vecteurs — proche de 1 = très similaire, indépendant de la norme des vecteurs (cf. Modélisation ▸ [Systèmes de recommandation](#ml-recommender-systems))"],
  ]},
  {id:"ml-hierarchical-clustering-syntax", group:"ml", subgroup:"Unsupervised Learning", title:"Clustering Hiérarchique — dendrogramme", headers:["Nom","Syntaxe","Params","Explication"], colClasses:["col-nom","col-formule","col-params","col-explication"], rows:[
    ["Calculer les fusions successives","from scipy.cluster.hierarchy import linkage\nZ = **linkage**(X, method='ward')","method : 'ward' (minimise la variance intra-cluster, le plus courant), 'average', 'complete'...","Z encode l'arbre complet des fusions — cf. page Modélisation ▸ Clustering Hiérarchique"],
    ["Visualiser le dendrogramme","from scipy.cluster.hierarchy import dendrogram\n**dendrogram**(Z, color_threshold=50)","color_threshold : hauteur de coupe affichée","couper à une hauteur donnée = choisir un nombre de clusters, sans relancer l'algorithme"],
  ]},
  {id:"ml-nearest-neighbors-syntax", group:"ml", subgroup:"Unsupervised Learning", title:"NearestNeighbors — recherche de similarité", headers:["Nom","Syntaxe","Params","Explication"], colClasses:["col-nom","col-formule","col-params","col-explication"], rows:[
    ["Indexer un jeu de données","from sklearn.neighbors import NearestNeighbors\nnn = **NearestNeighbors**(n_neighbors=11)\nnn.fit(X)","n_neighbors : k+1 si on interroge un point déjà dans X (le point lui-même sera son propre plus proche voisin, distance 0)","non supervisé : pas de .predict(), .fit() ne fait que STOCKER les données — contrairement à KNeighborsRegressor/Classifier, ne prédit ni ne classe rien"],
    ["Trouver les k plus proches voisins d'un point","distances, indices = nn.**kneighbors**(X_query)","-","renvoie deux arrays parallèles (distance + indice dans X) — base d'un système de recommandation par similarité"],
    ["⚠️ Toujours scaler avant de calculer des distances","scaler = MinMaxScaler().fit(X)\nnn.fit(scaler.transform(X))","-","des features sur des échelles très différentes dominent le calcul de distance — même piège que pour KNeighborsClassifier/Regressor (cf. Feature Scaling)"],
  ]},
  {id:"ml-decision-tree-syntax", group:"ml", subgroup:"Ensemble Methods", title:"Arbre de décision — DecisionTreeClassifier / Regressor", headers:["Nom","Syntaxe","Params","Explication"], colClasses:["col-nom","col-formule","col-params","col-explication"], rows:[
    ["Instancier et fitter","from sklearn.tree import DecisionTreeClassifier\ntree = **DecisionTreeClassifier**(max_depth=3)\ntree.fit(X, y)","max_depth, min_samples_split, min_samples_leaf","sans limite de profondeur, overfit quasi garanti — cf. page Modélisation ▸ Arbres de décision"],
    ["Importance des features","tree.**feature_importances_**","-","basé sur la baisse de Gini apportée par chaque feature — utile pour une sélection de features (cf. Feature Selection)"],
  ]},
  {id:"ml-ensemble-syntax", group:"ml", subgroup:"Ensemble Methods", title:"Bagging, Boosting & Stacking", headers:["Nom","Syntaxe","Params","Explication"], colClasses:["col-nom","col-formule","col-params","col-explication"], rows:[
    ["RandomForestClassifier / Regressor","from sklearn.ensemble import RandomForestRegressor\nforest = **RandomForestRegressor**(n_estimators=100)","n_estimators : nombre d'arbres","bagging d'arbres de décision — cf. page Modélisation ▸ Ensemble Methods"],
    ["Bagger n'importe quel modèle","from sklearn.ensemble import BaggingClassifier\nbagged = **BaggingClassifier**(KNeighborsClassifier(), n_estimators=40)","-","BaggingRegressor/Classifier applique le bagging à n'importe quel estimator, pas seulement des arbres"],
    ["AdaBoostClassifier / Regressor","from sklearn.ensemble import AdaBoostRegressor\nada = **AdaBoostRegressor**(DecisionTreeRegressor(max_depth=3), n_estimators=50)","-","chaque arbre corrige les erreurs du précédent en repondérant les observations mal prédites"],
    ["GradientBoostingClassifier / Regressor","from sklearn.ensemble import GradientBoostingRegressor\ngb = **GradientBoostingRegressor**(n_estimators=100, learning_rate=0.1, max_depth=3)","learning_rate : poids de chaque arbre ajouté","chaque arbre prédit le résidu du précédent au lieu de repondérer — généralement plus performant qu'AdaBoost"],
    ["XGBoost","from xgboost import XGBRegressor\nxgb = **XGBRegressor**(max_depth=10, n_estimators=100, learning_rate=0.1, early_stopping_rounds=5)\nxgb.fit(X_train, y_train, eval_set=[(X_train,y_train),(X_val,y_val)])","-","librairie dédiée optimisée, nécessite un jeu de validation (eval_set) pour early_stopping_rounds"],
    ["VotingClassifier / Regressor","from sklearn.ensemble import VotingClassifier\nensemble = **VotingClassifier**(estimators=[(\"rf\",forest),(\"lr\",logreg)], voting=\"soft\")","voting : hard (vote majoritaire) / soft (moyenne des probas)","agrège simplement les prédictions de plusieurs modèles déjà entraînés"],
    ["StackingClassifier / Regressor","from sklearn.ensemble import StackingClassifier\nensemble = **StackingClassifier**(estimators=[...], final_estimator=LogisticRegression())","-","un modèle final apprend à combiner les prédictions des autres, au lieu d'un simple vote/moyenne"],
  ]},
  {id:"ts-splitting", group:"ml", subgroup:"Time Series", title:"Train/test split contigu & features autorégressives", rows:[
    ["Split contigu (pas de shuffle)","train_size = 0.6\nindex = round(train_size * df.shape[0])\ndf_train = df.**iloc**[:index]\ndf_test = df.**iloc**[index:]","JAMAIS train_test_split classique — data leakage temporel (cf. page Modélisation ▸ Modèles ▸ Time Series ▸ [Décomposition](#ts-decomposition))"],
    ["Créer des features autorégressives (lags)","for i in range(1, 13):\n    df[f't-{i}'] = df['value'].**shift**(i)\ndf.**dropna**(inplace=True)","1 colonne par lag ; dropna() retire les premières lignes incomplètes"],
    ["Moyenne mobile (lisser, isoler la tendance)","df['roll_12'] = df['value'].**rolling**(12).mean()","fenêtre = nb de points glissants","moyenne des N derniers points — lisse le bruit court terme, fait ressortir la tendance"],
    ["Moyenne mobile exponentielle","df['ewm_3'] = df['value'].**ewm**(halflife=3).mean()","halflife : le poids d'un point est divisé par 2 tous les halflife pas","pondère les points récents plus fort que rolling() (poids décroissant exponentiellement) — réagit plus vite à un changement récent"],
    ["Baseline TS (prédire la valeur précédente)","y_pred = df_test['value'].**shift**(1)","modèle le plus simple possible — point de comparaison minimal (cf. [Baseline Score](#ml-baseline), groupe ml)"],
    ["Encoder une variable cyclique (angle, heure, jour...)","radians = df['angle_deg'] * np.pi / 180\ndf['x'] = np.**cos**(radians)\ndf['y'] = np.**sin**(radians)","une valeur brute (359° proche de 0°, 23h proche de 0h) trompe un modèle qui ne voit qu'un nombre — sin/cos replace la valeur sur un cercle, où le début et la fin de la période sont bien voisins"],
  ]},
  {id:"ts-cleaning-syntax", group:"ml", subgroup:"Time Series", title:"Nettoyer une série — dates manquantes & fréquence", rows:[
    ["Combler les dates manquantes dans l'index","full_range = pd.**date_range**(df.index.min(), df.index.max(), freq=\"D\")\ndf = df.**reindex**(full_range)","freq : \"D\" (jour), \"ME\" (fin de mois), \"MS\" (début de mois)...","reindex() fait apparaître des NaN exactement aux dates absentes de l'index d'origine — combler ensuite avec interpolate()"],
    ["Combler les NaN par interpolation linéaire","df['col'] = df['col'].**interpolate**(\"linear\")","-","relie les points connus par une droite — mieux qu'une imputation par moyenne/médiane qui casserait la continuité temporelle"],
    ["Changer la fréquence — agréger (ex: jour → mois)","monthly = df.**resample**(\"ME\").mean()","\"ME\" (fin de mois) vs \"MS\" (début de mois) : change l'index résultant — vérifier lequel est attendu en aval","peut laisser un NaN résiduel sur la dernière période (incomplète) — interpolate() après resample() si besoin"],
  ]},
  {id:"ts-decomposition-syntax", group:"ml", subgroup:"Time Series", title:"Décomposition & stationnarité", rows:[
    ["Décomposer (additive / multiplicative)","from statsmodels.tsa.seasonal import seasonal_decompose\nresult = **seasonal_decompose**(df['value'], model='additive')","result.trend / .seasonal / .resid — cf. page Modélisation ▸ [Décomposition](#ts-decomposition)"],
    ["Visualiser la décomposition","result.**plot**()","4 sous-graphiques : série, trend, seasonal, résidus"],
    ["Test de stationnarité (ADF)","from statsmodels.tsa.stattools import adfuller\np_value = **adfuller**(df.value)[1]","p < 0.05 → série stationnaire (cf. page Modélisation ▸ Stationnarité)"],
    ["Différencier (rendre stationnaire)","df.value.**diff**(1)   # ordre 1\ndf.value.diff().diff()  # ordre 2","chaque diff() supplémentaire = un ordre de differencing en plus"],
    ["Estimer automatiquement l'ordre de differencing","from pmdarima.arima.utils import ndiffs\n**ndiffs**(df['value'])","renvoie directement le d optimal, plutôt que de tester diff() par diff()"],
  ]},
  {id:"ts-acf-pacf-syntax", group:"ml", subgroup:"Time Series", title:"ACF & PACF", rows:[
    ["Tracer l'ACF","from statsmodels.graphics.tsaplots import plot_acf\n**plot_acf**(df.value, lags=50)","cône bleu = intervalle de confiance ; cutoff donne l'ordre q d'un MA (cf. page Modélisation)"],
    ["Tracer la PACF","from statsmodels.graphics.tsaplots import plot_pacf\n**plot_pacf**(df.value, lags=50, c='r')","cutoff donne l'ordre p d'un AR (cf. page Modélisation)"],
  ]},
  {id:"ts-arima-syntax", group:"ml", subgroup:"Time Series", title:"ARIMA & SARIMA", rows:[
    ["Fitter un ARIMA(p,d,q)","from statsmodels.tsa.arima.model import ARIMA\narima = **ARIMA**(df['value'], order=(2,1,1), trend='t')\narima = arima.fit()","order=(p,d,q) ; trend='t' ajoute une tendance linéaire"],
    ["Chercher (p,d,q) automatiquement","import pmdarima as pm\nsmodel = pm.**auto_arima**(df['value'], start_p=1, max_p=2, start_q=1, max_q=2, seasonal=False, trace=True)","grid search sur l'AIC — trace=True affiche chaque combinaison testée"],
    ["Prédire avec pmdarima (API différente de statsmodels)","preds, conf_int = smodel.**predict**(n_periods=len(test), return_conf_int=True)","n_periods (pas steps), return_conf_int=True (pas alpha=)","le modèle retourné par auto_arima a sa PROPRE API .predict() — ne pas confondre avec .get_forecast() de statsmodels (ligne au-dessus), noms de paramètres différents"],
    ["Résumé du modèle (AIC, coefficients)","arima.**summary**()","coefficients + p-values + AIC/BIC pour comparer plusieurs modèles"],
    ["Prédire avec intervalle de confiance","results = arima.**get_forecast**(len(test), alpha=0.05)\nforecast = results.**predicted_mean**\nconf_int = results.**conf_int**()","get_forecast expose la moyenne prédite ET l'intervalle de confiance ; forecast() seul ne renvoie que la moyenne"],
    ["SARIMA (saisonnalité intégrée)","from statsmodels.tsa.statespace.sarimax import SARIMAX\nsarima = **SARIMAX**(train, order=(0,1,1), seasonal_order=(2,0,2,12))\nsarima = sarima.fit()","seasonal_order=(P,D,Q,S) — S = période saisonnière (ex: 12 pour une saisonnalité annuelle mensuelle)"],
    ["SARIMAX avec variables exogènes","sarimax = SARIMAX(y_train, **exog**=X_train[exog_cols], order=(1,0,1), seasonal_order=(1,0,0,24))\nres = sarimax.fit()\nres.**get_forecast**(steps=48, exog=X_test[exog_cols])","exog : DataFrame de features externes (ex: météo, prix) — mêmes lignes/index que y","le X, en plus du passé de y — exog doit être fourni à la fois au fit() ET au get_forecast() (les vraies valeurs futures si connues, ex: prévisions météo)"],
    ["Chercher (p,d,q)(P,D,Q)[S] automatiquement","pm.auto_arima(train, seasonal=True, **m**=12, start_p=0, max_p=1, start_P=0, max_P=2, trace=True)","m = période saisonnière ; grid search combinée sur les deux jeux d'hyperparamètres"],
    ["Cross-valider un ARIMA (boucle manuelle)","from sklearn.model_selection import TimeSeriesSplit\ntscv = TimeSeriesSplit(n_splits=3)\nfor train_idx, test_idx in tscv.split(y):\n    m = ARIMA(y.iloc[train_idx], order=(1,1,1)).**fit**()\n    ...","-","ARIMA (statsmodels) n'a pas l'API sklearn compatible avec cross_validate — même raison/pattern que la CV manuelle en Deep Learning (cf. groupe dl)"],
  ]},
  {id:"ts-prophet-syntax", group:"ml", subgroup:"Time Series", title:"Facebook Prophet — alternative à ARIMA", rows:[
    ["Préparer les données au format imposé","df.columns = ['**ds**', '**y**']  # ds = date, y = valeur à prédire\ndf['ds'] = pd.to_datetime(df['ds'])","-","Prophet impose ces deux noms de colonnes exacts, quel que soit le dataset d'origine"],
    ["Entraîner","from prophet import Prophet\nmodel = **Prophet**()\nmodel.fit(train)","-","gère nativement tendance + saisonnalité(s) + jours fériés, sans passer par ACF/PACF/differencing comme ARIMA"],
    ["Générer le futur à prédire","future = model.**make_future_dataframe**(periods=24, freq='MS')\nforecast = model.**predict**(future)","periods : horizon ; freq : 'MS' (mois), 'D' (jour)...","future contient aussi les dates du train — forecast['yhat'] = prédiction, avec intervalle d'incertitude"],
    ["Visualiser prédiction / composantes","model.**plot**(forecast)\nmodel.**plot_components**(forecast)","-","plot() = série + prédiction + intervalle ; plot_components() = décompose en tendance + saisonnalité(s)"],
    ["Cross-validation spécifique aux séries temporelles","from prophet.diagnostics import cross_validation, performance_metrics\ndf_cv = **cross_validation**(model, initial='1825 days', period='180 days', horizon='365 days')\nperformance_metrics(df_cv)","initial : taille de la 1ère fenêtre d'entraînement ; period : décalage entre folds ; horizon : durée de prédiction","fenêtres d'entraînement glissantes (jamais de shuffle) — renvoie MAPE/RMSE... par horizon de prédiction"],
  ]},
  {id:"ts-darts-syntax", group:"ml", subgroup:"Time Series", title:"darts — API unifiée multi-modèles", rows:[
    ["Créer une TimeSeries depuis un DataFrame","from darts import TimeSeries\nseries = TimeSeries.**from_dataframe**(df, 'date_col', 'value_col')","-","objet natif darts — resample()/diff()/fill_missing_values() intégrés, sans repasser par pandas à chaque étape"],
    ["N'importe quel modèle : même API .fit()/.predict()","from darts.models import NaiveSeasonal, ExponentialSmoothing, AutoARIMA, Prophet, AutoTBATS\nmodel = **AutoARIMA**()\nmodel.fit(train)\npreds = model.predict(len(val))","-","tous les modèles (naïf, ARIMA, Prophet, TBATS...) partagent la même interface — permet de boucler sur plusieurs modèles pour comparer sans réécrire le code à chaque fois"],
    ["Évaluer avec une métrique TS dédiée","from darts.metrics import mape\n**mape**(val, preds)","-","équivalent MAPE prêt à l'emploi, pas besoin de le recalculer à la main"],
    ["Ajouter une covariable connue à l'avance (ex: météo)","model.fit(train, **future_covariates**=weather_ts)\nmodel.predict(len(val), future_covariates=weather_ts)","-","même principe que exog= pour SARIMAX (cf. ci-dessus) — covariables PASSÉES (connues seulement dans le passé) vs FUTURES (ex: prévisions météo) vs STATIQUES (constantes, ex: ID produit)"],
  ]},
  {id:"nlp-cleaning-syntax", group:"ml", subgroup:"NLP", title:"Cleaning — Python core + RegEx", rows:[
    ["Retirer les espaces en début/fin","text.**strip**()","-"],
    ["Remplacer un mot","text.**replace**(\"koala\", \"panda\")","-"],
    ["Découper une chaîne","text.**split**(\"/\")","liste de sous-chaînes"],
    ["Mettre en minuscules","text.**lower**()","obligatoire avant tokenizing/vectorizing — sinon \"Football\" ≠ \"football\""],
    ["Retirer les chiffres","''.join(c for c in text if not c.**isdigit**())","-"],
    ["Retirer ponctuation et symboles","import string\nfor p in string.punctuation:\n    text = text.**replace**(p, '')","string.punctuation = '!\"#$%&\\'()*+,-./:;<=>?@[\\\\]^_`{|}~'"],
    ["Retirer les accents","from unidecode import unidecode\ntext = **unidecode**(text)  # pip install unidecode","-","indispensable AVANT un regex `[^a-z]` sur du texte accentué (ex: français/portugais) — sinon 'ã'/'é' sont juste supprimés (\"não\" → \"n o\") au lieu de devenir 'a'/'e', ce qui casse la détection de mots"],
    ["Extraire un pattern (ex: e-mails) par RegEx","import re\n**re.findall**(r'[\\w.+-]+@[\\w-]+\\.[\\w.-]+', text)","liste des correspondances trouvées"],
  ]},
  {id:"nlp-nltk-syntax", group:"ml", subgroup:"NLP", title:"NLTK — tokenizing, stopwords, lemmatizing", rows:[
    ["Tokenizer un texte","from nltk.tokenize import word_tokenize\n**word_tokenize**(text)","liste de tokens (mots)"],
    ["Charger les stopwords","from nltk.corpus import stopwords\nstop_words = set(**stopwords.words**('english'))","set de mots à filtrer — attention en sentiment analysis (cf. page Modélisation)"],
    ["Retirer les stopwords d'une liste de tokens","[w for w in tokens if not w in stop_words]","-"],
    ["Lemmatiser (verbes / noms)","from nltk.stem import WordNetLemmatizer\n**WordNetLemmatizer**().lemmatize(word, pos='v')  # verbes\nWordNetLemmatizer().lemmatize(word, pos='n')  # noms","pos= précise la nature grammaticale (cf. page Modélisation) — 'n' (nom) par défaut si omis"],
    ["Détecter automatiquement le pos= de chaque mot","from nltk import pos_tag\nfrom nltk.corpus import wordnet\ntag = pos_tag([word])[0][1][0].upper()  # ex: 'V'\npos_map = {\"J\": wordnet.ADJ, \"N\": wordnet.NOUN, \"V\": wordnet.VERB, \"R\": wordnet.ADV}\nlemmatizer.lemmatize(word, pos=pos_map.get(tag, wordnet.NOUN))","-","évite de lemmatiser tout un texte avec un seul pos= fixe — chaque mot garde sa vraie nature grammaticale (pos_tag renvoie un tag détaillé type 'VBG', on ne garde que sa 1ère lettre)"],
  ]},
  {id:"nlp-vectorizer-syntax", group:"ml", subgroup:"NLP", title:"CountVectorizer & TfidfVectorizer", headers:["Nom","Syntaxe","Params","Explication"], colClasses:["col-nom","col-formule","col-params","col-explication"], rows:[
    ["Bag-of-Words","from sklearn.feature_extraction.text import CountVectorizer\nX = **CountVectorizer**().fit_transform(texts)","-","matrice sparse de comptages de mots — cf. page Modélisation ▸ Vectorizing"],
    ["Tf-idf","from sklearn.feature_extraction.text import TfidfVectorizer\nX = **TfidfVectorizer**().fit_transform(texts)","-","pondère par l'importance du mot dans le corpus (cf. page Modélisation)"],
    ["Récupérer le vocabulaire appris","vectorizer.**get_feature_names_out**()","-","array des mots correspondant à chaque colonne de X"],
    ["Filtrer les mots trop rares / trop fréquents","CountVectorizer(**max_df**=0.8, **min_df**=2)","max_df/min_df : float [0,1] (proportion) ou int (compte absolu)","construit des \"stopwords\" spécifiques au corpus"],
    ["Limiter le vocabulaire aux k mots les plus fréquents","CountVectorizer(**max_features**=1000)","max_features : int","lutte contre la curse of dimensionality (cf. Feature Selection, groupe ml)"],
    ["Capturer le contexte (bi/tri-grammes)","CountVectorizer(**ngram_range**=(1,2))","ngram_range=(min_n,max_n)","(1,1)=unigrams seuls (défaut) ; (1,2)=unigrams+bigrams ; (2,3)=bigrams+trigrams sans unigrams"],
  ]},
  {id:"nlp-naive-bayes-syntax", group:"ml", subgroup:"NLP", title:"Naive Bayes — classification de texte", rows:[
    ["Pipeline vectorizer + Naive Bayes","from sklearn.naive_bayes import MultinomialNB\npipeline = make_pipeline(TfidfVectorizer(), **MultinomialNB**())","cf. page Modélisation ▸ Naive Bayes pour la formule et l'hypothèse d'indépendance"],
    ["Lisser (éviter les probabilités nulles)","MultinomialNB(**alpha**=0.1)","alpha = paramètre de smoothing (cf. page Modélisation)"],
    ["Tuner vectorizer + modèle simultanément","grid = {\n    'tfidfvectorizer__ngram_range': ((1,1),(2,2)),\n    'multinomialnb__alpha': (0.1,1)\n}\n**GridSearchCV**(pipeline, grid, scoring='recall', cv=5)","syntaxe `étape__param`, comme n'importe quel pipeline (cf. Model Tuning, groupe ml)"],
  ]},
  {id:"nlp-lda-syntax", group:"ml", subgroup:"NLP", title:"LDA — Topic Modeling", rows:[
    ["Instancier et fitter","from sklearn.decomposition import LatentDirichletAllocation\nlda = **LatentDirichletAllocation**(n_components=2, max_iter=100)\nlda.fit(X)","X = matrice document-terme déjà vectorisée (CountVectorizer/TfidfVectorizer)"],
    ["Mixture de topics par document","lda.**transform**(X)","matrice (n_documents × n_topics) — poids de chaque topic dans chaque document"],
    ["Mixture de mots par topic","lda.**components_**","matrice (n_topics × n_mots) — poids de chaque mot dans chaque topic"],
  ]},
  {id:"plot-base", group:"plot", title:"Matplotlib — graphiques de base", rows:[
    ["Importer matplotlib","import **matplotlib.pyplot** as plt","-"],
    ["Diagramme en barres","plt.**bar**(x, y)","graphique à barres"],
    ["Nuage de points","plt.**scatter**(x=x, y=y, label=\"...\", s=8)","nuage de points, s = taille des points"],
    ["Courbe / ligne","plt.**plot**(x, y, label=\"...\", color=\"red\")","courbe reliant les points"],
    ["Ligne horizontale constante (ex: valeur théorique)","plt.**plot**([valeur] * len(y), label=\"...\", color=\"red\")","ligne droite horizontale à hauteur 'valeur'"],
    ["Afficher le graphique","plt.**show**()","affiche la figure à l'écran"],
    ["Tracer directement depuis une Series/DataFrame (sans passer par plt.bar/scatter)","serie.**plot**(kind=\"barh\")","graphique en barres horizontales — pratique pour trier et visualiser des coefficients de régression (model.params.sort_values().plot(kind=\"barh\")) ou toute autre Series"],
  ]},
  {id:"plot-mise-en-forme", group:"plot", title:"Matplotlib — mise en forme", rows:[
    ["Définir la taille de la figure","plt.**figure**(figsize=(20, 10))","figure plus grande (largeur, hauteur en pouces)"],
    ["Titre du graphique","plt.**title**(\"mon titre\")","-"],
    ["Titre au-dessus de toute la figure (utile avec plt.subplot)","plt.**suptitle**(\"mon titre global\")","-"],
    ["Titres des axes","plt.**xlabel**(\"x\")\nplt.**ylabel**(\"y\")","-"],
    ["Limiter un axe pour masquer des outliers (un seul côté possible)","plt.**xlim**(right=70)\nplt.**ylim**(bottom=0)","tronque la vue sans modifier les données — right=/left=/top=/bottom= bornent un seul côté, l'autre reste automatique"],
    ["Afficher la légende (labels définis dans bar/scatter/plot)","plt.**legend**(loc=\"best\")","légende positionnée automatiquement"],
  ]},
  {id:"plot-quadrillage", group:"plot", title:"Matplotlib — quadrillage de sous-graphiques", rows:[
    ["Grille de sous-graphiques (lignes, colonnes, position)","plt.**subplot**(2, 2, 1)","sélectionne le 1er graphique d'une grille 2x2 — les plt.plot suivants s'y appliquent"],
    ["Créer une grille de sous-graphiques indépendants (objets Axes)","fig, axes = plt.**subplots**(nrows=2, ncols=3, figsize=(15, 10))\n# cas le plus courant, 2 côte à côte :\nfig, (ax1, ax2) = plt.**subplots**(1, 2, figsize=(12, 5))\nax1.bar(x, y)\nax2.scatter(x, y)","fig = la figure entière, axes = tableau 2D d'objets Axes (ou directement (ax1, ax2) si on dépaquette 1x2)\nfigsize=(largeur, hauteur) en pouces — ex: (12, 5) = plus large que haut, adapté à 2 graphiques côte à côte"],
    ["Parcourir tous les axes d'une grille","for ax in axes.**flat**:\n    ...","aplatit le tableau 2D d'axes en liste 1D pour boucler dessus"],
    ["Configurer un axe précis (titre, limites)","ax.**set_title**(f\"n={n}\")\nax.**set_xlim**(0, 40)","équivalent de plt.title / plt.xlim mais appliqué à un axe particulier de la grille"],
    ["Deuxième axe Y (échelles très différentes sur le même graphe)","fig, ax1 = plt.subplots(figsize=(10,5))\nax1.plot(x, commandes, color=\"tab:blue\")\nax2 = ax1.**twinx**()\nax2.plot(x, paiements, color=\"tab:orange\")\n# fusionner les 2 légendes\nl1, la1 = ax1.get_legend_handles_labels()\nl2, la2 = ax2.get_legend_handles_labels()\nax1.legend(l1+l2, la1+la2)","-","twinx() crée un 2ᵉ axe Y partageant le même axe X — indispensable quand deux séries ont des échelles trop différentes pour partager un axe (sinon l'une s'écrase en ligne plate) ; chaque axe garde sa propre légende, à fusionner manuellement pour n'en afficher qu'une"],
  ]},
  {id:"sns-usage", group:"plot", title:"Seaborn — jeux de données & distributions", rows:[
    ["Importer seaborn","import **seaborn** as sns","-"],
    ["Charger un dataset d'exemple intégré","tips_df = sns.**load_dataset**(\"tips\")","DataFrame (ex: dataset 'tips')"],
    ["Histogramme rapide, sans import supplémentaire","df.**hist**(\"col\", bins=20)","affiche directement un histogramme matplotlib — pratique en exploration rapide, mais rendu moins soigné que sns.histplot ci-dessous"],
    ["Histogramme (rendu plus soigné que plt.hist / df.hist)","sns.**histplot**(data, bins=20)","histogramme affiché directement"],
    ["Histogramme sur un axe précis, avec courbe de densité (KDE)","sns.histplot(data, bins=10, ax=ax, **kde=True**)","ax= dessine dans un sous-graphique particulier (utile avec plt.subplots), kde=True superpose une courbe de densité lissée"],
    ["Histogramme en densité, avec un pas discret (ex: notes entières, variable 0/1)","sns.**histplot**(df[\"col\"], kde=False, stat=\"density\", discrete=True)","stat=\"density\" normalise l'aire totale à 1 (comparable à une pdf, cf. page Maths) ; discrete=True centre une barre par valeur entière plutôt que des bins continus"],
    ["Superposer deux distributions pour les comparer (ex: valeurs réelles vs. prédites)","sns.**histplot**(y_true, label=\"actual\", kde=True)\nsns.**histplot**(y_pred, label=\"predicted\", kde=True)\nplt.legend()","deux histogrammes sur le même axe, avec légende — utile pour juger visuellement la qualité d'une régression"],
    ["Histogramme cumulatif (fonction de répartition empirique)","sns.**histplot**(df[\"col\"], binwidth=5, **cumulative=True**, stat=\"percent\")","cumulative=True cumule les effectifs bin par bin, stat=\"percent\" affiche des % plutôt que des comptes — version empirique/graphique de la cdf théorique (cf. Probabilité à partir du z-score, page Maths)"],
    ["Distributions côte à côte, une par catégorie","g = sns.**FacetGrid**(df, hue=\"dimension\", col=\"dimension\")\ng.map(sns.kdeplot, \"variable\")","une grille de courbes de densité (kde), une par valeur de dimension — pattern utilisé dans olist/utils.py::plot_kde_plot"],
    ["Compter les occurrences par catégorie","sns.**countplot**(data=df, x=\"categorie\", hue=\"autre_categorie\", stat=\"percent\")","diagramme en barres du nombre de lignes par modalité — hue= ajoute une sous-répartition par couleur, stat=\"percent\" affiche des % plutôt que des comptes bruts"],
  ]},
  {id:"sns-comparaison-cat", group:"plot", title:"Seaborn — comparer une distribution par catégorie", rows:[
    ["Nuage de points coloré par catégorie","sns.**scatterplot**(data=df, x=\"col1\", y=\"col2\", **hue**=\"categorie\")","un point par ligne, coloré selon la modalité de categorie — syntaxe de base à privilégier sur plt.scatter dès qu'il y a une dimension catégorielle à distinguer"],
    ["Boîte à moustaches (médiane, quartiles, outliers) par catégorie","sns.**boxplot**(data=df, x=\"categorie\", y=\"valeur\", hue=\"autre_categorie\")","une boîte par modalité de categorie — les moustaches représentent $[Q_1-1.5\\,IQR,\\ Q_3+1.5\\,IQR]$, les points au-delà sont les outliers (cf. Écart interquartile, page Maths)"],
    ["Violon (boxplot + densité KDE combinés)","sns.**violinplot**(data=df, x=\"categorie\", y=\"valeur\")","même info qu'un boxplot mais avec la forme de la distribution en plus (silhouette symétrique = densité kde de chaque côté)"],
    ["Nuage de points par catégorie (alternative au boxplot pour peu de données)","sns.**stripplot**(data=df, x=\"categorie\", y=\"valeur\")","affiche chaque observation individuelle plutôt qu'un résumé statistique — utile quand il y a peu de points, où un boxplot serait trompeur"],
  ]},
  {id:"sns-reg-corr", group:"plot", title:"Seaborn — régression et corrélation", rows:[
    ["Nuage de points + droite de régression + intervalle de confiance","sns.**regplot**(x=\"col1\", y=\"col2\", data=df, y_jitter=0.3, ci=95)","scatter + droite de régression linéaire, avec un cône ombré = intervalle de confiance (95% par défaut) — y_jitter ajoute du bruit vertical pour mieux voir la densité de points quand y est discret"],
    ["Matrice de corrélation entre toutes les colonnes numériques","df.**corr**(numeric_only=True)","DataFrame carré (n_cols x n_cols) des coefficients de corrélation de Pearson entre chaque paire de colonnes (cf. Corrélation de Pearson, page Maths)"],
    ["Visualiser la matrice de corrélation","sns.**heatmap**(df.corr(numeric_only=True), cmap=\"coolwarm\", annot=True)","grille colorée : rouge = corrélation positive forte, bleu = corrélation négative forte ; annot=True affiche les valeurs numériques dans chaque case"],
  ]},
  {id:"plotly-scatter", group:"plot", title:"Plotly Express — scatter interactif", rows:[
    ["Importer plotly express","import **plotly.express** as px","-"],
    ["Scatter interactif : position, taille et couleur encodent 4 variables à la fois","fig = px.**scatter**(data_frame=df, x=\"col1\", y=\"col2\", size=\"col3\", color=\"col4\", size_max=60, opacity=0.5)\nfig.**show**()","graphique interactif (zoom, survol pour voir les valeurs) — utile en exploration pour repérer des points ou groupes atypiques sans faire un scatter par paire de variables"],
    ["Courbe interactive (évolution dans le temps)","fig = px.**line**(df, x=\"annee\", y=\"valeur\", color=\"pays\")\nfig.show()","graphique le plus utilisé pour visualiser une série temporelle par catégorie (une courbe par valeur de color) — équivalent interactif de plt.plot / sns.lineplot"],
    ["Scatter enrichi : marges + droite de régression + label au survol","px.scatter(df, x=\"col1\", y=\"col2\", **marginal_x**=\"histogram\", **marginal_y**=\"box\", **trendline**=\"ols\", **hover_name**=\"pays\")","marginal_x/marginal_y ajoutent un histogramme/boxplot en marge de chaque axe, trendline=\"ols\" superpose une droite de régression, hover_name affiche un libellé personnalisé au survol plutôt que juste (x,y)"],
    ["Boxplot interactif par catégorie","fig = px.**box**(df, x=\"categorie\", y=\"valeur\", category_orders={\"categorie\":[\"A\",\"B\",\"C\"]})\nfig.show()","category_orders : force l'ordre des catégories sur l'axe X (sinon ordre alphabétique/premier vu)","équivalent interactif de sns.boxplot — survol affiche médiane/quartiles/outliers exacts de chaque boîte"],
    ["Histogramme interactif + ligne de référence","fig = px.**histogram**(df, x=\"valeur\", nbins=60)\nfig.**add_vline**(x=0, line_dash=\"dash\", line_color=\"red\")\nfig.show()","add_vline : trace une ligne verticale (ex: seuil à 0) par-dessus le graphique — utile pour repérer visuellement combien d'observations passent sous/au-dessus d'un seuil"],
  ]},
  {id:"dash-app", group:"dash", title:"Dash — structure d'une app minimale", rows:[
    ["Installer Dash","**pip install dash dash-bootstrap-components**","-"],
    ["Structure minimale d'une app",[
      {code:"from dash import Dash, html, dcc\n\napp = **Dash**(__name__)\napp.**layout** = [html.H1(\"Titre\"), dcc.Graph(figure=fig)]\n\nif __name__ == \"__main__\":\n    app.**run**(debug=True)"}
    ],"lance un serveur local sur http://127.0.0.1:8050/ — debug=True recharge l'app automatiquement à chaque modification du fichier"],
    ["Deux familles de composants","dcc.**Graph**(...), dcc.**Dropdown**(...), dcc.**Slider**(...)   # Dash Core Components\nhtml.**H1**(...), html.**Div**(...), html.**P**(...)            # wrappers HTML","dcc = composants interactifs (graphiques, filtres) ; html = équivalents des balises HTML classiques — ensemble ils composent app.layout"],
    ["Appliquer un thème Bootstrap","import dash_bootstrap_components as dbc\napp = Dash(external_stylesheets=[dbc.**themes**.BOOTSTRAP])","applique un thème CSS prêt à l'emploi à toute l'app"],
    ["Grille responsive (12 colonnes)","dbc.**Row**([dbc.**Col**(comp1, width=8), dbc.**Col**(comp2, width=4)])","dispose des composants côte à côte, largeur en douzièmes de la ligne (8+4=12)"],
  ]},
  {id:"dash-callback", group:"dash", title:"Dash — interactivité (callbacks) & multi-pages", rows:[
    ["Callback : relier un composant d'entrée à une sortie",[
      {code:"**@callback**(\n    Output(\"mon_graph\", \"figure\"),\n    Input(\"mon_dropdown\", \"value\")\n)\ndef update(valeur):\n    filtered = df[df[\"country\"] == valeur]\n    return px.line(filtered, x=\"year\", y=\"co2\", title=f\"CO2 — {valeur}\")"}
    ],"la fonction décorée se réexécute automatiquement à chaque changement de la valeur du composant Input, et met à jour le composant Output (ici la figure du graphique)"],
    ["Identifier les composants à relier","dcc.Dropdown(**id=\"mon_dropdown\"**, options=[...])\ndcc.Graph(**id=\"mon_graph\"**)","id= sert de clé pour cibler le composant depuis Output()/Input() dans un callback"],
    ["Construire dynamiquement les options d'un Dropdown","dcc.**Dropdown**(\n    id=\"mon_dropdown\",\n    options=[{\"label\": c, \"value\": c} for c in df[\"country\"].unique()],\n    **value**=\"France\"\n)","options : liste de dicts {\"label\": texte affiché, \"value\": valeur envoyée au callback}\nvalue= : valeur sélectionnée par défaut au chargement de l'app","pattern courant pour générer les choix à partir des valeurs réellement présentes dans le DataFrame, plutôt que de les écrire à la main"],
    ["Structurer une app multi-pages","dossier pages/ma_page.py :\ndash.**register_page**(__name__)\nlayout = [...]  # pas de app.layout ici\n\napp = Dash(use_pages=True)\napp.layout = [dash.**page_container**]","chaque fichier de pages/ devient une route automatiquement ; page_container affiche la page active dans le layout principal"],
    ["Menu de navigation entre les pages","[dcc.**Link**(page[\"name\"], href=page[\"relative_path\"]) for page in dash.**page_registry**.values()]","page_registry : dict rempli automatiquement par register_page() pour chaque page — dcc.Link crée un lien de navigation sans recharger toute l'app (contrairement à un <a> classique)"],
  ]},
  {id:"dl-sequential-syntax", group:"dl", subgroup:"Keras", title:"Définir l'architecture — Sequential", rows:[
    ["Créer un modèle séquentiel","from keras import Sequential, layers\nmodel = **Sequential**()","empile les couches ajoutées ensuite dans l'ordre — cf. page Modélisation ▸ [Neurone, Layer, Réseau](#dl-neuron-network)"],
    ["Ajouter une couche Dense (fully connected)","model.**add**(layers.Dense(10, activation='relu'))","10 neurones, activation ReLU — cf. page Modélisation ▸ [Fonctions d'activation](#dl-activation)"],
    ["Préciser la forme des inputs","from keras import Input\nmodel.add(**Input**(shape=(4,)))","shape = tuple ; permet à Keras d'allouer le modèle immédiatement plutôt que d'inférer au 1er batch"],
    ["Dernière couche — régression (1 ou k valeurs)","model.add(layers.Dense(1, activation='**linear**'))","1 pour une seule valeur prédite, k pour un vecteur de k valeurs"],
    ["Dernière couche — classification binaire","model.add(layers.Dense(1, activation='**sigmoid**'))","1 neurone, sortie interprétable comme une probabilité"],
    ["Dernière couche — classification multi-classe","model.add(layers.Dense(k, activation='**softmax**'))","k = nombre de classes, sortie = probabilités qui somment à 1"],
    ["Préparer la target pour du softmax (One-Hot)","from tensorflow.keras.utils import to_categorical\ny_cat = **to_categorical**(y)","transforme un vecteur d'entiers (0,1,2...) en matrice One-Hot (n, k) — nécessaire pour loss='categorical_crossentropy' ; alternative : garder y tel quel avec loss='sparse_categorical_crossentropy'"],
    ["Résumé du modèle (couches, nombre de params)","model.**summary**()","affiche chaque couche, sa output shape, son nombre de paramètres entraînables"],
  ]},
  {id:"dl-functional-api-syntax", group:"dl", subgroup:"Keras", title:"Architectures non-linéaires — Functional API", rows:[
    ["Définir les entrées","from keras import Input\ninputs = **Input**(shape=(10,))","point de départ obligatoire de la Functional API — pas de Sequential() ici"],
    ["Appeler les couches comme des fonctions","x = layers.Dense(50, activation='relu')(inputs)\noutputs = layers.Dense(1, activation='sigmoid')(x)","chaque couche prend le tensor précédent en argument, au lieu d'un .add() séquentiel — permet de brancher plusieurs sorties sur un même tensor (architectures non-linéaires)"],
    ["Assembler le modèle","from keras import Model\nmodel = **Model**(inputs=inputs, outputs=outputs)","model se compile/entraîne ensuite exactement comme un Sequential (.compile(), .fit()...)"],
    ["Exposer la sortie de couches intermédiaires","sub_model = Model(inputs=model.inputs, outputs=[l.output for l in model.layers])","utile pour inspecter/visualiser ce qu'une couche a appris (ex: feature maps d'un CNN), sans ré-entraîner"],
    ["Modèle à PLUSIEURS entrées — fusionner deux branches","inputs_a = Input(shape=(60,))       # ex: branche texte\ninputs_b = Input(shape=(18,))       # ex: branche tabulaire\n...\ncombined = layers.**concatenate**([outputs_a, outputs_b])\nx = layers.Dense(10, activation='relu')(combined)\noutputs = layers.Dense(1, activation='linear')(x)\nmodel = Model(inputs=[inputs_a, inputs_b], outputs=outputs)","chaque branche a son propre Input et sa propre suite de couches ; concatenate() les fusionne en un seul tensor juste avant la tête commune"],
    ["Entraîner un modèle à plusieurs entrées","model.fit(x=[X_a, X_b], y=y, validation_split=0.3, epochs=50)","x = LISTE d'arrays (un par Input, dans le même ordre que inputs=[...] à la création du modèle), pas un seul array"],
    ["Visualiser l'architecture (utile si multi-branches)","tf.keras.utils.**plot_model**(model, \"model.png\", show_shapes=True)","alternative à model.summary() — un schéma est souvent plus lisible qu'un texte pour un modèle à plusieurs entrées/branches"],
  ]},
  {id:"dl-compile-fit-syntax", group:"dl", subgroup:"Keras", title:"Compiler & entraîner", rows:[
    ["Compiler (loss + optimizer)","model.**compile**(loss='mse', optimizer='adam', metrics=['accuracy'])","loss='binary_crossentropy' en classification binaire — cf. page Modélisation ▸ Entraînement"],
    ["Compiler — régression sur target positive étalée","model.compile(loss='**msle**', optimizer='adam')","Mean Squared Log Error — cf. page Modélisation ▸ Loss Functions — régression (groupe ml) ; dernière couche 'relu', pas 'linear'"],
    ["Personnaliser l'optimizer (learning rate...)","from keras.optimizers import Adam, SGD\nmodel.compile(optimizer=**Adam**(learning_rate=0.01), loss='mse')","instancier l'optimizer plutôt qu'un string ('adam') permet de régler learning_rate et les autres hyperparamètres — cf. page Modélisation ▸ Choisir un optimizer"],
    ["Learning rate qui décroît automatiquement (scheduler)","from keras.optimizers.schedules import ExponentialDecay\nlr_schedule = **ExponentialDecay**(initial_learning_rate=0.1, decay_steps=200, decay_rate=0.5)\nmodel.compile(optimizer=Adam(learning_rate=lr_schedule), loss='mse')","decay_steps = nb d'iterations (mises à jour, pas epochs) avant chaque décroissance ; decay_rate = facteur multiplicatif appliqué"],
    ["Compiler quand la dernière couche n'a PAS de softmax","loss = tf.keras.losses.SparseCategoricalCrossentropy(**from_logits**=True)\nmodel.compile(optimizer='adam', loss=loss)","si la couche de sortie renvoie des logits bruts (pas d'activation), la loss doit le savoir (from_logits=True) pour appliquer elle-même le softmax en interne — sinon elle traite des logits comme des probabilités déjà normalisées et la loss est FAUSSÉE dès le départ"],
    ["Entraîner","model.**fit**(X_train, y_train, batch_size=32, epochs=10)","batch_size = taille des sous-ensembles utilisés à chaque update ; epochs = nb de passages complets sur le dataset"],
    ["Évaluer sur le test set","model.**evaluate**(X_test, y_test)","renvoie [loss, metrics] — équivalent de .score() côté Sklearn"],
    ["Prédire","model.**predict**(X_new)","tableau de sorties (probabilités si sigmoid/softmax en dernière couche)"],
  ]},
  {id:"dl-history-syntax", group:"dl", subgroup:"Keras", title:"Historique d'entraînement — History", rows:[
    ["Récupérer l'historique d'entraînement","history = model.**fit**(X_train, y_train, epochs=100)","le retour de .fit() est un objet History — history.history est un dict {\"loss\": [...], \"accuracy\": [...]}, une valeur par epoch"],
    ["Tracer la courbe de loss","plt.plot(history.history['**loss**'])","clé toujours présente, quelle que soit la tâche"],
    ["Comparer train vs validation","model.fit(X_train, y_train, **validation_data**=(X_val, y_val), epochs=100)\nplt.plot(history.history['loss']); plt.plot(history.history['**val_loss**'])","les clés val_* n'existent QUE si validation_data (ou validation_split) est passé à .fit() — mêmes clés pour une métrique : val_accuracy, etc."],
  ]},
  {id:"dl-early-stopping-syntax", group:"dl", subgroup:"Keras", title:"Early Stopping (callback)", rows:[
    ["Créer le callback","from tensorflow.keras.callbacks import EarlyStopping\nes = **EarlyStopping**(patience=20, restore_best_weights=True)","patience = nb d'epochs tolérées sans amélioration de la loss de validation avant d'arrêter — cf. page Modélisation ▸ [Early Stopping & jeu de validation](#dl-early-stopping)"],
    ["Fournir un jeu de validation explicite","model.fit(X_train, y_train, validation_data=(X_val, y_val), callbacks=[es])","alternative : validation_split=0.3 (prend les 30% DERNIERS indices du train, AVANT le shuffle — pas de fuite de données)"],
    ["Entraîner avec beaucoup d'epochs","model.**fit**(X_train, y_train, epochs=1000, callbacks=[es])","epochs volontairement élevé : c'est l'EarlyStopping qui arrête réellement l'entraînement, pas ce nombre"],
    ["Surveiller une autre métrique que la loss","es = EarlyStopping(**monitor**='val_accuracy', patience=5, restore_best_weights=True)","monitor='val_loss' par défaut — utile quand l'accuracy (ou une autre métrique) est plus pertinente que la loss pour juger de l'arrêt"],
  ]},
  {id:"dl-callbacks-syntax", group:"dl", subgroup:"Keras", title:"Autres callbacks — ModelCheckpoint, ReduceLROnPlateau", rows:[
    ["Sauvegarder automatiquement le meilleur modèle","from keras.callbacks import ModelCheckpoint\nmc = **ModelCheckpoint**('best_model.keras', monitor='val_loss', save_best_only=True)","écrit sur disque à CHAQUE amélioration de val_loss pendant l'entraînement — utile pour ne rien perdre même si l'entraînement est interrompu"],
    ["Réduire le learning rate quand la loss stagne","from keras.callbacks import ReduceLROnPlateau\nrlr = **ReduceLROnPlateau**(monitor='val_loss', factor=0.1, patience=3)","si val_loss ne s'améliore plus pendant patience epochs, multiplie le learning rate par factor — alternative/complément à un scheduler fixe (cf. page Modélisation ▸ Hyperparamètres)"],
    ["Combiner plusieurs callbacks","model.fit(X_train, y_train, validation_data=(X_val, y_val), callbacks=[es, mc, rlr])","tous les callbacks passés dans la liste s'exécutent en parallèle, chacun à sa propre logique"],
    ["Suivre l'entraînement visuellement — TensorBoard","from keras.callbacks import TensorBoard\ntb = **TensorBoard**(log_dir='logs/fit', histogram_freq=1)\nmodel.fit(X_train, y_train, callbacks=[tb])\n# puis, dans le notebook :\n%load_ext tensorboard\n%tensorboard --logdir logs/fit","écrit les métriques à chaque epoch dans log_dir ; l'interface TensorBoard permet de suivre/comparer plusieurs entraînements en direct"],
  ]},
  {id:"dl-manual-cv-syntax", group:"dl", subgroup:"Keras", title:"Cross-Validation manuelle (K-Fold)", rows:[
    ["Découper les INDICES en K folds","from sklearn.model_selection import KFold\nkf = **KFold**(n_splits=5, shuffle=True, random_state=0)\nfor train_idx, val_idx in kf.split(X):\n    ...","contrairement à cross_val_score (Sklearn only), un modèle Keras se boucle à la main — cf. page Modélisation ▸ [Cross-Validation manuelle en Deep Learning](#dl-manual-cv)"],
    ["Boucle complète (1 fold)","X_tr, X_va = X[train_idx], X[val_idx]\ny_tr, y_va = y[train_idx], y[val_idx]\npreproc = create_preproc(X_tr)   # refait à zéro CHAQUE fold\nX_tr_p, X_va_p = preproc.fit_transform(X_tr, y_tr), preproc.transform(X_va)\nmodel = **initialize_model**(X_tr_p.shape[1])   # modèle NEUF chaque fold\nh = model.fit(X_tr_p, y_tr, validation_data=(X_va_p, y_va), epochs=150)","score du fold : min(h.history['val_loss']) ou sa dernière valeur, selon ce qu'on veut comparer"],
  ]},
  {id:"dl-regularization-syntax", group:"dl", subgroup:"Keras", title:"Régularisation — L1/L2 & Dropout", rows:[
    ["Régulariser les poids d'une couche (L1/L2)","from keras import regularizers\nreg = regularizers.**L2**(0.01)\nlayers.Dense(50, activation='relu', kernel_regularizer=reg)","kernel_regularizer = poids W, le choix par défaut ; regularizers.L1(...) / regularizers.l1_l2(l1=..., l2=...) existent aussi"],
    ["Régulariser les biais / la sortie d'une couche","layers.Dense(20, activation='relu', bias_regularizer=reg)\nlayers.Dense(10, activation='relu', activity_regularizer=reg)","bias_regularizer = biais b (rare) ; activity_regularizer = sortie f(W·X+b) (sparsité, ex: autoencoders)"],
    ["Ajouter du Dropout","model.add(layers.**Dropout**(rate=0.2))","rate = fraction de neurones \"tués\" à chaque itération d'entraînement — actif seulement pendant model.fit()"],
  ]},
  {id:"dl-preprocessing-syntax", group:"dl", subgroup:"Keras", title:"Preprocessing intégré au modèle — Normalization", rows:[
    ["Scaler en dehors du modèle (façon Sklearn)","from sklearn.preprocessing import StandardScaler\nscaler = StandardScaler().fit(X_train)\nX_train_scaled = scaler.transform(X_train)","fonctionne, mais il faut refaire cette étape à chaque prédiction — le scaler vit en dehors du modèle sauvegardé"],
    ["Intégrer le scaling dans l'architecture","from tensorflow.keras.layers import Normalization\nnorm = **Normalization**()\nnorm.adapt(X_train)\nmodel.add(norm)","norm.adapt() calcule moyenne/écart-type sur X_train (équivalent d'un .fit()) ; ajoutée comme 1ère couche, elle scale automatiquement toute entrée du modèle (train, test, futures prédictions)"],
    ["Autres couches de preprocessing intégrables","CategoryEncoding (OneHotEncoder), TextVectorization (NLP), Resizing (images)...","même logique que Normalization : le preprocessing fait partie du modèle sauvegardé, plus besoin de le répéter manuellement à l'inférence"],
  ]},
  {id:"dl-tfdata-syntax", group:"dl", subgroup:"Keras", title:"Charger un gros dataset — tf.data.Dataset", rows:[
    ["Charger des images depuis un dossier, batch par batch","from tensorflow.keras.utils import image_dataset_from_directory\nds = **image_dataset_from_directory**(data_dir, batch_size=32)","les classes sont inférées automatiquement depuis la structure des sous-dossiers ; les images ne sont chargées en RAM qu'au moment de leur utilisation, pas toutes d'un coup"],
    ["Entraîner directement sur le Dataset","model.**fit**(ds, epochs=10)","pas besoin de X_train/y_train séparés — Keras itère lui-même sur ds batch par batch"],
    ["Construire un Dataset à partir d'un array/tensor","ds = tf.data.Dataset.**from_tensor_slices**(array)","point de départ générique (pas seulement des images) — ex: un long texte encodé en entiers, un array Numpy..."],
    ["Chaîner les opérations (shuffle, batch, prefetch)","ds = (ds\n    .**shuffle**(buffer_size=10000)\n    .**batch**(64, drop_remainder=True)\n    .**prefetch**(tf.data.AUTOTUNE))","shuffle = mélange par blocs de buffer_size (pas tout le dataset d'un coup) ; prefetch prépare le batch suivant PENDANT que le GPU traite le batch courant"],
  ]},
  {id:"dl-save-load-syntax", group:"dl", subgroup:"Keras", title:"Sauvegarder & charger un modèle", rows:[
    ["Sauvegarder un modèle entraîné","from tensorflow.keras import models\nmodels.**save_model**(model, 'my_model.keras')","format .keras (Keras 3) — indispensable pour partager/déployer un modèle sans le ré-entraîner"],
    ["Charger un modèle sauvegardé","loaded_model = models.**load_model**('my_model.keras')","renvoie un modèle directement utilisable avec .predict()/.evaluate()"],
    ["Sauvegarder/charger UNIQUEMENT les poids","model.**save_weights**('weights.h5')\nmodel.**load_weights**('weights.h5')","fichier plus léger (pas l'architecture) — il faut redéfinir/reconstruire le même modèle avant de charger les poids dedans"],
  ]},
  {id:"dl-cnn-conv2d-syntax", group:"dl", subgroup:"Modèles de base", subsubgroup:"CNN", title:"Couche de convolution — Conv2D", rows:[
    ["Ajouter le canal manquant (images en niveaux de gris)","X = X.**reshape**(len(X), 28, 28, 1)\n# ou : from keras.ops import expand_dims\nX = **expand_dims**(X, axis=-1)","Conv2D exige (n_images, hauteur, largeur, CANAUX) — un dataset grayscale (n, h, w) n'a pas cette 4e dimension, à ajouter avant de fitter"],
    ["Ajouter une couche de convolution","model.add(layers.**Conv2D**(16, kernel_size=(3, 3), activation='relu'))","16 = nombre de filtres ; kernel_size=3 <=> (3,3) — cf. page Modélisation ▸ [Convolution — kernel, filtre, feature map](#dl-cnn-convolution)"],
    ["Contrôler le pas de déplacement du kernel","layers.Conv2D(16, (3, 3), **strides**=(2, 2), activation='relu')","strides=1 par défaut ; strides=2 divise par ~2 la taille de la feature map en sortie"],
    ["Contrôler le remplissage des bords","layers.Conv2D(16, (3, 3), **padding**='same', activation='relu')","'valid' (défaut) : la feature map rétrécit ; 'same' : padding à 0 pour garder la même taille que l'input — cf. page Modélisation ▸ Hyperparamètres de la convolution"],
  ]},
  {id:"dl-cnn-pooling-syntax", group:"dl", subgroup:"Modèles de base", subsubgroup:"CNN", title:"Pooling — réduire la taille sans paramètres", rows:[
    ["Max-pooling (garde le max de chaque zone)","model.add(layers.**MaxPool2D**(pool_size=(2, 2)))","bonne pratique : une couche MaxPool2D après chaque Conv2D — cf. page Modélisation ▸ Hyperparamètres de la convolution"],
    ["Average-pooling (moyenne de chaque zone)","model.add(layers.**AveragePooling2D**(pool_size=(2, 2)))","moins courant que MaxPooling en pratique"],
  ]},
  {id:"dl-cnn-full-model-syntax", group:"dl", subgroup:"Modèles de base", subsubgroup:"CNN", title:"Modèle CNN complet (ex: MNIST)", rows:[
    ["Architecture — convolutions + pooling puis Dense","model = Sequential([\n    Input(shape=(28, 28, 1)),\n    layers.**Rescaling**(scale=1./255.),\n    layers.Conv2D(16, (3, 3), padding='same', activation='relu'),\n    layers.MaxPool2D(pool_size=(2, 2)),\n    layers.Conv2D(32, (2, 2), padding='same', activation='relu'),\n    layers.MaxPool2D(pool_size=(2, 2)),\n    layers.**Flatten**(),\n    layers.Dense(50, activation='relu'),\n    layers.Dropout(0.3),\n    layers.Dense(10, activation='softmax'),\n])","Rescaling intégré au modèle (cf. page Syntaxes ▸ Preprocessing intégré au modèle) ; Flatten uniquement à la FIN, juste avant les couches Dense ; kernel_size decroît / filtres augmentent en profondeur — cf. page Modélisation ▸ [Architecture typique & Transfer Learning](#dl-cnn-transfer-learning)"],
    ["Compiler sans One-Hot Encoder la target","model.compile(loss='**sparse_categorical_crossentropy**', optimizer='adam', metrics=['accuracy'])","équivalent de categorical_crossentropy + to_categorical(y), mais prend directement des labels entiers (0,1,2...) en target"],
  ]},
  {id:"dl-cnn-augmentation-syntax", group:"dl", subgroup:"Modèles de base", subsubgroup:"CNN", title:"Data Augmentation — ImageDataGenerator", rows:[
    ["Définir les transformations","from keras.preprocessing.image import ImageDataGenerator\ndatagen = **ImageDataGenerator**(rotation_range=20, width_shift_range=0.1, zoom_range=0.1, horizontal_flip=True)\ndatagen.fit(X_train)","chaque paramètre active un type de variation aléatoire ; .fit() calcule les stats nécessaires (ex: pour un centrage) sur X_train"],
    ["Entraîner sur le flux augmenté","train_flow = datagen.**flow**(X_train, y_train, batch_size=64)\nmodel.fit(train_flow, validation_data=(X_val, y_val), epochs=50)","validation_data OBLIGATOIRE ici (validation_split impossible avec un flow) — cf. page Modélisation ▸ [Data Augmentation](#dl-cnn-data-augmentation) ; le split train/val doit être fait AVANT d'appeler flow()"],
    ["Alternative — augmentation intégrée au modèle (couches)","model.add(layers.**RandomFlip**('horizontal'))\nmodel.add(layers.**RandomZoom**(0.1))\nmodel.add(layers.**RandomRotation**(0.1))","ajoutées juste après Rescaling ; actives UNIQUEMENT pendant model.fit() (comme Dropout), inactives à l'inférence — mêmes avantages que Normalization intégrée (cf. page Syntaxes ▸ Preprocessing intégré au modèle) : pas de logique séparée à maintenir"],
  ]},
  {id:"dl-cnn-transfer-learning-syntax", group:"dl", subgroup:"Modèles de base", subsubgroup:"CNN", title:"Transfer Learning — charger & greffer un modèle pré-entraîné", rows:[
    ["Charger un modèle pré-entraîné sans sa tête","from keras.applications.vgg16 import VGG16\nbase_model = **VGG16**(weights='imagenet', include_top=False, input_shape=(256, 256, 3))","include_top=False retire les couches Dense de classification d'origine (spécifiques à ImageNet) — ne garde que les couches de convolution"],
    ["Geler ses poids","base_model.**trainable** = False","empêche ces couches d'être mises à jour pendant model.fit() — cf. page Modélisation ▸ Architecture typique &amp; Transfer Learning"],
    ["Greffer de nouvelles couches par-dessus","model = Sequential([\n    base_model,\n    layers.Flatten(),\n    layers.Dense(500, activation='relu'),\n    layers.Dense(num_classes, activation='softmax'),\n])","un modèle pré-entraîné entier peut être passé comme premier élément de la forme liste de Sequential, exactement comme une couche normale"],
    ["Preprocessing spécifique au modèle pré-entraîné","from keras.applications.vgg16 import preprocess_input\nX_train_vgg = **preprocess_input**(X_train)","chaque modèle pré-entraîné attend un preprocessing précis (ici : centrage RGB selon les stats ImageNet) — PAS le /255 habituel ; alternative : l'insérer comme couche via layers.Lambda(preprocess_input) dans le Sequential"],
    ["Learning rate réduit pour le fine-tuning","model.compile(optimizer=optimizers.Adam(learning_rate=**1e-4**), loss='categorical_crossentropy')","plus faible que le défaut d'Adam (1e-3) — on ne veut ajuster les nouvelles couches qu'en douceur"],
  ]},
  {id:"dl-rnn-layer-syntax", group:"dl", subgroup:"Modèles de base", subsubgroup:"RNN", title:"Couche récurrente — SimpleRNN, LSTM, GRU", rows:[
    ["Ajouter une couche récurrente simple","model.add(layers.**SimpleRNN**(units=10, activation='tanh'))","units = n_h (taille de l'état interne / nombre de \"mémoires\" parallèles) — cf. page Modélisation ▸ [Sous le capot d'une couche RNN](#dl-rnn-mechanics)"],
    ["Variantes plus robustes au vanishing gradient","model.add(layers.**LSTM**(units=10))\nmodel.add(layers.**GRU**(units=10))","même syntaxe que SimpleRNN — cf. page Modélisation ▸ SimpleRNN, LSTM, GRU"],
    ["Préciser la forme d'entrée","model.add(Input(shape=(n_observations, n_features)))","shape = (longueur de séquence, nb de features par pas de temps) — PAS de dimension batch"],
  ]},
  {id:"dl-rnn-windowing-syntax", group:"dl", subgroup:"Modèles de base", subsubgroup:"RNN", title:"Fenêtrage — transformer une série en séquences", rows:[
    ["Construire (X, y) par fenêtres glissantes","sequence_length = 7\nX, y = [], []\nfor i in range(len(series) - sequence_length):\n    X.append(series[i:i+sequence_length])\n    y.append(series[i+sequence_length])\nX, y = np.array(X), np.array(y)","chaque fenêtre de sequence_length observations consécutives devient une séquence d'entrée ; l'observation suivante devient sa cible — cf. page Modélisation ▸ Pourquoi un RNN"],
    ["Ajouter la dimension features (univarié)","X = X.reshape(X.shape[0], X.shape[1], **1**)","passe de (n_sequences, n_observations) à (n_sequences, n_observations, 1) — 1 seule feature observée par pas de temps"],
  ]},
  {id:"dl-rnn-return-sequences-syntax", group:"dl", subgroup:"Modèles de base", subsubgroup:"RNN", title:"Sortie complète & stacking — return_sequences", rows:[
    ["Ne garder que le dernier état (défaut)","layers.SimpleRNN(10)","sortie = (batch, units) — 1 vecteur par séquence"],
    ["Garder la sortie à chaque pas de temps","layers.SimpleRNN(10, **return_sequences**=True)","sortie = (batch, n_observations, units) — nécessaire pour prédire une séquence ou empiler un autre RNN"],
    ["Empiler deux couches récurrentes","model.add(layers.SimpleRNN(10, return_sequences=True))\nmodel.add(layers.SimpleRNN(3))","return_sequences=True sur TOUTES les couches sauf la dernière — cf. page Modélisation ▸ Sortie d'une couche RNN"],
  ]},
  {id:"dl-rnn-padding-syntax", group:"dl", subgroup:"Modèles de base", subsubgroup:"RNN", title:"Séquences de longueurs différentes — padding & Masking", rows:[
    ["Uniformiser la longueur des séquences","from tensorflow.keras.preprocessing.sequence import pad_sequences\nX_pad = **pad_sequences**(X, dtype='float32', padding='post', value=-1000)","padding='post' = ajoute le padding à LA FIN (recommandé) ; value = valeur absente des vraies données"],
    ["Ignorer le padding pendant l'entraînement","model.add(layers.**Masking**(mask_value=-1000))","1ère couche du modèle — même valeur que celle utilisée dans pad_sequences"],
  ]},
  {id:"dl-nlp-tokenizer-syntax", group:"dl", subgroup:"LLM", subsubgroup:"NLP", title:"Tokenizer — mots vers entiers", rows:[
    ["Construire le vocabulaire (mots → entiers)","from tensorflow.keras.preprocessing.text import Tokenizer\ntk = Tokenizer()\ntk.**fit_on_texts**(X)","équivalent d'un .fit() Sklearn — construit le dictionnaire mot→entier à partir du corpus"],
    ["Convertir les phrases en séquences d'entiers","X_token = tk.**texts_to_sequences**(X)","équivalent d'un .transform() — remplace chaque mot par son entier"],
    ["Uniformiser la longueur des séquences","from tensorflow.keras.preprocessing.sequence import pad_sequences\nX_pad = **pad_sequences**(X_token, padding='post')","même logique que pour un RNN classique — cf. page Modélisation ▸ Séquences de longueurs différentes (groupe RNN)"],
    ["Plafonner la longueur (accélère l'entraînement)","X_pad = pad_sequences(X_token, padding='post', **maxlen**=200)","sans maxlen, TOUTES les séquences sont paddées à la longueur de la plus longue phrase du corpus — souvent démesurée à cause de quelques outliers ; tronquer à une longueur raisonnable réduit fortement le temps d'entraînement, pour une perte de performance généralement négligeable"],
    ["Alternative — tokenisation intégrée au modèle","from keras.layers import TextVectorization\nvec = **TextVectorization**(standardize='lower', output_mode='int', output_sequence_length=200)\nvec.adapt(X)\nmodel.add(vec)","même logique que Normalization/Rescaling intégrées (cf. page Syntaxes ▸ Preprocessing intégré au modèle) : combine Tokenizer + pad_sequences en UNE couche, fait partie du modèle sauvegardé"],
  ]},
  {id:"dl-nlp-embedding-syntax", group:"dl", subgroup:"LLM", subsubgroup:"NLP", title:"Couche d'embedding — layers.Embedding", rows:[
    ["Ajouter une couche d'embedding apprise","model.add(layers.**Embedding**(\n    input_dim=vocab_size+1,\n    output_dim=embedding_dim,\n    mask_zero=True,\n))","input_dim = taille du vocabulaire +1 (pour le 0 du padding) ; output_dim = dimension de l'espace d'embedding (30 à 300 typiquement) ; mask_zero=True = Masking intégré (ignore le padding automatiquement)"],
  ]},
  {id:"dl-nlp-word2vec-syntax", group:"dl", subgroup:"LLM", subsubgroup:"NLP", title:"Word2Vec — embedding indépendant de la tâche (Gensim)", rows:[
    ["Entraîner un Word2Vec sur son propre corpus","from gensim.models import Word2Vec\nword2vec = **Word2Vec**(sentences=X_train, vector_size=100, window=5, min_count=5)","vector_size = dimension de l'embedding ; window = nb de mots voisins utilisés pour la prédiction ; min_count = ignore les mots vus moins de N fois"],
    ["Récupérer le vecteur d'un mot","word2vec.wv['movie']","vecteur dense de taille vector_size"],
    ["Trouver les mots les plus proches","word2vec.wv.**most_similar**('movie', topn=10)","proximité = similarité cosinus dans l'espace d'embedding — cf. page Modélisation ▸ Représenter les mots"],
    ["Charger un Word2Vec pré-entraîné (Transfer Learning)","import gensim.downloader\nmodel_wiki = gensim.downloader.**load**('glove-wiki-gigaword-50')","à préférer sur un petit corpus — cf. page Modélisation ▸ Embedding indépendant de la tâche"],
    ["Voir le vocabulaire appris","vocab_size = len(word2vec.wv.**key_to_index**)","taille réellement retenue après filtrage par min_count — souvent bien plus petite que le nombre de mots distincts du corpus"],
    ["Embedder une phrase mot par mot (sans layers.Embedding)","def embed_sentence(word2vec, sentence):\n    return np.array([word2vec.wv[w] for w in sentence if w in word2vec.wv])\n\nX_train_embedded = [embed_sentence(word2vec, s) for s in X_train]\nX_train_pad = pad_sequences(X_train_embedded, dtype='float32', padding='post')","transforme chaque phrase en matrice (n_mots, vector_size) AVANT de la passer au RNN — alternative à layers.Embedding : le vecteur de chaque mot est déjà figé par Word2Vec, pas appris pendant model.fit() ; ignorer les mots absents du vocabulaire (min_count, ou mot inconnu du train)"],
  ]},
  {id:"dl-nlp-conv1d-syntax", group:"dl", subgroup:"LLM", subsubgroup:"NLP", title:"Conv1D — convolution pour du texte", rows:[
    ["Convolution 1D après un Embedding","model.add(layers.**Conv1D**(filters=20, kernel_size=3))","glisse uniquement le long de l'axe des mots (jamais sur l'axe de l'embedding) ; kernel_size = nb de mots consécutifs considérés — cf. page Modélisation ▸ [CNN pour du texte — Conv1D](#dl-nlp-conv1d)"],
  ]},
  {id:"dl-huggingface-syntax", group:"dl", subgroup:"LLM", subsubgroup:"Transformers", title:"Hugging Face — pipeline pré-entraîné", rows:[
    ["Charger un pipeline pré-entraîné","from transformers import pipeline\npipe = **pipeline**(\"translation\", model=\"Helsinki-NLP/opus-mt-en-fr\")","combine automatiquement le tokenizer ET le modèle adaptés à la tâche et au modèle choisis"],
    ["Exécuter le pipeline","result = pipe(\"I am a student\")\nresult[0]['translation_text']","sortie déjà post-traitée (texte lisible, pas des tokens/probabilités brutes)"],
    ["Sans préciser de modèle (défaut HuggingFace)","pipe = pipeline(\"sentiment-analysis\")\npipe(\"Transformers are awesome!\")","pratique pour prototyper vite ; le modèle par défaut généralise parfois mal (vocabulaire différent du corpus d'entraînement) — préciser model= pour un domaine spécifique"],
    ["Autres tâches courantes (même interface)","pipeline(\"summarization\", model=\"sshleifer/distilbart-xsum-12-6\")\npipeline(\"question-answering\", model=\"deepset/roberta-base-squad2\")(question=q, context=texte)\npipeline(\"automatic-speech-recognition\", model=\"openai/whisper-tiny\")(\"audio.wav\")","même abstraction pipeline(tâche, model=...) pour résumé, question-réponse, reconnaissance vocale... — seuls le nom de la tâche et les arguments d'appel changent"],
  ]},
  {id:"dl-huggingface-finetune-syntax", group:"dl", subgroup:"LLM", subsubgroup:"Transformers", title:"Hugging Face — charger tokenizer/modèle séparément & fine-tuner", rows:[
    ["Charger le tokenizer d'un modèle précis","from transformers import AutoTokenizer\ntokenizer = AutoTokenizer.**from_pretrained**(\"prajjwal1/bert-tiny\")","le tokenizer DOIT correspondre exactement au modèle choisi (même vocabulaire) — sinon les tokens ne veulent plus rien dire pour lui"],
    ["Tokeniser un batch de textes pour Keras","tokens = tokenizer(\n    df['text'].tolist(),\n    max_length=500, truncation=True, padding='max_length',\n    return_tensors='**tf**',\n)","renvoie un dict avec input_ids/attention_mask/token_type_ids, chacun un tf.Tensor (return_tensors='tf', sinon simples listes Python)"],
    ["Charger le modèle pré-entraîné (poids figés)","from transformers import TFAutoModel\nmodel = TFAutoModel.**from_pretrained**(\"prajjwal1/bert-tiny\", from_pt=True)","from_pt=True convertit des poids PyTorch en TensorFlow — nécessaire pour la plupart des modèles du hub, entraînés en PyTorch à l'origine"],
    ["Récupérer les embeddings contextuels","output = model.predict(tokens['input_ids'])\ncls_embedding = output.**last_hidden_state**[:, 0, :]","last_hidden_state a pour shape (n_phrases, n_tokens, hidden_size) ; le token [CLS] (index 0) résume toute la phrase — utilisable comme feature pour un petit classifieur Dense séparé (BERT gelé)"],
    ["Fine-tuner le modèle en entier pour une tâche précise","from transformers import TFAutoModelForSequenceClassification\nmodel = TFAutoModelForSequenceClassification.from_pretrained(\n    \"prajjwal1/bert-tiny\", from_pt=True, **num_labels**=2,\n)\nmodel.compile(optimizer='adam', loss=tf.keras.losses.SparseCategoricalCrossentropy(from_logits=True), metrics=['accuracy'])\nmodel.fit(tokens['input_ids'], y, validation_data=(...), epochs=3, batch_size=16)","ajoute une tête de classification et RÉENTRAÎNE tous les poids (contrairement à l'extraction de features ci-dessus) — from_logits=True indispensable, ce modèle renvoie des logits bruts ; peu d'epochs (3-5), beaucoup de poids à mettre à jour d'un coup"],
  ]},
  {id:"dl-autoencoder-syntax", group:"dl", subgroup:"LLM", subsubgroup:"Autoencoder", title:"Construire un Autoencoder", rows:[
    ["Décodeur — upsampling avec Conv2DTranspose","from keras.layers import Conv2DTranspose, Reshape\ndecoder = Sequential([\n    layers.Dense(7*7*8, activation='tanh'),\n    Reshape((7, 7, 8)),\n    **Conv2DTranspose**(8, (2,2), strides=2, padding='same', activation='relu'),\n    Conv2DTranspose(1, (2,2), strides=2, padding='same', activation='relu'),\n])","strides=2 double la taille spatiale à chaque couche (7→14→28) — inverse d'un MaxPooling2D, cf. page Modélisation ▸ Conv2DTranspose"],
    ["Assembler encodeur + décodeur","from keras import Model\ninputs = Input(shape=(28, 28, 1))\noutputs = decoder(encoder(inputs))\nautoencoder = **Model**(inputs, outputs)","Functional API (cf. page Syntaxes ▸ Architectures non-linéaires) — encoder et decoder restent utilisables séparément après coup"],
    ["Entraîner — la target EST l'input","autoencoder.compile(loss='mse', optimizer='adam')\nautoencoder.fit(**X_train, X_train**, epochs=20)","pas de label externe ; pour du débruitage : fit(X_train_noisy, X_train) — entrée bruitée, target propre"],
    ["Encoder seul — obtenir la représentation compressée","X_encoded = encoder.**predict**(X_train)","ex: (60000, 28, 28, 1) → (60000, 2) — chaque image compressée en 2 valeurs"],
  ]},
  {id:"dl-diffusion-syntax", group:"dl", subgroup:"Génération", title:"Génération d'image — diffusers (Stable Diffusion)", rows:[
    ["Charger un pipeline de diffusion pré-entraîné","from diffusers import StableDiffusionPipeline\npipe = StableDiffusionPipeline.**from_pretrained**(model_id, torch_dtype=torch.float32)\npipe = pipe.to(\"cpu\")","même logique qu'un pipeline Hugging Face (cf. page Syntaxes ▸ Hugging Face) mais pour l'image ; torch_dtype=torch.float16 sur GPU pour accélérer, float32 sur CPU"],
    ["Générer une image à partir d'un prompt","image = pipe(prompt, **num_inference_steps**=25, **guidance_scale**=7.5).images[0]\nimage.save(\"out.png\")","cf. page Modélisation ▸ Modèles de diffusion pour le rôle de ces deux hyperparamètres"],
    ["Accéder à un modèle à accès restreint (gated)","from huggingface_hub import login\nlogin()","nécessite d'avoir accepté la licence du modèle sur sa page Hugging Face au préalable — sinon from_pretrained() lève une GatedRepoError (401), avant même de télécharger le moindre poids"],
  ]},
  {id:"dl-tensorflow-tensors", group:"dl", subgroup:"TensorFlow", title:"Manipuler des tensors (équivalents Numpy)", rows:[
    ["Créer un tensor constant","import tensorflow as tf\ntf.**constant**([1, 2, 3])  # ou tf.ones((3,3)), tf.zeros((3,3))","un tf.Tensor — équivalent de np.array()/np.ones()/np.zeros(), PAS un ndarray"],
    ["Reconvertir un tensor en tableau Numpy","x.**numpy**()","ndarray — pour inspecter/afficher un résultat EN DEHORS du graphe, jamais dans une loss/metric/layer custom (cf. page Modélisation ▸ Pourquoi tf.* et pas Numpy/Pandas)"],
    ["Élever au carré","**tf**.square(x)","équivalent de np.square(x) / x**2"],
    ["Valeur absolue","**tf**.abs(x)","équivalent de np.abs(x)"],
    ["Racine carrée","**tf**.sqrt(x)","équivalent de np.sqrt(x)"],
    ["Moyenne (réduit le tensor à un scalaire)","**tf**.reduce_mean(x)","équivalent de np.mean(x) — 'reduce' = agrège en réduisant une dimension du tensor"],
    ["Somme (réduit le tensor à un scalaire)","**tf**.reduce_sum(x)","équivalent de np.sum(x)"],
    ["Convertir un array/une liste en tensor","tf.**convert_to_tensor**(array)  # ou tf.constant(liste)","équivalent de np.array() — convert_to_tensor pour un array Numpy, constant pour une liste Python"],
    ["Changer le dtype","tf.**cast**(tensor, dtype=tf.float32)","équivalent de .astype() côté Numpy/Pandas"],
    ["Ajouter/reformer les dimensions","tf.**expand_dims**(x, axis=0)   # équivalent de np.expand_dims\ntf.**reshape**(x, (1, 3, 3))     # équivalent de x.reshape(...)","mêmes usages qu'en Numpy — utile pour ajouter la dimension batch/canal attendue par une couche"],
    ["Produit matriciel","tf.**matmul**(a, b, transpose_b=True)","équivalent de a @ b / np.matmul — transpose_a/transpose_b évitent un .T explicite avant l'appel"],
    ["Empiler / répéter des tensors","tf.**concat**([t1, t2], axis=0)   # équivalent de np.concatenate\ntf.**tile**(t, [50, 1])            # répète t 50 fois sur l'axe 0","concat assemble des tensors déjà existants ; tile répète UN SEUL tensor plusieurs fois"],
  ]},
  {id:"dl-llm-api-syntax", group:"aieng", subgroup:"Fondamentaux", title:"Appeler une API LLM (ex: Gemini)", rows:[
    ["Instancier un client API","from google import genai\nclient = genai.**Client**()","nécessite une clé API (variable d'environnement, ex: GOOGLE_API_KEY)"],
    ["Générer du texte","response = client.models.**generate_content**(\n    model=\"gemini-2.5-flash-lite\",\n    contents=prompt,\n)\nresponse.text","équivalent d'un .predict() — le modèle est choisi par son nom (string)"],
    ["Configurer la génération","config=genai.types.GenerateContentConfig(\n    system_instruction=system_prompt,\n    max_output_tokens=200,\n    **temperature**=1.2,\n)","system_instruction = rôle/consignes globales ; temperature/max_output_tokens — cf. page Modélisation ▸ Contrôler la génération (groupe Transformers)"],
  ]},
  {id:"dl-langchain-chat-syntax", group:"aieng", subgroup:"Fondamentaux", title:"LangChain — interface de chat agnostique du fournisseur", rows:[
    ["Instancier un modèle sans coder en dur son fournisseur","from langchain.chat_models import init_chat_model\nmodel = **init_chat_model**(\"gemini-2.5-flash-lite\", model_provider=\"google_genai\")","au lieu d'une classe spécifique (ex: ChatGoogleGenerativeAI) — changer de fournisseur (OpenAI, Anthropic...) ne demande alors que de changer ces deux arguments, pas de réécrire le code"],
    ["Appeler le modèle","response = model.**invoke**(\"What is the capital of France?\")\nresponse.text","interface commune quel que soit le fournisseur choisi à l'instanciation"],
    ["Ajuster les hyperparamètres après coup","model.temperature = 1.0\nmodel.max_output_tokens = 200","exposés comme de simples attributs de l'objet modèle — alternative à les passer à la création"],
    ["Conversation à plusieurs messages (système + utilisateur)","from langchain_core.messages import SystemMessage, HumanMessage\nmessages = [\n    SystemMessage(content=\"You are a helpful assistant.\"),\n    HumanMessage(content=\"Explain attention in one sentence.\"),\n]\nresponse = model.invoke(messages)","SystemMessage = consigne de comportement donnée une fois ; HumanMessage = message utilisateur ; AIMessage = une réponse du modèle (pour repasser un historique multi-tours)"],
  ]},
  {id:"dl-prompt-patterns-syntax", group:"aieng", subgroup:"Fondamentaux", title:"Prompt Engineering — patterns concrets", rows:[
    ["Format Instruction/Sortie (réponse structurée)","f\"Instruct: {question}\\n**Output**:\"","pousse le modèle à répondre directement plutôt qu'à continuer/paraphraser la question — surtout utile sur un petit modèle (SLM) pas fine-tuné par RLHF"],
    ["Forcer un raisonnement étape par étape","question + \" **Solve step by step.**\"","sur un petit modèle, ce raisonnement n'est pas toujours implicite (contrairement à un gros LLM) — à demander explicitement"],
    ["Forcer une réponse concise (1 mot/1 label)","f\"Classify the sentiment as Positive/Negative:\\n{text}\\n\\n**Sentiment:**\"","terminer le prompt par le début du format de réponse attendu (ici juste avant le label) pousse le modèle à continuer directement par ce mot, au lieu de répéter toute la consigne"],
    ["Guider une génération de code","code_start + '\\n    \"\"\"**Docstring décrivant précisément le comportement attendu**\"\"\"'","une docstring précise (API à utiliser, comportement des paramètres) donne beaucoup plus de matière au modèle qu'une simple signature de fonction"],
  ]},
  {id:"dl-rag-langchain-syntax", group:"aieng", subgroup:"RAG", title:"Pipeline RAG avec LangChain", rows:[
    ["Charger un document","from langchain_community.document_loaders import PyPDFLoader\ndata = PyPDFLoader(\"book.pdf\").**load**()","découpe déjà 1 Document par page — mais des frontières de page arbitraires (souvent en plein milieu d'une phrase) ; pour un vrai découpage en chunks, cf. ligne suivante"],
    ["Découper en chunks avec chevauchement","from langchain_text_splitters import RecursiveCharacterTextSplitter\nsplitter = **RecursiveCharacterTextSplitter**(chunk_size=2000, chunk_overlap=400)\nchunks = splitter.**split_documents**(data)","chunk_overlap = chevauchement entre chunks consécutifs, pour ne pas perdre le contexte à une frontière de coupe — le splitter essaie d'abord de couper aux frontières naturelles (paragraphes, phrases)"],
    ["Indexer les chunks dans une Vector Database","from langchain_chroma import Chroma\nvector_db = Chroma.**from_documents**(\n    documents=chunks,\n    embedding=embeddings,\n)","embedding = un modèle d'embedding (ex: GoogleGenerativeAIEmbeddings) — calcule et stocke un vecteur par chunk ; vit en RAM, perdu à la fermeture (cf. ligne suivante pour la persistance)"],
    ["Vector Database PERSISTANTE (stockée sur disque)","vector_db = Chroma(\n    collection_name=\"mes_docs\",\n    embedding_function=embeddings,\n    **persist_directory**=\"./chroma_db\",\n)\nvector_db.**add_documents**(chunks)","évite de re-payer l'embedding de tous les documents à chaque redémarrage — construction en 2 temps (instancier vide, puis add_documents) plutôt que from_documents() en une fois"],
    ["Récupérer les documents les plus pertinents","docs = vector_db.**similarity_search**(query, k=5)","k = nombre de chunks retournés — cf. page Industrialisation ▸ Pipeline RAG"],
    ["Filtrer la recherche par métadonnées","vector_db.similarity_search(query, k=5, **filter**={\"session_date\": \"2026-07-06\"})","restreint la recherche par similarité aux documents dont les metadata correspondent EXACTEMENT au filtre — utile pour combiner similarité sémantique et filtre exact (date, source...)"],
    ["Réutiliser un prompt RAG déjà éprouvé","from langchain_classic import hub\ntemplate = hub.**pull**(\"rlm/rag-prompt\")\nprompt = template.invoke({\"context\": docs_content, \"question\": query})","évite d'écrire son prompt RAG à la main (concaténation de contexte+question) — bibliothèque de prompts partagés (LangSmith Hub)"],
  ]},
  {id:"dl-tool-calling-syntax", group:"aieng", subgroup:"Agents", title:"Tool Calling — déclarer et appeler une fonction", rows:[
    ["Décrire la fonction pour le LLM","tool_declaration = {\n    \"name\": \"get_matches\",\n    \"description\": \"...\",\n    \"parameters\": {\"type\": \"object\", \"properties\": {...}, \"required\": [...]},\n}","schéma JSON standard (nom, description, paramètres attendus) — cf. page Industrialisation ▸ Tool Calling"],
    ["Attacher le tool à la requête","tools = genai.types.**Tool**(function_declarations=[tool_declaration])\nresponse = client.models.generate_content(\n    ..., config=genai.types.GenerateContentConfig(tools=[tools])\n)","le LLM choisit d'appeler (ou non) la fonction décrite"],
    ["Récupérer les arguments extraits","args = response.candidates[0].content.parts[0].function_call.args\nmy_function(**args)","le LLM ne fait qu'EXTRAIRE les arguments — c'est votre code qui appelle réellement la fonction"],
    ["Convertir une fonction Python en tool (LangChain)","from langchain.tools import tool\n\n@**tool**\ndef get_recipes_tool(ingredient: str, max_prep_time: int = 0) -> pd.DataFrame:\n    \"\"\"Scrape recipes for a given ingredient.\"\"\"\n    return recipe.get_recipes(ingredient, max_prep_time)","LangChain déduit le schéma JSON (nom, description, paramètres) directement des type hints + du docstring — évite d'écrire ce schéma à la main comme sur la 1ère ligne"],
    ["Inspecter le schéma déduit","get_recipes_tool.name\nget_recipes_tool.description\nget_recipes_tool.args\nget_recipes_tool.args_schema.**model_json_schema**()","utile pour vérifier ce que \"voit\" le LLM avant de l'utiliser"],
    ["Lier le(s) tool(s) à un chat model LangChain","model_with_tools = model.**bind_tools**([get_recipes_tool])","le binding est local (pas d'appel réseau) — le LLM ne \"connaît\" le tool qu'au moment de .invoke()"],
    ["Extraire l'appel de tool proposé (LangChain)","response = model_with_tools.invoke(query)\nargs = response.**tool_calls**[0]['args']\nget_recipes_tool.**invoke**(args)","tool_calls = liste (plusieurs tools possibles) ; même principe que ci-dessus, le LLM extrait juste les arguments"],
    ["Détailler chaque paramètre du schéma","@tool(**parse_docstring**=True)\ndef ma_fonction(x: int) -> ...:\n    \"\"\"Résumé court.\n\n    Args:\n        x: description du paramètre x.\n    \"\"\"","répartit le docstring entre description générale et description par paramètre (extraite de la section Args:), au lieu d'un seul bloc de texte"],
    ["Énumérer les valeurs acceptées par un paramètre","from typing import Literal\ndifficulty_levels: list[**Literal**[\"Easy\", \"Moderate\", \"Hard\"]] = []","ajoute un enum au schéma JSON du paramètre — le LLM est bien plus fiable pour renvoyer exactement les valeurs attendues (plutôt que deviner une valeur proche mais fausse)"],
  ]},
  {id:"dl-langgraph-agent-syntax", group:"aieng", subgroup:"Agents", title:"LangGraph — construire un agent (create_agent)", rows:[
    ["Créer un agent (LLM + tools)","from langchain.agents import create_agent\nagent_executor = **create_agent**(model, tools)","tools = liste d'objets Tool (cf. Tool Calling ci-dessus) — combine le cerveau (model) et la boîte à outils ; contrairement au tool calling seul, l'agent EXÉCUTE lui-même les tools nécessaires"],
    ["Appeler l'agent","from langchain_core.messages import HumanMessage\nresponse = agent_executor.**invoke**({\"messages\": [HumanMessage(content=query)]})\nresponse[\"messages\"][-1].content","l'état de l'agent est une liste de messages ; le dernier contient la réponse finale, après tous les appels de tools intermédiaires"],
    ["Suivre les étapes intermédiaires en streaming","for step in agent_executor.**stream**(\n    {\"messages\": [HumanMessage(content=query)]}, stream_mode=\"values\",\n):\n    step[\"messages\"][-1].pretty_print()","affiche chaque étape (appel de tool, résultat, réponse) au fur et à mesure plutôt que d'attendre la fin — utile pour vérifier que le LLM n'hallucine pas"],
    ["Gérer les erreurs d'un tool sans planter l'agent","from langchain.agents.middleware import wrap_tool_call\n\n@**wrap_tool_call**\ndef handle_tool_errors(request, handler):\n    try:\n        return handler(request)\n    except Exception as e:\n        return ToolMessage(content=f\"Tool error: {e}\", tool_call_id=request.tool_call[\"id\"])\n\nagent_executor = create_agent(model, tools, middleware=[handle_tool_errors])","middleware = intercepte les appels de tool ; sans ça, une erreur d'un tool (ex: accès API refusé) fait planter tout l'agent au lieu de laisser le LLM s'adapter"],
    ["Donner une mémoire à l'agent (conversation multi-tours)","from langgraph.checkpoint.memory import MemorySaver\nmemory = MemorySaver()\nagent_executor = create_agent(model, tools, **checkpointer**=memory)","sans checkpointer, chaque .invoke() repart de zéro — avec, l'historique de conversation est conservé automatiquement, identifié par un thread_id (ligne suivante)"],
    ["Poursuivre une conversation existante","config = {\"configurable\": {\"**thread_id**\": \"abc123\"}}\nagent_executor.invoke({\"messages\": [...]}, config=config)","même thread_id = même fil de conversation (mémoire partagée) ; changer de thread_id démarre une conversation vierge"],
    ["Orienter le comportement de l'agent","agent_executor = create_agent(\n    model, tools, **system_prompt**=\"...instructions de comportement...\",\n)","ex: \"si l'accès au prix courant échoue, utilise le dernier prix de clôture disponible\" — LangGraph n'ajoute AUCUN system prompt par défaut"],
  ]},

  // ---------- Groupe : Reinforcement Learning (sorti du groupe dl) ----------
  {id:"dl-rl-gymnasium-syntax", group:"rl", subgroup:"Reinforcement Learning", title:"Gymnasium — créer & piloter un environnement", rows:[
    ["Créer un environnement","import gymnasium as gym\nenv = gym.**make**('FrozenLake-v1', is_slippery=False)","catalogue d'environnements prêts à l'emploi (grilles, Atari, contrôle continu...)"],
    ["Démarrer un épisode","state, info = env.**reset**()","renvoie l'état initial — à appeler au début de chaque épisode"],
    ["Échantillonner une action aléatoire","action = env.action_space.**sample**()","utile pour explorer, notamment en tout début d'entraînement"],
    ["Exécuter une action","next_state, reward, terminated, truncated, info = env.**step**(action)","terminated = objectif atteint/échoué ; truncated = arrêt anticipé (ex: limite de temps) — cf. page Modélisation ▸ Reinforcement Learning — composants &amp; boucle"],
    ["Afficher l'environnement visuellement","env.**render**()","nécessite render_mode=\"human\" passé à gym.make() — inutile (et ralentit beaucoup) pendant un entraînement, à réserver pour observer/déboguer un agent"],
    ["Libérer l'environnement","env.**close**()","surtout utile avec du rendering ou plusieurs environnements en parallèle"],
  ]},
  {id:"dl-rl-qlearning-syntax", group:"rl", subgroup:"Reinforcement Learning", title:"Q-Learning manuel — Q-table avec NumPy", rows:[
    ["Initialiser la Q-table","q = np.**zeros**((env.observation_space.n, env.action_space.n))","une ligne par état, une colonne par action — toutes les valeurs Q(s,a) partent à 0, l'agent n'a encore rien appris"],
    ["Choisir une action — epsilon-greedy","if np.random.random() < epsilon:\n    action = env.action_space.sample()  # explore\nelse:\n    action = np.**argmax**(q[state, :])  # exploite","cf. page Modélisation ▸ Exploration vs Exploitation ; np.argmax = indice de l'action à la plus haute valeur Q connue pour cet état"],
    ["Mettre à jour la Q-table — équation de Bellman","q[state, action] += learning_rate * (\n    reward + discount_factor * np.**max**(q[new_state, :]) - q[state, action]\n)","traduction directe de la formule Q(s,a) ← Q(s,a) + α[R + γ·maxQ(s',a') − Q(s,a)] — cf. page Modélisation ▸ Q-Learning"],
    ["Décroître epsilon au fil des épisodes","epsilon = max(epsilon - epsilon_decay, 0)","explore moins et exploite davantage à mesure que l'agent apprend — jamais en dessous de 0"],
  ]},
  {id:"dl-rl-sb3-syntax", group:"rl", subgroup:"Reinforcement Learning", title:"Stable Baselines3 — entraîner un agent", rows:[
    ["Rendre l'environnement compatible SB3","from stable_baselines3.common.vec_env import DummyVecEnv\nenv = **DummyVecEnv**([lambda: env])","obligatoire avant de le passer à un modèle SB3 — standardise l'API attendue, même avec un seul environnement (facilite le passage à plusieurs en parallèle plus tard)"],
    ["Créer un modèle RL sur un environnement","from stable_baselines3 import DQN\nmodel = **DQN**(\"MlpPolicy\", env, tensorboard_log=\"./logs/\")","\"MlpPolicy\" = réseau Dense classique, adapté aux observations non-image — cf. page Modélisation ▸ Deep Q-Network (DQN) ; tensorboard_log = dossier de logs pour suivre l'entraînement (TensorBoard)"],
    ["Entraîner avec Policy Gradient (PPO)","from stable_baselines3 import **PPO**\nmodel = PPO(\"MlpPolicy\", env)","même API que DQN (.learn/.save/.predict) — ajuste la policy directement plutôt qu'une Q-table/Q-network ; seul algorithme adapté à des actions CONTINUES — cf. page Modélisation ▸ Deep Q-Network (DQN)"],
    ["Entraîner l'agent","model.**learn**(total_timesteps=10_000, progress_bar=True)","total_timesteps = nombre d'interactions avec l'environnement (pas des epochs classiques) ; progress_bar = barre de progression tqdm"],
    ["Suivre/piloter l'entraînement avec un callback","from stable_baselines3.common.callbacks import BaseCallback\n\nclass MyCallback(BaseCallback):\n    def _on_step(self) -> bool:\n        # appelé à CHAQUE pas d'entraînement\n        return True  # False = arrête l'entraînement\n\nmodel.learn(total_timesteps=10_000, **callback**=MyCallback())","permet de logger des métriques custom (récompense par épisode...) ou d'arrêter dynamiquement l'entraînement (ex: plateau de performance détecté) en retournant False"],
    ["Évaluer l'agent entraîné","from stable_baselines3.common.evaluation import evaluate_policy\nevaluate_policy(model, env)","exécute plusieurs épisodes SANS apprendre — mesure récompense moyenne et longueur d'épisode"],
    ["Sauvegarder / charger un modèle","model.**save**(model_path)\nmodel = DQN.**load**(model_path, env=env)","même logique que la sauvegarde d'un modèle Keras (cf. page Syntaxes ▸ Sauvegarder &amp; charger un modèle)"],
    ["Utiliser le modèle entraîné","action, info = model.**predict**(state)\nstate, reward, terminated, truncated, info = env.step(action)","équivalent d'un .predict() — répéter jusqu'à terminated/truncated (environnement BRUT, non wrappé)"],
    ["Boucle complète sur un environnement wrappé DummyVecEnv","obs = env.reset()\ndone = [False]\nwhile not done[0]:\n    action, _ = model.predict(obs)\n    obs, reward, done, info = env.**step**(action)","⚠️ une fois wrappé par DummyVecEnv, .step() renvoie 4 éléments (terminated/truncated FUSIONNÉS en un seul done), contre 5 pour un environnement brut (ligne ci-dessus) — ne pas mélanger les deux formes"],
  ]},
  {id:"dl-rl-custom-env-syntax", group:"rl", subgroup:"Reinforcement Learning", title:"Créer un environnement Gymnasium personnalisé", rows:[
    ["Définir l'espace d'actions/d'observation","from gymnasium import spaces\nself.action_space = spaces.**Discrete**(4)\nself.observation_space = spaces.**Box**(low=0, high=self.size-1, shape=(2,), dtype=np.int32)","Discrete = choix parmi n valeurs (ex: 4 directions) ; Box = vecteur continu/discret borné ; Dict = combiner plusieurs sous-espaces (ligne suivante)"],
    ["Combiner plusieurs observations","self.observation_space = spaces.**Dict**({\n    \"agent\": spaces.Box(low=0, high=self.size-1, shape=(2,), dtype=np.int32),\n    \"target\": spaces.Box(low=0, high=self.size-1, shape=(2,), dtype=np.int32),\n})","observation = dict {\"agent\": [...], \"target\": [...]} — cf. note Modélisation sur MultiInputPolicy"],
    ["Sous-classer gym.Env","class CustomGridEnv(**gym.Env**):\n    def reset(self, seed=None, options=None):\n        ...\n        return observation, info\n\n    def step(self, action):\n        ...\n        return observation, reward, terminated, truncated, info","même signature reset()/step() qu'un environnement Gymnasium natif — cf. page Modélisation ▸ [Créer son propre environnement Gymnasium](#dl-rl-custom-env)"],
    ["Vectoriser un environnement personnalisé pour SB3","from stable_baselines3.common.env_util import make_vec_env\nvec_env = **make_vec_env**(lambda: CustomGridEnv(), n_envs=1)","équivalent de DummyVecEnv([lambda: env]) — nécessaire pour entraîner avec Stable Baselines3"],
    ["Policy pour une observation en dictionnaire","model = DQN(\"**MultiInputPolicy**\", vec_env)","obligatoire si observation_space est un spaces.Dict — \"MlpPolicy\" lève une ValueError dans ce cas"],
    ["Prédiction déterministe (pas d'exploration)","action, _states = model.predict(obs, **deterministic**=True)","toujours l'action de plus haute Q-value plutôt qu'une action échantillonnée selon la policy — utile pour observer un comportement stable une fois l'agent entraîné"],
  ]},
];
